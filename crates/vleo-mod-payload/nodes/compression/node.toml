# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "pay_compression"
label = "Image compression ratio"
folder = "compression"   # frozen at seed; renaming the label never moves the directory
subsystem = "pay"
parent = "pay_optical"
kind = "declared"
owner = "payload"
tier = "A"
state = "published"

[question]
text = "By what factor is the imagery compressed before downlink?"

[maths]
expression = "CR_img = 3"
source = "ccsds122"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "CR_img"
type = "Ratio"
unit = "One"
lower = 1.0
upper = 20.0
reason_lower = "unity is no compression, the limit"
reason_upper = "above 20:1 the compression is lossy enough to destroy the radiometry the product is sold on"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 3.0
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

