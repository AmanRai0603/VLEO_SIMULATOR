# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "pay_gsd_detector"
label = "Detector-limited ground sample"
folder = "gsd_detector"   # frozen at seed; renaming the label never moves the directory
subsystem = "pay"
parent = "pay_optical"
kind = "computed"
owner = "payload"
tier = "A"
state = "published"

[question]
text = "What is the finest detail the detector can sample, whatever the optics?"

[maths]
expression = "GSD = h*p/f"
source = "larson_wertz"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "GSD_p"
type = "Length"
unit = "Metre"
lower = 0.001
upper = 1000.0
reason_lower = "below a millimetre the relation is being fed a focal length that is not physical"
reason_upper = "above a kilometre no product in this design is being made"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "h"
var = "orbit_altitude"
type = "Length"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "p"
var = "pay_pixel_pitch"
type = "Length"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "f"
var = "pay_focal_length"
type = "Length"

[[algorithm.step]]
number = 1
text = "project one pixel onto the ground through the focal length"
binds = "g"
type = "Length"

[view]
kind = "number"

