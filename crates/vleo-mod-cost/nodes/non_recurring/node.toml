# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "cost_non_recurring"
label = "Non-recurring engineering cost"
folder = "non_recurring"   # frozen at seed; renaming the label never moves the directory
subsystem = "cost"
parent = "sys_cost"
kind = "computed"
owner = "programme"
tier = "C"
layer = 2   # 1 management · 2 the system · 3 subsystem · 4 the run
state = "published"

[question]
text = "What does developing the design cost, once?"

[maths]
expression = "C = a*m_dry^b, escalated"
source = "nasa_cer"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "C_nre"
type = "Money"
unit = "MillionUsDollar"
lower = 0.0
upper = 100000.0
reason_lower = "a cost cannot be negative"
reason_upper = "above 100 billion the relation is being fed a mass it was not fitted on"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "m"
var = "mass_dry"
type = "Mass"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "i"
var = "cost_inflation"
type = "Ratio"

[[algorithm.step]]
number = 1
text = "evaluate the non-recurring cost estimating relation on dry mass and escalate to 2026"
binds = "c"
type = "Money"

[view]
kind = "number"

