[package]
name = "vleo-mod-cost"
description = "RING 3 — cost. What a decision costs, downstream of a selected design and never upstream of it."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
vleo-core = { workspace = true }

[build-dependencies]
# The module list is an aggregate: built here, never committed, because every
# node added would otherwise touch one file and every merge would conflict in
# generated content nobody is allowed to edit.

[lints.rust]
unsafe_code = "forbid"
