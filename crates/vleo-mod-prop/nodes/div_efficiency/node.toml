# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "prop_div_efficiency"
label = "Beam divergence efficiency"
folder = "div_efficiency"   # frozen at seed; renaming the label never moves the directory
subsystem = "prop"
parent = "prop_thruster"
kind = "declared"
owner = "propulsion"
tier = "A"
state = "published"

[question]
text = "How much thrust is lost because the beam is not perfectly axial?"

[maths]
expression = "alpha_div = 0.97"
source = "romano2021"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "alpha_div"
type = "Ratio"
unit = "One"
lower = 0.7
upper = 1.0
reason_lower = "below 0.7 the beam is a cloud rather than a beam"
reason_upper = "unity is a perfectly collimated beam, which does not exist"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 0.97
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

