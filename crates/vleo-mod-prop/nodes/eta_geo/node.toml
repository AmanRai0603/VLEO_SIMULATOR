# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "prop_eta_geo"
label = "Intake geometric transmission"
folder = "eta_geo"   # frozen at seed; renaming the label never moves the directory
subsystem = "prop"
parent = "sys_keep_orbit"
kind = "declared"
owner = "propulsion"
tier = "A"
layer = 2   # 1 management · 2 the system · 3 subsystem · 4 the run
state = "published"

[question]
text = "What fraction of the mouth is open duct, and what fraction of a hyperthermal beam does that duct pass?"

[maths]
expression = "eta_geo = 0.9"
source = "romano2021"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "eta_geo"
type = "Ratio"
unit = "One"
lower = 0.1
upper = 1.0
reason_lower = "below 10% the duct blocks more than it passes and is not an intake"
reason_upper = "unity means a perfectly open mouth with no structure, which is not manufacturable"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 0.9
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

