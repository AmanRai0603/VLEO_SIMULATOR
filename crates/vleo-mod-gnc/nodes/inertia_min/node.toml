# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "gnc_inertia_min"
label = "Smallest principal moment of inertia"
folder = "inertia_min"   # frozen at seed; renaming the label never moves the directory
subsystem = "gnc"
parent = "sys_point"
kind = "declared"
owner = "gnc"
tier = "A"
layer = 2   # 1 management · 2 the system · 3 subsystem · 4 the run
state = "published"

[question]
text = "What is the smallest principal moment of inertia?"

[maths]
expression = "I_min = 1.2"
source = "orbitt_case_c1"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "I_min"
type = "Ratio"
unit = "One"
lower = 0.01
upper = 1000.0
reason_lower = "below 0.01 kg.m2 there is no vehicle"
reason_upper = "cannot exceed the largest principal moment"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 1.2
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

