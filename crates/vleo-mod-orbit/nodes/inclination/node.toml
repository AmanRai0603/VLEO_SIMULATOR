# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "orbit_inclination"
label = "Orbit inclination"
folder = "inclination"   # frozen at seed; renaming the label never moves the directory
subsystem = "orbit"
parent = "l3_x_envorbit"
kind = "declared"
owner = "systems"
tier = "A"
layer = 3   # 1 management · 2 the system · 3 subsystem · 4 the run
# Where this row sits among its siblings, as whoever wrote them ordered
# them. Folders sort alphabetically and a tree that reads alphabetically
# is a tree nobody wrote: `Achievable lifetime` before `Specific impulse`
# reverses the order the source put them in, and the order is part of
# what the source said.
order = 511
state = "published"

[question]
text = "What is the angle between the orbit plane and the equator?"

[maths]
expression = "i = 96.6"
source = "orbitt_case_c1"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "i"
type = "Angle"
unit = "Degree"
lower = 0.0
upper = 180.0
reason_lower = "inclination is defined on 0..180 degrees"
reason_upper = "inclination is defined on 0..180 degrees"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 96.6
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

