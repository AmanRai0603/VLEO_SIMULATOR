# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "mass_avionics"
label = "Avionics mass"
folder = "avionics"   # frozen at seed; renaming the label never moves the directory
subsystem = "mass"
parent = "mass_aero"
kind = "declared"
owner = "avionics"
tier = "A"
state = "published"

[question]
text = "What do the on-board computer, harness electronics and data handling weigh?"

[maths]
expression = "m_av = 9"
source = "orbitt_case_c1"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "m_av"
type = "Mass"
unit = "Kilogram"
lower = 0.5
upper = 200.0
reason_lower = "below half a kilogram no avionics set exists"
reason_upper = "above 200 kg the avionics are not in this class"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 9.0
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

