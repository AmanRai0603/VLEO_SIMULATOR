# Known-good values, and where each came from. An expected value may
# never be produced by the code under test.
#
# The study's own pair is NOT used here: its sum is the prior
# implementation's published answer, and that may not be a fixture. It is in
# parity.csv beside this sheet.

[[fixture]]
label = "the sustained Ap level with the daily departure this row USED to be handed, before sw_ap_daily_band_spread was conditioned on level. Kept as an arithmetic case: the conditioned departure at Ap 26.70 is 63.85, not 15.00"
expect = 41.6971614919
tolerance = 1e-12
provenance = "independent-derivation"
source = "noaa_swpc"
inputs = { sustained = 26.69530964, daily = 15.0018518519 }

[[fixture]]
label = "round numbers, checkable without a calculator"
expect = 40.0
tolerance = 1e-12
provenance = "independent-derivation"
source = "noaa_swpc"
inputs = { sustained = 25.0, daily = 15.0 }

[[fixture]]
label = "a higher sustained level with a narrower daily departure — the terms are independent and the row must not assume otherwise"
expect = 42.0
tolerance = 1e-12
provenance = "independent-derivation"
source = "noaa_swpc"
inputs = { sustained = 30.0, daily = 12.0 }
