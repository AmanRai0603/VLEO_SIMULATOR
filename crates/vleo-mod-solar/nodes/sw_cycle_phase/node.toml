# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "sw_cycle_phase"
label = "Solar cycle phase"
folder = "sw_cycle_phase"   # frozen at seed; renaming the label never moves the directory
subsystem = "solar"
parent = "l3_solar"
kind = "computed"
owner = "environment"
tier = "A"
criticality = "minor"   # minor | significant — significant means two reviewers and a differential fill
layer = 3   # 1 management · 2 the system · 3 subsystem · 4 the run
order = 1337
state = "published"

# No migrated_from and no parity grid: the numbers are statistics of the record
# rather than a closed form, and there is no MATLAB here to run. Same reasoning
# as every measured row in this group.

[question]
text = "How far through its solar cycle is the mission epoch?"
note = "Zero at the cycle's start and one at its end. This is the row the whole cycle-dependent half of the subsystem turns on: at the declared epoch it is 0.619, which is past maximum on the declining side — the storm-rich phase."

[maths]
expression = "phase(T) = (T - start_of_cycle) / mean_cycle_length"
source = "noaa_swpc"

# WHERE THIS RELATION CAME FROM. Prose, outside the sheet hash: correcting a
# sentence here does not invalidate a generated artefact. The tab before this one
# states the relation; this says why it is that relation, and the tab after it
# shows what the machine made of it.
[theory]
why = """
A design does not need to know which cycle it flies in so much as WHERE in
one. Activity is a strong function of phase and a much weaker function of
which cycle is being lived through, which is what makes a phase-indexed
climatology possible at all. The difficulty in the relation is not the
definition, which is a division. It is that the current cycle has not
finished, so the denominator does not exist yet — and every consequence of
this row follows from which length is put there in its place.
"""
reading = """
0.6194 is not 61.9 per cent of anything physical. The cycle is not
symmetric: the phase-stacked mean F10.7 climbs from 72 sfu at phase 0.025 to
165 at 0.425 and then takes twice as long to fall back to 68, so equal steps
in phase are not equal steps in activity and the number cannot be read as a
proxy for one. sw_mean_cycle_level is the row that converts. The answer also
inherits the guess about cycle 25's length in full: if 25 runs longer than
11.44 years this phase is too large and the design is being sized against a
quieter part of the cycle than it will meet.
"""

[[theory.step]]
text = """
Define the phase as the fraction of a cycle elapsed — zero at the opening
minimum, one at the next. It needs two things the row must get from
somewhere: a start and a length.
"""
math = "phase(T) = (T - start) / length"

[[theory.step]]
text = """
The start is not a problem. The published boundaries give cycle 25 an
opening of 2019-12-01, day 7274, and the epoch is 2588 days past it.
"""
math = "T - start = 9862 - 7274 = 2588 d"

[[theory.step]]
text = """
The length is the problem, and it is worth being explicit about why.
solar_cycles.csv gives cycle 25 an end of 2025-12-15 and a length of 6.04
years, and both are artefacts of where the RECORD stops rather than where
the cycle stops. Taking that length at face value puts the epoch past the
end of its own cycle and the row would refuse a date that is only a year
beyond the archive.
"""
math = "2588 / (6.04 * 365.25) = 1.17 — above the declared bound of 1"

[[theory.step]]
text = """
The two COMPLETE cycles in the table ran 11.88 and 11.00 years. Their mean
is the only length the record supports for a cycle that has not ended, and
it is what the study's own meanCycleAt folds with beyond its last cycle, so
using it is a port rather than an invention.
"""
math = "L = (11.88 + 11.00) / 2 = 11.44 yr = 4178.46 d"

[[theory.step]]
text = """
Which puts the declared epoch a little past the middle of its cycle, on the
declining side — the storm-rich part, and the same part of the cycle the
exceedance rows independently point at.
"""
math = "phase = 2588 / 4178.46 = 0.6194"

[[theory.step]]
text = """
The bound at one is load-bearing rather than tidy. Dividing by a mean
instead of the truth means a date far enough out returns a phase above one,
and a phase above one says the date belongs to a cycle this row cannot name.
Refusing is the only honest answer available: folding round would report an
early-cycle phase for a date whose cycle number nothing in the data fixes.
"""


[[assumption]]
text = "The epoch is past the record, so the phase is FOLDED with a mean cycle length rather than measured"
fails_when = "solar_cycles.csv gives cycle 25 a start of 2019-12-01 and an end of 2025-12-15 with a length of 6.04 years. That end and that length are artefacts of where the RECORD stops, not where the cycle stops: cycles 23 and 24 ran 11.88 and 11.00 years. The epoch, day 9862, is 382 days past that recorded end, so no cycle in the table contains it. The phase is therefore computed against the mean of the two COMPLETE cycles, 11.44 years, which is what prf_design's meanCycleAt does beyond its last cycle. If cycle 25 turns out short or long the phase moves, and every row reading it moves with it"

[[assumption]]
text = "It is a fraction of a cycle and not a measure of activity"
fails_when = "phase 0.619 does not mean 61.9% of anything physical. The cycle is not symmetric — the record's mean F10.7 by phase rises from 72 sfu at phase 0.025 to about 165 at 0.425 and falls to 68 by 0.975, so the rise is faster than the decline and equal phase steps are not equal activity steps. sw_mean_cycle_level is the row that turns a phase into a flux; reading the phase as a proxy for activity would get the asymmetry backwards"

[[assumption]]
text = "It cannot exceed one, and an epoch late in an unobserved cycle would want it to"
fails_when = "the fold divides elapsed time by a mean length, so an epoch more than 11.44 years past 2019-12-01 gives a phase above 1 and the declared bound refuses it. That is correct rather than convenient: past one cycle length the right answer is a new cycle number, and this row cannot supply one because the data does not say when cycle 26 begins"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "phase"
type = "Ratio"
unit = "One"
lower = 0.0
upper = 1.0
reason_lower = "a phase is a fraction of the way through a cycle and cannot be negative. A negative value means the epoch precedes the cycle it was assigned to"
reason_upper = "one is the end of the cycle. Above it the epoch belongs to the next cycle, and this row cannot name it because the record does not contain its start — so it refuses instead of folding round silently"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "epoch"
var = "sys_mission_requirements_mission_epoch"
type = "Time"

[[algorithm.step]]
number = 1
text = "divide the time elapsed since the current cycle's start by the mean length of the complete cycles"
binds = "out"
type = "Ratio"

[data]
# Pinned to solar-weather@2026.09.14; this list can only pin the NAME. The
# numbers must be re-measured if the bundle version changes.
bundles = ["solar-weather"]

[view]
kind = "line"
over = "sys_mission_requirements_mission_epoch"
points = 60
