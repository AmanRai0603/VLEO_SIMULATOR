# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "thm_absorptivity"
label = "Solar absorptivity"
folder = "absorptivity"   # frozen at seed; renaming the label never moves the directory
subsystem = "thm"
parent = "thermal"
kind = "declared"
owner = "thermal"
tier = "A"
state = "published"

[question]
text = "What fraction of incident sunlight does the external surface absorb?"

[maths]
expression = "alpha_s = 0.3"
source = "larson_wertz"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "alpha_s"
type = "Ratio"
unit = "One"
lower = 0.05
upper = 1.0
reason_lower = "below 0.05 no space-qualified coating is that reflective after atomic-oxygen exposure"
reason_upper = "unity is a perfect black body, the limit rather than a coating"

[value]
# A number a person picked. It carries the same discipline as a
# fixture, because every margin in the design is built out of these.
number = 0.3
confirmed_by = "A. Rai / 2026-09-01"

[view]
kind = "number"

