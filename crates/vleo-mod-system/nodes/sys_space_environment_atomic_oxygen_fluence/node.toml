# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "sys_space_environment_atomic_oxygen_fluence"
label = "Atomic oxygen fluence"
folder = "sys_space_environment_atomic_oxygen_fluence"   # frozen at seed; renaming the label never moves the directory
subsystem = "sys"
parent = "sys_space_environment"
kind = "computed"
owner = "environment"
tier = ""
layer = 2   # 1 management · 2 the system · 3 subsystem · 4 the run
# Where this row sits among its siblings, as whoever wrote them ordered
# them. Folders sort alphabetically and a tree that reads alphabetically
# is a tree nobody wrote: `Achievable lifetime` before `Specific impulse`
# reverses the order the source put them in, and the order is part of
# what the source said.
order = 311
state = "empty"

# SEEDED — the folder exists and nothing has been specified in it.
# Every field below is required before anything can be generated. An
# open field blocks generation, which is the mechanism: ambiguity
# becomes a blocking item on an engineer's screen rather than
# something an implementer resolves silently.
#
# STILL SEEDED AFTER §20 STEP 6, ON PURPOSE.
#
# No subsystem computes atomic oxygen fluence. It follows from density, which is
# also still seeded and for the same reason, so this row is waiting on a row that
# is waiting on a subsystem that does not exist.
#
# sys_mass_and_aero_erosion_depth reads this row and is waiting on it. Fluence is
# the number that sizes a material choice at VLEO: atomic oxygen erodes polymers
# and unprotected silver at rates measured in cubic centimetres per incident
# atom, and the depth over a mission is the integral of number density times
# relative velocity times exposure time.
#
# What writing it would need, in order: a density row that publishes the atomic
# oxygen NUMBER density rather than total mass density, the relative velocity
# sys_mass_and_aero_relative_velocity is itself waiting for, and the mission
# duration, which is the one of the three that already exists.

[question]
text = ""

[maths]
expression = ""
source = ""

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
# A unit is a decision, not data. None of these is filled yet.
symbol = ""
type = ""
unit = ""
lower = 0.0
upper = 0.0
reason_lower = ""
reason_upper = ""

[view]
kind = "number"

