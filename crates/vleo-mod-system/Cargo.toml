# GENERATED by tools/seed_tree.py from the tree. One crate per owner: a sibling
# this crate did not declare will not compile, which is the isolation rule made
# a manifest line rather than a convention.
[package]
name = "vleo-mod-system"
description = "RING 3 - LAYER 2 - the system layer. What the satellite is made of, and what reads what."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
vleo-core = { workspace = true }

[build-dependencies]

[lints.rust]
unsafe_code = "forbid"
