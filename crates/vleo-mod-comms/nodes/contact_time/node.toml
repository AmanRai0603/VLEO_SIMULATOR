# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "com_contact_time"
label = "Maximum contact time per pass"
folder = "contact_time"   # frozen at seed; renaming the label never moves the directory
subsystem = "com"
parent = "l3_ttc"
kind = "computed"
owner = "comms"
tier = "A"
layer = 3   # 1 management · 2 the system · 3 subsystem · 4 the run
# Where this row sits among its siblings, as whoever wrote them ordered
# them. Folders sort alphabetically and a tree that reads alphabetically
# is a tree nobody wrote: `Achievable lifetime` before `Specific impulse`
# reverses the order the source put them in, and the order is part of
# what the source said.
order = 649
state = "published"

[question]
text = "How long is one ground station pass?"

[maths]
expression = "t = T_orb*lambda/pi"
source = "larson_wertz"

[[assumption]]
text = "A pass directly overhead"
fails_when = "the mean over many passes is roughly 60% of this, so a downlink volume computed from it will not be achieved"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "t_con"
type = "Time"
unit = "Minute"
lower = 0.0
upper = 60.0
reason_lower = "a contact time cannot be negative"
reason_upper = "above an hour is not a low-orbit pass"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "lam"
var = "orbit_earth_central_angle"
type = "Angle"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "p"
var = "orbit_period"
type = "Time"

[[algorithm.step]]
number = 1
text = "take the fraction of the revolution spent inside the access circle"
binds = "t"
type = "Time"

[view]
kind = "number"

