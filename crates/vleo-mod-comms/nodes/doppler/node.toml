# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "com_doppler"
label = "Doppler shift at closest approach"
folder = "doppler"   # frozen at seed; renaming the label never moves the directory
subsystem = "com"
parent = "l3_ttc"
kind = "computed"
owner = "comms"
tier = "A"
layer = 3   # 1 management · 2 the system · 3 subsystem · 4 the run
state = "published"

[question]
text = "How far does the carrier move in frequency as the satellite passes?"

[maths]
expression = "df = f*v/c"
source = "larson_wertz"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "df"
type = "Frequency"
unit = "Megahertz"
lower = 0.0
upper = 10.0
reason_lower = "a shift magnitude cannot be negative"
reason_upper = "above 10 MHz the receiver acquisition range is exceeded and the link never locks"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "f"
var = "com_frequency"
type = "Frequency"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "v"
var = "orbit_ground_track_speed"
type = "Velocity"

[[algorithm.step]]
number = 1
text = "apply the first-order Doppler relation at the ground track speed"
binds = "d"
type = "Frequency"

[view]
kind = "number"

