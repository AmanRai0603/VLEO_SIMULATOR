[package]
name = "vleo-mod-comms"
description = "RING 3 — telemetry, tracking and the payload downlink. The link budget, the achievable rate and what actually reaches the ground each day."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
vleo-core = { workspace = true }

[build-dependencies]
# The module list is an aggregate: built here, never committed, because every
# node added would otherwise touch one file and every merge would conflict in
# generated content nobody is allowed to edit.

[lints.rust]
unsafe_code = "forbid"
