[package]
name = "vleo-mod-subsystem"
description = "LAYER 3 — the fifteen subsystem layers — one team each, reached only through an interface node. It holds sheets and, for now, no code: every row in it is seeded and nothing is generated from a sheet nobody has specified."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
vleo-core = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
