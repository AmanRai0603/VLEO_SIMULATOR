[package]
name = "vleo-mod-mission"
description = "RING 3 — mission performance and the KPI closures. Coverage, revisit, latency, availability — and the required-against-achieved comparison, identical at every boundary."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
vleo-core = { workspace = true }

[build-dependencies]
# The module list is an aggregate: built here, never committed, because every
# node added would otherwise touch one file and every merge would conflict in
# generated content nobody is allowed to edit.

[lints.rust]
unsafe_code = "forbid"
