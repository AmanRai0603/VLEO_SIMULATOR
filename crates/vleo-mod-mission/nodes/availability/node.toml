# The sheet. This is the only file in this folder that must be written by
# hand, and every other artefact here is generated from it. A hand edit
# outside a numbered HOLE block in model.rs fails the regeneration diff.
id = "mis_availability"
label = "Constellation service availability"
folder = "availability"   # frozen at seed; renaming the label never moves the directory
subsystem = "mis"
parent = "sys_performance"
kind = "computed"
owner = "operations"
tier = "B"
layer = 2   # 1 management · 2 the system · 3 subsystem · 4 the run
state = "published"

[question]
text = "What fraction of the time is the service available?"

[maths]
expression = "A = P(at least ceil(0.8*N) of N satellites up)"
source = "larson_wertz"

[output]
# The node's one answer. The variable id is the node id: one small
# question, one answer, one folder, one row on the tree.
symbol = "A_svc"
type = "Ratio"
unit = "One"
lower = 0.0
upper = 1.0
reason_lower = "an availability cannot be negative"
reason_upper = "an availability cannot exceed one"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "a"
var = "mis_unit_availability"
type = "Ratio"

[[input]]
# The consumer declares what it expects. Assembly refuses a mismatch
# against what the producer publishes — which is the only place the
# whole graph is visible and therefore the only place to ask.
binding = "n"
var = "mis_satellites"
type = "Ratio"

[[algorithm.step]]
number = 1
text = "evaluate the binomial probability that at least 80% of the constellation is operational"
binds = "av"
type = "Ratio"

[contributes]
kpis = ["kpi_availability"]

[view]
kind = "number"

