<!-- GENERATED by `cargo xtask variables`. Do not edit: the sheets are the source. -->

# The variable register

Every row in the tree, with the unit it publishes in, the range over which it
is declared valid, and the reason for each bound. A guard whose reason is not
written down gets deleted by the next person who finds it awkward, so the
reasons are part of the register rather than a comment in the code.

**1361 rows** — 668 a person picked, 693 worked out. Two thirds of any design tree is
the first kind: cheaper than a computed node, and not free, because every margin
in the design is built out of them.


## `acs` — Attitude control sizing

### `l3_acs_ach_01` — Attitude deadband

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_02` — Slew rate capability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_03` — Magnetic dipole authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_04` — Control bandwidth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_05` — Settling time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_06` — Control torque authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_07` — Momentum storage capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_08` — Momentum dumping frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_ach_09` — Aerodynamic trim angle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_interface` — Attitude control sizing — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_01` — Attitude deadband

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_02` — Slew rate capability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_03` — Magnetic dipole authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_04` — Control bandwidth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_05` — Settling time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_06` — Control torque authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_07` — Momentum storage capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_08` — Momentum dumping frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_acs_req_09` — Aerodynamic trim angle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `aero` — Mass and aero

### `aero_accommodation` — Energy accommodation coefficient

> How completely does an incoming molecule thermalise with the surface before leaving?

| | |
|---|---|
| symbol | `alpha` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | aero |
| evidence tier | B |
| relation | `alpha = K*n_O*T_inf/(1 + K*n_O*T_inf),  K = 7.5e-17` |
| source | `moe2005` |
| valid over | 0 … 1 - |

- **lower bound** — accommodation is a fraction and cannot be negative
- **upper bound** — full accommodation is one; above it the reflected molecules would carry away energy the surface does not have
- **reads** — `env_atomic_oxygen_density`, `env_exospheric_temperature`
- **read by** — `aero_drag_coefficient`
- **assumes** The surface is covered in adsorbed atomic oxygen and behaves as that coverage dictates — fails when a freshly cleaned or a fluorinated surface adsorbs far less, and its drag differs by tens of per cent from this
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

A design that assumes a fixed accommodation has a margin that moves when the Sun does.

### `aero_ao_fluence` — Atomic oxygen fluence

> How many oxygen atoms strike a forward-facing surface over the mission?

| | |
|---|---|
| symbol | `F_AO` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | mass |
| evidence tier | B |
| relation | `F = n_O*V*t` |
| source | `moe2005` |
| valid over | 0 … 100000000000000000000000000 - |

- **lower bound** — a fluence cannot be negative
- **upper bound** — above 1e26 atoms per square metre no known external material survives, so the design is not a design
- **reads** — `env_atomic_oxygen_density`, `orbit_velocity`, `orbit_mission_duration`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_service_lifetime
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Polyimide erodes at about 3e-24 cm3 per incident atom, so 1e22 removes roughly 30 micrometres of it.

### `aero_appendage_area` — Appendage frontal area

> How much frontal area do the array, antennas and radiators add beyond the body?

| | |
|---|---|
| symbol | `A_app` |
| type | `Area` |
| unit | m^2 |
| kind | declared |
| owner | mass |
| evidence tier | A |
| relation | `A_app = 0.05` |
| source | `orbitt_case_c1` |
| declared value | **0.05** m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 5 m^2 |

- **lower bound** — an area cannot be negative
- **upper bound** — above 5 m2 of appendage the design is an array with a satellite attached, and the drag closure is decided by the array alone
- **read by** — `aero_frontal_area`

Separated from the body area because it is the term a power or thermal decision moves, and the tree has to show that coupling.

### `aero_ballistic_coefficient` — Ballistic coefficient

> How well does this vehicle resist the atmosphere, per unit of its own mass?

| | |
|---|---|
| symbol | `BC` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `BC = m/(Cd*A)` |
| source | `vallado2013` |
| valid over | 5 … 500 - |

- **lower bound** — below 5 kg/m2 the vehicle is a sail, and nothing in this band closes
- **upper bound** — above 500 kg/m2 the vehicle is denser than any spacecraft ever flown
- **reads** — `mass_wet`, `aero_drag_coefficient`, `aero_frontal_area`
- **read by** — `orbit_decay_rate`, `orbit_lifetime_uncontrolled`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Higher is better here. A cubesat is around 50; a slender VLEO platform reaches 150, and that difference is worth tens of kilometres of altitude.

### `aero_body_diameter` — Body diameter

> How wide is the vehicle across the flight direction?

| | |
|---|---|
| symbol | `D_b` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | mass |
| evidence tier | A |
| relation | `D_b = 0.6` |
| source | `orbitt_case_c1` |
| declared value | **0.6** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 3 m |

- **lower bound** — below 0.1 m no useful payload aperture fits
- **upper bound** — above 3 m the frontal area makes the air-breathing closure impossible at any altitude in the band
- **read by** — `aero_drag_coefficient`, `aero_frontal_area`

### `aero_body_length` — Body length

> How long is the vehicle along the flight direction?

| | |
|---|---|
| symbol | `L` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | mass |
| evidence tier | A |
| relation | `L = 2` |
| source | `orbitt_case_c1` |
| declared value | **2** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.3 … 10 m |

- **lower bound** — below 0.3 m the vehicle is smaller than a 3U cubesat and the multipayload mission does not fit in it
- **upper bound** — above 10 m the launch envelope and the free-molecular panel model both stop applying
- **read by** — `aero_drag_coefficient`, `env_knudsen`

### `aero_cp_cm_offset` — Centre of pressure to centre of mass offset

> How far behind the centre of mass does the aerodynamic force act?

| | |
|---|---|
| symbol | `x_cp` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `x_cp = 0.15` |
| source | `orbitt_case_c1` |
| declared value | **0.15** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 2 m |

- **lower bound** — a zero offset is neutrally stable, which is the limit and not a design
- **upper bound** — the offset cannot exceed the body length
- **read by** — `aero_torque`, `gnc_solar_torque`

Positive means aft, which is the stable configuration. Arranging that is most of what passive aerostability in VLEO means.

### `aero_drag_acceleration` — Drag deceleration

> What deceleration does the drag produce on this mass?

| | |
|---|---|
| symbol | `a_D` |
| type | `Acceleration` |
| unit | m/s^2 |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `a_D = D/m` |
| source | `vallado2013` |
| valid over | 0 … 0.1 m/s^2 |

- **lower bound** — drag deceleration cannot be negative
- **upper bound** — above 0.1 m/s2 the orbit decays within hours
- **reads** — `aero_drag_force`, `mass_wet`
- **read by** — `gnc_along_track_error`, `orbit_makeup_delta_v`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `aero_drag_coefficient` — Drag coefficient

> What is the drag coefficient of this body, at this speed ratio, with this surface?

| | |
|---|---|
| symbol | `Cd` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `Cd = P/sqrt(pi) + gamma*Q*Z + (gamma/2)*vr*(gamma*sqrt(pi)*Z + P)` |
| source | `doornbos2011` |
| valid over | 1.5 … 5 - |

- **lower bound** — no free-molecular body has a drag coefficient below 1.5; a lower value means the speed ratio or the accommodation is wrong
- **upper bound** — above 5.0 the geometry is not the slender body this model assumes
- **reads** — `aero_speed_ratio`, `aero_body_length`, `aero_body_diameter`, `aero_accommodation`, `env_wall_temperature`, `orbit_velocity`, `env_mean_molar_mass`, `env_knudsen`
- **read by** — `aero_ballistic_coefficient`, `aero_drag_force`
- **assumes** Free-molecular flow — a molecule leaving the surface never returns and never strikes an incoming one — fails when below Kn = 10, which is roughly 150 km for this body; the Knudsen number is an input so that the guard on it is visible here
- **evidence** 250 km cylinder, moderate activity — expect 2.85807 ± 0.001 relative, from `matlab_legacy` (independent-tool)

Treating this as a constant 2.2 is the single largest avoidable error in a VLEO drag estimate, and it is what most concept studies do.

### `aero_drag_force` — Drag force

> How hard is the atmosphere pulling the satellite backwards?

| | |
|---|---|
| symbol | `D` |
| type | `Force` |
| unit | mN |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `D = 0.5*rho*V^2*Cd*A` |
| source | `vallado2013` |
| valid over | 0 … 10000 mN |

- **lower bound** — drag cannot be negative
- **upper bound** — above 10 N the vehicle is decelerating at a rate no electric propulsion system can answer
- **reads** — `aero_dynamic_pressure`, `aero_drag_coefficient`, `aero_frontal_area`
- **read by** — `aero_drag_acceleration`, `aero_torque`, `prop_thrust_to_drag`
- **contributes to** — kpi_service_lifetime
- **evidence** 250 km, baseline vehicle — expect 0.0018957 ± 0.001 relative, from `vallado2013` (independent-derivation)

### `aero_dynamic_pressure` — Dynamic pressure

> What pressure does the flow exert per unit of drag coefficient and area?

| | |
|---|---|
| symbol | `q` |
| type | `Pressure` |
| unit | Pa |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `q = 0.5*rho*V^2` |
| source | `vallado2013` |
| valid over | 0 … 1 Pa |

- **lower bound** — dynamic pressure cannot be negative
- **upper bound** — above 1 Pa the vehicle is re-entering, not orbiting
- **reads** — `env_mass_density`, `orbit_velocity`
- **read by** — `aero_drag_force`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `aero_frontal_area` — Total frontal area

> What area does the flow actually see?

| | |
|---|---|
| symbol | `A` |
| type | `Area` |
| unit | m^2 |
| kind | computed |
| owner | mass |
| evidence tier | A |
| relation | `A = pi*D^2/4 + A_app` |
| source | `orbitt_case_c1` |
| valid over | 0.005 … 20 m^2 |

- **lower bound** — below 50 cm2 there is no vehicle
- **upper bound** — above 20 m2 nothing in this design closes
- **reads** — `aero_body_diameter`, `aero_appendage_area`
- **read by** — `aero_ballistic_coefficient`, `aero_drag_force`, `gnc_solar_torque`, `thm_absorbed_albedo`, `thm_absorbed_ir`, `thm_absorbed_solar`, `thm_aero_heating`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `aero_speed_ratio` — Molecular speed ratio

> How hyperthermal is the flow — does the gas arrive as a beam or as a cloud?

| | |
|---|---|
| symbol | `s` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | aero |
| evidence tier | A |
| relation | `s = V/sqrt(2*R*T/M)` |
| source | `sentman1961` |
| valid over | 1 … 30 - |

- **lower bound** — below a speed ratio of one the flow is thermal rather than hyperthermal, and Sentman's relations lose their meaning
- **upper bound** — above 30 the exponential terms underflow and the relations are numerically dead
- **reads** — `orbit_velocity`, `env_local_temperature`, `env_mean_molar_mass`
- **read by** — `aero_drag_coefficient`
- **evidence** 250 km, moderate activity — expect 8.6435 ± 0.005 relative, from `sentman1961` (independent-derivation)

### `aero_torque` — Aerodynamic disturbance torque

> How much torque does the drag apply about the centre of mass?

| | |
|---|---|
| symbol | `T_aero` |
| type | `Torque` |
| unit | N.m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `T = D*x_cp` |
| source | `larson_wertz` |
| valid over | 0 … 1 N.m |

- **lower bound** — a torque magnitude cannot be negative
- **upper bound** — above 1 N.m no reaction wheel that fits this vehicle can hold attitude
- **reads** — `aero_drag_force`, `aero_cp_cm_offset`
- **read by** — `gnc_total_disturbance`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

In VLEO this dominates gravity gradient and solar pressure by one to three orders of magnitude, which is why a control design carried over from a higher orbit is undersized here.


## `atthw` — Attitude hardware

### `l3_atthw_ach_01` — Number of reaction wheels

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_02` — Wheel speed limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_03` — Star tracker accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_04` — Gyro drift

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_05` — Earth magnetic field

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_06` — Aerodynamic torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_07` — Gravity gradient torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_08` — Solar radiation torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_ach_09` — First mode frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_interface` — Attitude hardware — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_n_01` — Attitude hardware · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_02` — Attitude hardware · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_03` — Attitude hardware · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_04` — Attitude hardware · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_05` — Attitude hardware · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_06` — Attitude hardware · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_07` — Attitude hardware · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_08` — Attitude hardware · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_09` — Attitude hardware · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_10` — Attitude hardware · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_11` — Attitude hardware · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_12` — Attitude hardware · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_13` — Attitude hardware · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_14` — Attitude hardware · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_15` — Attitude hardware · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_16` — Attitude hardware · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_17` — Attitude hardware · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_18` — Attitude hardware · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_19` — Attitude hardware · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_20` — Attitude hardware · internal — to be named (20)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_21` — Attitude hardware · internal — to be named (21)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_22` — Attitude hardware · internal — to be named (22)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_n_23` — Attitude hardware · internal — to be named (23)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_atthw_req_01` — Number of reaction wheels

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_02` — Wheel speed limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_03` — Star tracker accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_04` — Gyro drift

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_05` — Earth magnetic field

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_06` — Aerodynamic torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_07` — Gravity gradient torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_08` — Solar radiation torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_atthw_req_09` — First mode frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `closure` — Closure and cost — addition

### `l3_x_closure_interface` — Closure and cost — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `com` — TT&C and downlink

### `com_antenna_diameter` — Spacecraft antenna diameter

> How large is the spacecraft downlink antenna?

| | |
|---|---|
| symbol | `D_a` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `D_a = 0.3` |
| source | `orbitt_case_c1` |
| declared value | **0.3** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.02 … 2 m |

- **lower bound** — below 2 cm the aperture gain relation is not valid at this frequency
- **upper bound** — above 2 m the antenna is a drag surface larger than the vehicle
- **read by** — `com_antenna_gain`

### `com_antenna_efficiency` — Antenna aperture efficiency

> How efficiently does the antenna use its aperture?

| | |
|---|---|
| symbol | `eta_a` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `eta_a = 0.6` |
| source | `larson_wertz` |
| declared value | **0.6** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.2 … 0.9 - |

- **lower bound** — below 0.2 the aperture is badly illuminated and the design is not one considered here
- **upper bound** — above 0.9 no practical feed achieves it
- **read by** — `com_antenna_gain`, `com_gs_gain`

### `com_antenna_gain` — Spacecraft antenna gain

> How much does the spacecraft antenna concentrate the transmitted power?

| | |
|---|---|
| symbol | `G_t` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `G = eta*(pi*D/lambda)^2` |
| source | `larson_wertz` |
| valid over | -10 … 80 dB |

- **lower bound** — an aperture antenna does not have less than -10 dBi of gain
- **upper bound** — above 80 dBi is a deep-space aperture, not a spacecraft antenna
- **reads** — `com_antenna_diameter`, `com_frequency`, `com_antenna_efficiency`
- **read by** — `com_eirp`
- **evidence** 0.3 m at 8.2 GHz, 60% efficient — expect 26.00679804 ± 0.00000001 relative, from `larson_wertz` (independent-derivation)

### `com_atmospheric_loss` — Atmospheric and polarisation loss

> How much is lost to the atmosphere, rain and polarisation mismatch, in decibels?

| | |
|---|---|
| symbol | `L_atm` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `L_atm = 1.8` |
| source | `itu_p618` |
| declared value | **1.8** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 15 - |

- **lower bound** — an atmospheric loss cannot be negative
- **upper bound** — above 15 dB the link is not available at the stated elevation and frequency
- **read by** — `com_cn0`

### `com_cn0` — Carrier to noise density ratio

> How strong is the received carrier relative to the noise floor?

| | |
|---|---|
| symbol | `CN0` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `C/N0 = EIRP - L_fs - L_atm + G/T - 10*log10(k)` |
| source | `larson_wertz` |
| valid over | 20 … 120 dB |

- **lower bound** — below 20 dB-Hz no link in this design closes
- **upper bound** — above 120 dB-Hz the geometry is not one this tool covers
- **reads** — `com_eirp`, `com_path_loss`, `com_atmospheric_loss`, `com_g_over_t`
- **read by** — `com_data_rate`, `com_ebn0`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_contact_time` — Maximum contact time per pass

> How long is one ground station pass?

| | |
|---|---|
| symbol | `t_con` |
| type | `Time` |
| unit | min |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `t = T_orb*lambda/pi` |
| source | `larson_wertz` |
| valid over | 0 … 60 min |

- **lower bound** — a contact time cannot be negative
- **upper bound** — above an hour is not a low-orbit pass
- **reads** — `orbit_earth_central_angle`, `orbit_period`
- **read by** — `com_pass_volume`, `mis_latency`
- **assumes** A pass directly overhead — fails when the mean over many passes is roughly 60% of this, so a downlink volume computed from it will not be achieved
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_daily_volume` — Daily downlink capacity

> How much data can the system bring down in a day?

| | |
|---|---|
| symbol | `V_day` |
| type | `DataVolume` |
| unit | Gbit |
| kind | computed |
| owner | operations |
| evidence tier | B |
| relation | `V = V_pass*N_pass*A` |
| source | `larson_wertz` |
| valid over | 0 … 10000000 Gbit |

- **lower bound** — a data volume cannot be negative
- **upper bound** — above 10 petabits a day no ground segment in this design receives it
- **reads** — `com_pass_volume`, `com_passes_per_day`
- **read by** — `kpi_data_volume`
- **contributes to** — kpi_data_volume
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_data_rate` — Achievable downlink data rate

> How fast can data come down while keeping the required margin?

| | |
|---|---|
| symbol | `R_b` |
| type | `DataRate` |
| unit | Mbit/s |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `R = 10^((C/N0 - EbN0_req - L_imp - M)/10)` |
| source | `larson_wertz` |
| valid over | 0.001 … 100000 Mbit/s |

- **lower bound** — below 1 kbit/s the downlink cannot even carry housekeeping
- **upper bound** — above 100 Gbit/s no spacecraft modem in this class runs
- **reads** — `com_cn0`, `com_required_ebn0`, `com_implementation_loss`, `com_link_margin_required`
- **read by** — `com_ebn0`, `com_pass_volume`
- **contributes to** — kpi_data_volume
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_doppler` — Doppler shift at closest approach

> How far does the carrier move in frequency as the satellite passes?

| | |
|---|---|
| symbol | `df` |
| type | `Frequency` |
| unit | MHz |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `df = f*v/c` |
| source | `larson_wertz` |
| valid over | 0 … 10 MHz |

- **lower bound** — a shift magnitude cannot be negative
- **upper bound** — above 10 MHz the receiver acquisition range is exceeded and the link never locks
- **reads** — `com_frequency`, `orbit_ground_track_speed`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_ebn0` — Achieved energy per bit over noise density

> What energy per bit does the link actually deliver at the design rate?

| | |
|---|---|
| symbol | `EbN0` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `Eb/N0 = C/N0 - 10*log10(R)` |
| source | `larson_wertz` |
| valid over | -10 … 40 dB |

- **lower bound** — below -10 dB no coded link closes
- **upper bound** — above 40 dB the link is overdesigned by orders of magnitude
- **reads** — `com_cn0`, `com_data_rate`
- **read by** — `com_link_margin`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_eirp` — Effective isotropic radiated power

> How much power does the spacecraft appear to radiate towards the ground station?

| | |
|---|---|
| symbol | `EIRP` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `EIRP = 10*log10(P_t) + G_t - L_line` |
| source | `larson_wertz` |
| valid over | -20 … 90 dB |

- **lower bound** — below -20 dBW no link in this design closes
- **upper bound** — above 90 dBW the transmitter is not the one described
- **reads** — `com_tx_power`, `com_antenna_gain`, `com_line_loss`
- **read by** — `com_cn0`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_frequency` — Downlink carrier frequency

> On what frequency does the payload data come down?

| | |
|---|---|
| symbol | `f_c` |
| type | `Frequency` |
| unit | GHz |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `f_c = 8.2` |
| source | `itu_sa1862` |
| declared value | **8.2** GHz |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 40 GHz |

- **lower bound** — below 100 MHz the antenna does not fit on this spacecraft
- **upper bound** — above 40 GHz rain fade dominates and the ground segment is not the one costed here
- **read by** — `com_antenna_gain`, `com_doppler`, `com_gs_gain`, `com_path_loss`

### `com_g_over_t` — Ground station figure of merit

> What is the ground station's sensitivity, as gain over system noise temperature?

| | |
|---|---|
| symbol | `GT` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `G/T = G_r - 10*log10(T_s)` |
| source | `larson_wertz` |
| valid over | -30 … 60 dB |

- **lower bound** — below -30 dB/K no ground station in this network performs that badly
- **upper bound** — above 60 dB/K is a deep-space station
- **reads** — `com_gs_gain`, `com_system_noise_temperature`
- **read by** — `com_cn0`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_gs_antenna_diameter` — Ground station antenna diameter

> How large is the ground station antenna?

| | |
|---|---|
| symbol | `D_g` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `D_g = 5.4` |
| source | `orbitt_case_c1` |
| declared value | **5.4** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 35 m |

- **lower bound** — below 0.5 m no ground station in the costed network is that small
- **upper bound** — above 35 m is a deep-space aperture, not a commercial ground station
- **read by** — `com_gs_gain`

### `com_gs_gain` — Ground station antenna gain

> How much does the ground antenna concentrate the received power?

| | |
|---|---|
| symbol | `G_r` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `G = eta*(pi*D/lambda)^2` |
| source | `larson_wertz` |
| valid over | 0 … 80 dB |

- **lower bound** — a ground aperture of this class always has positive gain
- **upper bound** — above 80 dBi is a deep-space aperture
- **reads** — `com_gs_antenna_diameter`, `com_frequency`, `com_antenna_efficiency`
- **read by** — `com_g_over_t`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_gs_noise_temperature` — Ground station antenna noise temperature

> How much noise does the ground antenna see?

| | |
|---|---|
| symbol | `T_a` |
| type | `Temperature` |
| unit | K |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `T_a = 60` |
| source | `larson_wertz` |
| declared value | **60** K |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 10 … 300 K |

- **lower bound** — below 10 K requires cryogenic cooling that no commercial station in this network has
- **upper bound** — above 300 K the antenna is looking at the ground
- **read by** — `com_system_noise_temperature`

### `com_implementation_loss` — Implementation loss

> How far short of theory does the real modem fall, in decibels?

| | |
|---|---|
| symbol | `L_imp` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `L_imp = 1.5` |
| source | `ccsds131` |
| declared value | **1.5** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 6 - |

- **lower bound** — an implementation loss cannot be negative
- **upper bound** — above 6 dB the equipment is not flight hardware
- **read by** — `com_data_rate`, `com_link_margin`

### `com_line_loss` — Transmit line loss

> How much is lost between the transmitter and the antenna, in decibels?

| | |
|---|---|
| symbol | `L_line` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `L_line = 1.2` |
| source | `larson_wertz` |
| declared value | **1.2** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 6 - |

- **lower bound** — a loss cannot be negative in decibels here
- **upper bound** — above 6 dB most of the transmitter output is heating the harness
- **read by** — `com_eirp`, `com_system_noise_temperature`

### `com_link_margin` — Link margin

> Does the downlink close with margin to spare?

| | |
|---|---|
| symbol | `M_link` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `M = Eb/N0 - EbN0_req - L_imp` |
| source | `ecss_e_st_50` |
| valid over | -30 … 40 dB |

- **lower bound** — below -30 dB the link is not close to closing and the design is wrong somewhere upstream
- **upper bound** — above 40 dB the link is overdesigned by orders of magnitude
- **reads** — `com_ebn0`, `com_required_ebn0`, `com_implementation_loss`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_data_volume
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_link_margin_required` — Required link margin

> How much margin must the link carry above threshold, in decibels?

| | |
|---|---|
| symbol | `M_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `M_req = 3` |
| source | `ecss_e_st_50` |
| declared value | **3** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 12 - |

- **lower bound** — a required margin cannot be negative
- **upper bound** — above 12 dB the link is being designed for an availability nobody asked for
- **read by** — `com_data_rate`

### `com_pass_volume` — Data volume per pass

> How much data comes down in one pass?

| | |
|---|---|
| symbol | `V_pass` |
| type | `DataVolume` |
| unit | Gbit |
| kind | computed |
| owner | comms |
| evidence tier | B |
| relation | `V = R*t*eta_link` |
| source | `larson_wertz` |
| valid over | 0 … 1000000 Gbit |

- **lower bound** — a data volume cannot be negative
- **upper bound** — above a petabit per pass no modem in this class runs
- **reads** — `com_data_rate`, `com_contact_time`
- **read by** — `com_daily_volume`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_passes_per_day` — Usable passes per day

> How many usable ground station contacts are there each day?

| | |
|---|---|
| symbol | `N_pass` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | operations |
| evidence tier | A |
| relation | `N_pass = 9` |
| source | `orbitt_case_c1` |
| declared value | **9** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 60 - |

- **lower bound** — below half a pass a day the mission cannot be operated
- **upper bound** — above 60 passes a day exceeds the number of revolutions in a day
- **read by** — `com_daily_volume`, `mis_time_to_downlink`
- **contributes to** — kpi_latency, kpi_data_volume

### `com_path_loss` — Free space path loss

> How much of the signal is lost simply by spreading out over the range?

| | |
|---|---|
| symbol | `L_fs` |
| type | `Ratio` |
| unit | dB |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `L = (4*pi*d/lambda)^2` |
| source | `larson_wertz` |
| valid over | 100 … 250 dB |

- **lower bound** — below 100 dB the range is shorter than any orbit in this band
- **upper bound** — above 250 dB the range is not one this tool covers
- **reads** — `orbit_slant_range`, `com_frequency`
- **read by** — `com_cn0`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_required_ebn0` — Required energy per bit over noise density

> What energy per bit does the modulation and coding need, in decibels?

| | |
|---|---|
| symbol | `EbN0_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `EbN0_req = 4.5` |
| source | `ccsds131` |
| declared value | **4.5** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 20 - |

- **lower bound** — below 0 dB no practical coded modulation reaches the required bit error rate
- **upper bound** — above 20 dB the modulation and coding are not the ones assumed here
- **read by** — `com_data_rate`, `com_link_margin`

### `com_system_noise_temperature` — System noise temperature

> How much noise does the receiving system contribute?

| | |
|---|---|
| symbol | `T_s` |
| type | `Temperature` |
| unit | K |
| kind | computed |
| owner | comms |
| evidence tier | A |
| relation | `T_s = T_a/L + T0*(L-1)/L + T0*(F-1)` |
| source | `larson_wertz` |
| valid over | 10 … 3000 K |

- **lower bound** — below 10 K requires cryogenic cooling not present in this ground segment
- **upper bound** — above 3000 K the receiving system is not usable
- **reads** — `com_gs_noise_temperature`, `com_line_loss`
- **read by** — `com_g_over_t`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `com_tx_power` — Transmitter radio-frequency output power

> How much radio-frequency power does the transmitter deliver?

| | |
|---|---|
| symbol | `P_t` |
| type | `Power` |
| unit | W |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `P_t = 8` |
| source | `orbitt_case_c1` |
| declared value | **8** W |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 200 W |

- **lower bound** — below 0.1 W no link closes from this altitude
- **upper bound** — above 200 W the transmitter is the largest load on the bus and the thermal design does not close
- **read by** — `com_eirp`, `prop_throttle`, `pwr_demand`, `thm_dissipation`


## `cost` — Closure and cost — addition

### `cost_annual_operations` — Annual operations cost

> What does running the constellation cost each year?

| | |
|---|---|
| symbol | `C_ops` |
| type | `Money` |
| unit | MUSD |
| kind | declared |
| owner | programme |
| evidence tier | A |
| relation | `C_ops = 4.5` |
| source | `orbitt_case_c1` |
| declared value | **4.5** MUSD |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 200 MUSD |

- **lower bound** — below 100 thousand a year no ground segment and team exist
- **upper bound** — above 200 million a year the programme is not the one costed here
- **read by** — `cost_programme`

### `cost_bus_recurring` — Bus recurring cost, first unit

> What does the first spacecraft bus cost to build?

| | |
|---|---|
| symbol | `C_bus` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = a*m_dry^b, escalated` |
| source | `nasa_cer` |
| valid over | 0 … 10000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above 10 billion for one bus the relation is being fed a mass it was not fitted on
- **reads** — `mass_dry`, `cost_inflation`
- **read by** — `cost_production`, `cost_replacement_avoided`
- **assumes** The historical fit applies to this class of spacecraft — fails when the fit's one-sigma residual is 40%, which is larger than most of the design decisions it would be used to compare
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_inflation` — Annual inflation rate

> At what annual rate are the fitted cost relations escalated to the target year?

| | |
|---|---|
| symbol | `i_r` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | programme |
| evidence tier | A |
| relation | `i_r = 0.03` |
| source | `nasa_cer` |
| declared value | **0.03** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.15 - |

- **lower bound** — a negative rate is deflation, which the cost model has no basis for
- **upper bound** — above 15% a year the estimate is dominated by the escalation rather than the design
- **read by** — `cost_bus_recurring`, `cost_non_recurring`, `cost_payload_recurring`

### `cost_launch_per_satellite` — Launch cost per satellite

> What does it cost to put one satellite in orbit?

| | |
|---|---|
| symbol | `C_lch` |
| type | `Money` |
| unit | MUSD |
| kind | declared |
| owner | programme |
| evidence tier | A |
| relation | `C_lch = 1.8` |
| source | `orbitt_case_c1` |
| declared value | **1.8** MUSD |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 100 MUSD |

- **lower bound** — below 50 thousand dollars no launch service exists
- **upper bound** — above 100 million per satellite the constellation is not the one costed here
- **read by** — `cost_programme`, `cost_replacement_avoided`

### `cost_learning_slope` — Production learning curve slope

> By what factor does unit cost fall with each doubling of cumulative production?

| | |
|---|---|
| symbol | `b_lc` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | programme |
| evidence tier | A |
| relation | `b_lc = 0.92` |
| source | `nasa_cer` |
| declared value | **0.92** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.7 … 1 - |

- **lower bound** — below 0.70 no space production programme has learned that fast
- **upper bound** — unity is no learning at all, the pessimistic limit
- **read by** — `cost_production`

The term that decides whether forty satellites cost forty times one, and the term most concept estimates leave out.

### `cost_non_recurring` — Non-recurring engineering cost

> What does developing the design cost, once?

| | |
|---|---|
| symbol | `C_nre` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = a*m_dry^b, escalated` |
| source | `nasa_cer` |
| valid over | 0 … 100000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above 100 billion the relation is being fed a mass it was not fitted on
- **reads** — `mass_dry`, `cost_inflation`
- **read by** — `cost_programme`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_payload_recurring` — Payload recurring cost, first unit

> What does the first payload set cost to build?

| | |
|---|---|
| symbol | `C_pay` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = a*m_pay^b, escalated` |
| source | `nasa_cer` |
| valid over | 0 … 10000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above 10 billion for one payload set the relation is being fed a mass it was not fitted on
- **reads** — `mass_payload`, `cost_inflation`
- **read by** — `cost_production`, `cost_replacement_avoided`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_per_year` — Cost per operational year

> What does a year of service cost?

| | |
|---|---|
| symbol | `C_yr` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = C_total/years` |
| source | `nasa_cer` |
| valid over | 0 … 1000000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above a trillion dollars a year the programme is not the one being designed
- **reads** — `cost_programme`, `orbit_mission_duration`
- **read by** — `kpi_cost_per_year`
- **contributes to** — kpi_cost_per_year
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_production` — Production run cost

> What does building the whole constellation cost?

| | |
|---|---|
| symbol | `C_prod` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = sum over n of T1*n^log2(b)` |
| source | `nasa_cer` |
| valid over | 0 … 1000000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above a trillion dollars the programme is not the one being designed
- **reads** — `cost_bus_recurring`, `cost_payload_recurring`, `mis_satellites`, `cost_learning_slope`
- **read by** — `cost_programme`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_programme` — Total programme cost

> What does the whole programme cost over the mission?

| | |
|---|---|
| symbol | `C_tot` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = NRE + production + launch + operations*years` |
| source | `nasa_cer` |
| valid over | 0 … 1000000 MUSD |

- **lower bound** — a cost cannot be negative
- **upper bound** — above a trillion dollars the programme is not the one being designed
- **reads** — `cost_non_recurring`, `cost_production`, `cost_launch_per_satellite`, `mis_satellites`, `cost_annual_operations`, `orbit_mission_duration`
- **read by** — `cost_per_year`
- **contributes to** — kpi_cost_per_year
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `cost_replacement_avoided` — Replacement cost avoided

> What does air-breathing propulsion save by removing the need to replace satellites when propellant runs out?

| | |
|---|---|
| symbol | `C_avd` |
| type | `Money` |
| unit | MUSD |
| kind | computed |
| owner | programme |
| evidence tier | C |
| relation | `C = (C_unit + C_launch)*N_replacements` |
| source | `orbitt_case_c1` |
| valid over | 0 … 1000000 MUSD |

- **lower bound** — a saving cannot be negative
- **upper bound** — above a trillion dollars the programme is not the one being designed
- **reads** — `cost_bus_recurring`, `cost_payload_recurring`, `cost_launch_per_satellite`, `mis_satellites`, `prop_thrust_to_drag`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The commercial argument for the whole programme, as a node with a source and evidence rather than a slide.


## `env` — Environment and orbit — addition

### `env_atomic_oxygen_density` — Atomic oxygen number density

> How much atomic oxygen is there — the species that erodes materials and the one an intake actually collects?

| | |
|---|---|
| symbol | `n_O` |
| type | `NumberDensity` |
| unit | 1/m^3 |
| kind | computed |
| owner | environment |
| evidence tier | B |
| relation | `n_O(z) = n_O(z0)*(T0/T)*exp(-INT M_O*g/(R*T) dz)` |
| source | `jacchia1971` |
| valid over | 100000000 … 10000000000000000000 1/m^3 |

- **lower bound** — below 1e8 there is effectively no atomic oxygen and the accommodation model returns a specular surface
- **upper bound** — above 1e19 is denser than the total number density at the model base
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — `aero_accommodation`, `aero_ao_fluence`
- **contributes to** — kpi_service_lifetime
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_chamber_temperature` — Intake chamber gas temperature

> What temperature does the collected gas reach in the collection chamber?

| | |
|---|---|
| symbol | `T_c` |
| type | `Temperature` |
| unit | K |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `T_c = 600` |
| source | `romano2021` |
| declared value | **600** K |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 300 … 1500 K |

- **lower bound** — the chamber cannot be colder than the walls that bound it
- **upper bound** — above 1500 K the assumption that the chamber gas is thermalised and un-ionised stops holding
- **read by** — `prop_capture_efficiency`, `prop_compression_ratio`

Drives the mean thermal speed, and therefore back-flow: a hotter chamber leaks harder.

### `env_density_uncertainty` — Density model uncertainty

> How wrong might the density be, and therefore how wrong is every margin built on it?

| | |
|---|---|
| symbol | `sigma_rho` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `sigma_rho = 0.15 + 0.03*Kp` |
| source | `doornbos2011` |
| valid over | 0.05 … 1 - |

- **lower bound** — no published comparison of an empirical thermosphere model against measured drag does better than 5%
- **upper bound** — an uncertainty above 100% of the value is not an uncertainty, it is an absence of a model
- **reads** — `env_kp`
- **read by** — `gnc_along_track_error`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

A node that consumes density and does not carry this forward is a node whose margin is fictional.

### `env_exospheric_temperature` — Exospheric temperature

> How hot is the upper thermosphere today, given the Sun and the geomagnetic field?

| | |
|---|---|
| symbol | `T_inf` |
| type | `Temperature` |
| unit | K |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `T_inf = 379 + 3.24*F10.7A + 1.3*(F10.7 - F10.7A) + 28*Kp + 0.03*exp(Kp)` |
| source | `jacchia1971` |
| valid over | 400 … 2500 K |

- **lower bound** — no observed thermosphere is colder than 400 K; below that the Bates profile inverts
- **upper bound** — above 2500 K is beyond any recorded storm and beyond the fit
- **reads** — `env_f107`, `env_f107a`, `env_kp`
- **read by** — `aero_accommodation`, `env_atomic_oxygen_density`, `env_local_temperature`, `env_mass_density`, `env_mean_molar_mass`, `env_number_density`, `env_scale_height`, `orbit_lifetime_uncontrolled`
- **assumes** Night-time minimum, with no diurnal or seasonal term — fails when the diurnal bulge adds up to 30% at 14:00 local solar time; a design sized on this value alone is sized on the quiet side
- **evidence** solar minimum, quiet — expect 633.9 ± 0.002 relative, from `jacchia1971` (published-source)
- **evidence** moderate activity — expect 949.6 ± 0.002 relative, from `jacchia1971` (published-source)
- **evidence** solar maximum, storm — expect 1417.9 ± 0.002 relative, from `jacchia1971` (published-source)

### `env_f107` — Solar radio flux F10.7, daily

> How active is the Sun today, on the 10.7 cm radio flux index?

| | |
|---|---|
| symbol | `F107` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | environment |
| evidence tier | A |
| relation | `F107 = 150` |
| source | `noaa_swpc` |
| declared value | **150** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 60 … 400 - |

- **lower bound** — below 60 sfu has never been observed; the fit has no support there
- **upper bound** — above 400 sfu is beyond the largest recorded daily value, so the temperature relation is extrapolated
- **read by** — `env_exospheric_temperature`

Shipped climatology stands in when no solar-drivers bundle is synced, and the run is marked amber.

### `env_f107a` — Solar radio flux F10.7, 81-day mean

> Where has the Sun been sitting over the last three solar rotations?

| | |
|---|---|
| symbol | `F107A` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | environment |
| evidence tier | A |
| relation | `F107A = 150` |
| source | `noaa_swpc` |
| declared value | **150** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 60 … 400 - |

- **lower bound** — same support limit as the daily value
- **upper bound** — same support limit as the daily value
- **read by** — `env_exospheric_temperature`

Separate from the daily value on purpose: the mean sets where the atmosphere sits, the daily value sets how far it is from there today.

### `env_knudsen` — Knudsen number

> Is the flow free-molecular — which is what licenses every aerodynamic relation in this tree?

| | |
|---|---|
| symbol | `Kn` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `Kn = lambda/L` |
| source | `us_std_1976` |
| valid over | 10 … 1000000000000 - |

- **lower bound** — below Kn = 10 the flow is transitional and Sentman's free-molecular relations are outside their envelope. This bound is the Ariane 501 lesson written as a guard: a component used outside the envelope it was specified for, with nothing re-derived
- **upper bound** — above 1e12 the number is meaningless but harmless
- **reads** — `env_mean_free_path`, `aero_body_length`
- **read by** — `aero_drag_coefficient`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_kp` — Planetary geomagnetic index Kp

> How disturbed is the geomagnetic field?

| | |
|---|---|
| symbol | `Kp` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | environment |
| evidence tier | A |
| relation | `Kp = 3` |
| source | `noaa_swpc` |
| declared value | **3** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 9 - |

- **lower bound** — Kp is defined on 0..9; a negative index is not a quiet day, it is a unit error
- **upper bound** — Kp is defined on 0..9. This is the guard that catches an Ap value passed in by mistake, which would otherwise return an exospheric temperature of 1e18 K without complaint
- **read by** — `env_density_uncertainty`, `env_exospheric_temperature`

### `env_local_temperature` — Local kinetic temperature

> What is the gas temperature at the flight altitude?

| | |
|---|---|
| symbol | `T` |
| type | `Temperature` |
| unit | K |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `T(z) = T_inf - (T_inf - T_120)*exp(-s*(z - z_120))` |
| source | `jacchia1971` |
| valid over | 200 … 2500 K |

- **lower bound** — below the 120 km base temperature the profile is not defined
- **upper bound** — cannot exceed the exospheric temperature it approaches
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — `aero_speed_ratio`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_magnetic_field` — Geomagnetic flux density

> How strong is the magnetic field at the flight altitude?

| | |
|---|---|
| symbol | `B` |
| type | `MagneticFluxDensity` |
| unit | T |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `B = B0*(Re/r)^3*sqrt(1 + 3*sin^2(lat_m))` |
| source | `igrf2020` |
| valid over | 0.000001 … 0.0001 T |

- **lower bound** — the field never falls below a microtesla at these altitudes
- **upper bound** — the surface equatorial field is 3.1e-5 T; 1e-4 would be four times the polar surface value
- **reads** — `orbit_radius`, `gnc_magnetic_latitude`
- **read by** — `gnc_magnetic_torque`, `gnc_magnetorquer_dipole`
- **assumes** Centred dipole, no higher-order terms — fails when the South Atlantic Anomaly departs from a dipole by up to 30%; a magnetorquer sized on this alone is undersized there
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_mass_density` — Atmospheric mass density

> How much air is there at the flight altitude, in kilograms per cubic metre?

| | |
|---|---|
| symbol | `rho` |
| type | `MassDensity` |
| unit | kg/m^3 |
| kind | computed |
| owner | environment |
| evidence tier | B |
| relation | `rho = SUM_i n_i(z)*m_i,  n_i(z) = n_i(z0)*(T0/T)^(1+alpha_i)*exp(-INT M_i*g/(R*T) dz)` |
| source | `jacchia1971` |
| valid over | 0.000000000000001 … 0.000001 kg/m^3 |

- **lower bound** — below 1e-15 kg/m3 the free-molecular relations downstream stop producing a meaningful drag
- **upper bound** — above 1e-6 kg/m3 the flow is no longer free-molecular and every aerodynamic node in this tree is outside its envelope
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — `aero_dynamic_pressure`, `orbit_decay_rate`, `prop_collected_flow`, `prop_incident_flux`, `thm_aero_heating`
- **contributes to** — kpi_service_lifetime
- **assumes** Diffusive equilibrium above 120 km, five species — fails when below about 100 km turbulent mixing dominates and species do not separate; the model is not valid there
- **assumes** No diurnal, seasonal or longitudinal variation — fails when the real density at a fixed altitude varies by a factor of two around the orbit; this returns the daily mean
- **evidence** 250 km, moderate activity — expect 0.00000000006631 ± 0.005 relative, from `matlab_legacy` (independent-tool)
- **evidence** 400 km, moderate activity — expect 0.000000000002907 ± 0.005 relative, from `matlab_legacy` (independent-tool)
- **evidence** 200 km, solar minimum — expect 0.0000000001679 ± 0.005 relative, from `matlab_legacy` (independent-tool)

### `env_mean_free_path` — Mean free path

> How far does a molecule travel between collisions?

| | |
|---|---|
| symbol | `lambda` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `lambda = 1/(sqrt(2)*n*sigma)` |
| source | `us_std_1976` |
| valid over | 0.001 … 1000000000 m |

- **lower bound** — a mean free path below a millimetre would put a spacecraft-scale body in continuum flow
- **upper bound** — beyond 1e9 m the concept of a mean free path has no operational meaning here
- **reads** — `env_number_density`
- **read by** — `env_knudsen`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_mean_molar_mass` — Mean molar mass of the local gas

> What is the mean molar mass of the air the vehicle is flying through?

| | |
|---|---|
| symbol | `M` |
| type | `MolarMass` |
| unit | kg/mol |
| kind | computed |
| owner | environment |
| evidence tier | B |
| relation | `M = rho*N_A/n` |
| source | `jacchia1971` |
| valid over | 0.002 … 0.03 kg/mol |

- **lower bound** — below 2 g/mol the mixture would be pure hydrogen, which does not occur in this band
- **upper bound** — above 30 g/mol the mixture would be heavier than molecular nitrogen, which does not occur above the base
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — `aero_drag_coefficient`, `aero_speed_ratio`, `prop_capture_efficiency`, `prop_compression_ratio`, `prop_exhaust_velocity`, `prop_ionisation_power`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Falls from 26 g/mol at 120 km to near 16 — pure atomic oxygen — by 350 km. Every free-molecular relation downstream reads it.

### `env_number_density` — Total number density

> How many particles per cubic metre are there at the flight altitude?

| | |
|---|---|
| symbol | `n` |
| type | `NumberDensity` |
| unit | 1/m^3 |
| kind | computed |
| owner | environment |
| evidence tier | B |
| relation | `n = SUM_i n_i(z)` |
| source | `jacchia1971` |
| valid over | 100000000 … 100000000000000000000 1/m^3 |

- **lower bound** — below 1e8 per cubic metre there is nothing for an intake to collect
- **upper bound** — above 1e20 the flow is continuum and the intake model does not apply
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — `env_mean_free_path`, `prop_capture_efficiency`, `prop_chamber_density`, `prop_compression_ratio`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_scale_height` — Density scale height

> Over what altitude change does the density fall by a factor of e?

| | |
|---|---|
| symbol | `H` |
| type | `Length` |
| unit | km |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `H = R*T/(M*g)` |
| source | `vallado2013` |
| valid over | 5 … 200 km |

- **lower bound** — a scale height below 5 km does not occur above the model base
- **upper bound** — above 200 km the atmosphere is effectively isothermal and the concept stops being useful
- **reads** — `orbit_altitude`, `env_exospheric_temperature`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `env_wall_temperature` — Spacecraft surface temperature

> What temperature are the surfaces the atmosphere strikes?

| | |
|---|---|
| symbol | `T_w` |
| type | `Temperature` |
| unit | K |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `T_w = 300` |
| source | `sentman1961` |
| declared value | **300** K |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 150 … 500 K |

- **lower bound** — below 150 K no external surface of this design is predicted to fall, and the accommodation fit has no data there
- **upper bound** — above 500 K the surface materials are outside their qualification range, so a drag figure computed there is meaningless anyway
- **read by** — `aero_drag_coefficient`


## `envorbit` — Environment and orbit — addition

### `l3_x_envorbit_interface` — Environment and orbit — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `fsw` — Flight software

### `l3_fsw_ach_01` — Processor throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_ach_02` — Memory size

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_ach_03` — Clock distribution accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_interface` — Flight software — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_n_01` — Flight software · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_02` — Flight software · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_03` — Flight software · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_04` — Flight software · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_05` — Flight software · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_06` — Flight software · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_07` — Flight software · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_08` — Flight software · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_09` — Flight software · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_10` — Flight software · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_11` — Flight software · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_12` — Flight software · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_13` — Flight software · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_14` — Flight software · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_15` — Flight software · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_16` — Flight software · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_17` — Flight software · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_18` — Flight software · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_19` — Flight software · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_20` — Flight software · internal — to be named (20)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_21` — Flight software · internal — to be named (21)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_22` — Flight software · internal — to be named (22)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_23` — Flight software · internal — to be named (23)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_24` — Flight software · internal — to be named (24)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_25` — Flight software · internal — to be named (25)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_26` — Flight software · internal — to be named (26)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_27` — Flight software · internal — to be named (27)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_28` — Flight software · internal — to be named (28)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_29` — Flight software · internal — to be named (29)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_30` — Flight software · internal — to be named (30)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_31` — Flight software · internal — to be named (31)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_32` — Flight software · internal — to be named (32)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_33` — Flight software · internal — to be named (33)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_34` — Flight software · internal — to be named (34)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_35` — Flight software · internal — to be named (35)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_36` — Flight software · internal — to be named (36)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_37` — Flight software · internal — to be named (37)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_38` — Flight software · internal — to be named (38)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_39` — Flight software · internal — to be named (39)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_40` — Flight software · internal — to be named (40)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_41` — Flight software · internal — to be named (41)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_42` — Flight software · internal — to be named (42)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_43` — Flight software · internal — to be named (43)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_44` — Flight software · internal — to be named (44)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_n_45` — Flight software · internal — to be named (45)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_fsw_req_01` — Processor throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_req_02` — Memory size

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_fsw_req_03` — Clock distribution accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | avionics |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `gnc` — Attitude control sizing

### `gnc_alignment_error` — Payload to sensor alignment error

> How well is the payload aligned to the attitude sensor after launch and thermal cycling?

| | |
|---|---|
| symbol | `sig_ali` |
| type | `Angle` |
| unit | arcsec |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `sig_ali = 30` |
| source | `larson_wertz` |
| declared value | **30** arcsec |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 3600 arcsec |

- **lower bound** — below 1 arcsec no mechanical alignment survives launch and thermal cycling
- **upper bound** — above one degree the alignment does not support any pointing requirement in this design
- **read by** — `gnc_pointing_error`

### `gnc_along_track_error` — Along-track prediction error after one day

> How far out is a one-day orbit prediction, given how wrong the density model is?

| | |
|---|---|
| symbol | `e_at` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | gnc |
| evidence tier | B |
| relation | `e = 1.5*da*t^2` |
| source | `doornbos2011` |
| valid over | 0 … 10000000 m |

- **lower bound** — an error magnitude cannot be negative
- **upper bound** — above 10000 km the prediction is meaningless and the answer is that the model is unusable there
- **reads** — `aero_drag_acceleration`, `env_density_uncertainty`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The term that dominates orbit prediction in this regime, and the reason the density uncertainty is a published output rather than a footnote.

### `gnc_control_error` — Attitude control error

> How well does the controller hold the commanded attitude?

| | |
|---|---|
| symbol | `sig_ctl` |
| type | `Angle` |
| unit | arcsec |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `sig_ctl = 40` |
| source | `larson_wertz` |
| declared value | **40** arcsec |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 7200 arcsec |

- **lower bound** — below 1 arcsec no wheel-based controller holds that in the presence of VLEO aerodynamic torque
- **upper bound** — above two degrees the controller does not support any pointing requirement in this design
- **read by** — `gnc_pointing_error`

### `gnc_dump_time` — Momentum dump interval

> How long may the wheels accumulate momentum before it is dumped?

| | |
|---|---|
| symbol | `t_dump` |
| type | `Time` |
| unit | min |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `t_dump = 45` |
| source | `larson_wertz` |
| declared value | **45** min |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 1440 min |

- **lower bound** — below a minute the magnetorquers are being commanded faster than the field geometry changes
- **upper bound** — above a day the wheels saturate long before the dump
- **read by** — `gnc_magnetorquer_dipole`

### `gnc_gdop` — Geometric dilution of precision

> How much does the satellite geometry amplify ranging error into position error?

| | |
|---|---|
| symbol | `GDOP` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `GDOP = 2.5` |
| source | `larson_wertz` |
| declared value | **2.5** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 20 - |

- **lower bound** — a dilution below one is not geometrically possible
- **upper bound** — above 20 the geometry is degenerate and the solution is not usable
- **read by** — `gnc_nav_position_error`

### `gnc_gravity_gradient_torque` — Gravity gradient torque

> How much torque does the gravity gradient apply?

| | |
|---|---|
| symbol | `T_gg` |
| type | `Torque` |
| unit | N.m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `T = (3*mu/(2*r^3))*|Iz - Iy|*sin(2*theta)` |
| source | `larson_wertz` |
| valid over | 0 … 1 N.m |

- **lower bound** — a torque magnitude cannot be negative
- **upper bound** — above 1 N.m no wheel that fits this vehicle can hold attitude
- **reads** — `orbit_radius`, `gnc_inertia_max`, `gnc_inertia_min`
- **read by** — `gnc_total_disturbance`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_ground_pointing_error` — Ground pointing error

> How far off the ground does the pointing error put the aim point?

| | |
|---|---|
| symbol | `e_gnd` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `e = sigma*d` |
| source | `larson_wertz` |
| valid over | 0 … 100000 m |

- **lower bound** — a ground error cannot be negative
- **upper bound** — above 100 km the pointing is not usable for any payload here
- **reads** — `gnc_pointing_error`, `orbit_slant_range`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_geolocation
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_inertia_max` — Largest principal moment of inertia

> What is the largest principal moment of inertia?

| | |
|---|---|
| symbol | `I_max` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `I_max = 8` |
| source | `orbitt_case_c1` |
| declared value | **8** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.01 … 1000 - |

- **lower bound** — below 0.01 kg.m2 there is no vehicle
- **upper bound** — above 1000 kg.m2 the vehicle is not in this mass class
- **read by** — `gnc_gravity_gradient_torque`

### `gnc_inertia_min` — Smallest principal moment of inertia

> What is the smallest principal moment of inertia?

| | |
|---|---|
| symbol | `I_min` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `I_min = 1.2` |
| source | `orbitt_case_c1` |
| declared value | **1.2** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.01 … 1000 - |

- **lower bound** — below 0.01 kg.m2 there is no vehicle
- **upper bound** — cannot exceed the largest principal moment
- **read by** — `gnc_gravity_gradient_torque`

### `gnc_magnetic_latitude` — Magnetic latitude

> At what magnetic latitude is the field being evaluated?

| | |
|---|---|
| symbol | `lat_m` |
| type | `Angle` |
| unit | deg |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `lat_m = 45` |
| source | `igrf2020` |
| declared value | **45** deg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | -90 … 90 deg |

- **lower bound** — magnetic latitude is defined on -90..90
- **upper bound** — magnetic latitude is defined on -90..90
- **read by** — `env_magnetic_field`

### `gnc_magnetic_torque` — Residual dipole torque

> How much torque does the residual magnetic dipole produce in the geomagnetic field?

| | |
|---|---|
| symbol | `T_mag` |
| type | `Torque` |
| unit | N.m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `T = m_res x B` |
| source | `larson_wertz` |
| valid over | 0 … 1 N.m |

- **lower bound** — a torque magnitude cannot be negative
- **upper bound** — above 1 N.m no wheel that fits this vehicle can hold attitude
- **reads** — `gnc_residual_dipole`, `env_magnetic_field`
- **read by** — `gnc_total_disturbance`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_magnetorquer_dipole` — Magnetorquer dipole required

> How strong must the torque rods be to dump that momentum?

| | |
|---|---|
| symbol | `m_req` |
| type | `DipoleMoment` |
| unit | A.m^2 |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `m = h/(B*t_dump)` |
| source | `larson_wertz` |
| valid over | 0 … 1000 A.m^2 |

- **lower bound** — a dipole magnitude cannot be negative
- **upper bound** — above 1000 A.m2 no torque rod that fits this vehicle produces it
- **reads** — `gnc_momentum_storage`, `env_magnetic_field`, `gnc_dump_time`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_momentum_storage` — Momentum storage required

> How much angular momentum must the wheels be able to store?

| | |
|---|---|
| symbol | `h_req` |
| type | `AngularMomentum` |
| unit | N.m.s |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `h = T*T_orb/2*(1 + margin)` |
| source | `larson_wertz` |
| valid over | 0 … 100 N.m.s |

- **lower bound** — a momentum magnitude cannot be negative
- **upper bound** — above 100 N.m.s no wheel assembly that fits this vehicle stores it
- **reads** — `gnc_total_disturbance`, `orbit_period`
- **read by** — `gnc_magnetorquer_dipole`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_nav_position_error` — Navigation position error

> How well does the spacecraft know where it is?

| | |
|---|---|
| symbol | `e_nav` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `e = URE*GDOP` |
| source | `larson_wertz` |
| valid over | 0 … 10000 m |

- **lower bound** — a position error cannot be negative
- **upper bound** — above 10 km the navigation solution is not usable
- **reads** — `gnc_user_range_error`, `gnc_gdop`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_geolocation
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_pointing_error` — Pointing error, three sigma

> How accurately does the payload point?

| | |
|---|---|
| symbol | `sig_pt` |
| type | `Angle` |
| unit | arcsec |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `sigma = sqrt(sum of squares of the independent contributors)` |
| source | `larson_wertz` |
| valid over | 0 … 10800 arcsec |

- **lower bound** — a pointing error cannot be negative
- **upper bound** — above three degrees no payload in this design produces a usable product
- **reads** — `gnc_sensor_noise`, `gnc_alignment_error`, `gnc_control_error`, `gnc_thermal_distortion`
- **read by** — `gnc_ground_pointing_error`
- **contributes to** — kpi_geolocation
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Root-sum-squared, unlike the disturbance torques above. These terms are genuinely independent, and summing them linearly would give a budget nothing could ever meet.

### `gnc_residual_dipole` — Residual magnetic dipole

> How much uncompensated magnetic moment does the spacecraft carry?

| | |
|---|---|
| symbol | `m_res` |
| type | `DipoleMoment` |
| unit | A.m^2 |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `m_res = 0.05` |
| source | `larson_wertz` |
| declared value | **0.05** A.m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 5 A.m^2 |

- **lower bound** — a residual dipole magnitude cannot be negative
- **upper bound** — above 5 A.m2 the magnetic disturbance dominates every other torque and the design is uncontrollable
- **read by** — `gnc_magnetic_torque`

### `gnc_sensor_noise` — Attitude sensor noise

> How noisy is the attitude measurement?

| | |
|---|---|
| symbol | `sig_sen` |
| type | `Angle` |
| unit | arcsec |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `sig_sen = 12` |
| source | `larson_wertz` |
| declared value | **12** arcsec |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 3600 arcsec |

- **lower bound** — below 0.5 arcsec no star tracker in this class performs that well
- **upper bound** — above one degree the sensor does not support any pointing requirement in this design
- **read by** — `gnc_pointing_error`

### `gnc_solar_torque` — Solar radiation pressure torque

> How much torque does solar radiation pressure apply?

| | |
|---|---|
| symbol | `T_srp` |
| type | `Torque` |
| unit | N.m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `T = (S/c)*A*(1+q)*cos(theta)*x_cp` |
| source | `larson_wertz` |
| valid over | 0 … 1 N.m |

- **lower bound** — a torque magnitude cannot be negative
- **upper bound** — above 1 N.m no wheel that fits this vehicle can hold attitude
- **reads** — `aero_frontal_area`, `aero_cp_cm_offset`, `pwr_array_incidence`
- **read by** — `gnc_total_disturbance`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `gnc_thermal_distortion` — Thermal distortion error

> How much does the structure move between the sensor and the payload over an orbit?

| | |
|---|---|
| symbol | `sig_thm` |
| type | `Angle` |
| unit | arcsec |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `sig_thm = 20` |
| source | `larson_wertz` |
| declared value | **20** arcsec |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 3600 arcsec |

- **lower bound** — a distortion cannot be negative
- **upper bound** — above one degree the structure is not stable enough for any payload in this design
- **read by** — `gnc_pointing_error`

### `gnc_total_disturbance` — Total disturbance torque

> What is the worst-case disturbance torque the control system has to answer?

| | |
|---|---|
| symbol | `T_dis` |
| type | `Torque` |
| unit | N.m |
| kind | computed |
| owner | gnc |
| evidence tier | A |
| relation | `T = |T_aero| + |T_gg| + |T_srp| + |T_mag|` |
| source | `larson_wertz` |
| valid over | 0 … 5 N.m |

- **lower bound** — a torque magnitude cannot be negative
- **upper bound** — above 5 N.m the vehicle is uncontrollable by any actuator that fits it
- **reads** — `aero_torque`, `gnc_gravity_gradient_torque`, `gnc_solar_torque`, `gnc_magnetic_torque`
- **read by** — `gnc_momentum_storage`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Summed, not root-sum-squared. These are biases that can and do align, and a wheel sized on their RSS saturates the first time they do.

### `gnc_user_range_error` — GNSS user range error

> How accurate is one pseudorange measurement?

| | |
|---|---|
| symbol | `URE` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `URE = 1.5` |
| source | `larson_wertz` |
| declared value | **1.5** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 50 m |

- **lower bound** — below 5 cm requires carrier-phase processing that this receiver does not perform on board
- **upper bound** — above 50 m the receiver is not usable for orbit determination
- **read by** — `gnc_nav_position_error`


## `kpi` — Closure and cost — addition

### `kpi_availability` — Service availability — closure

> Is the service available often enough?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_availability_required`, `mis_availability`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_availability_required` — Service availability — required

> What does the customer require for service availability?

| | |
|---|---|
| symbol | `R_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 0.99` |
| source | `orbitt_case_c1` |
| declared value | **0.99** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 0.99999 - |

- **lower bound** — below 50% the service is unavailable more often than not
- **upper bound** — above five nines no constellation in this class has demonstrated it
- **read by** — `kpi_availability`

### `kpi_cost_per_year` — Cost per operational year — closure

> Does a year of service cost what the business case allows?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_cost_per_year_required`, `cost_per_year`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_cost_per_year_required` — Cost per operational year — required

> What does the customer require for cost per operational year?

| | |
|---|---|
| symbol | `R_req` |
| type | `Money` |
| unit | MUSD |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 45` |
| source | `orbitt_case_c1` |
| declared value | **45** MUSD |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 10000 MUSD |

- **lower bound** — below 100 thousand a year no programme of this size runs
- **upper bound** — above 10 billion a year the programme is not the one being designed
- **read by** — `kpi_cost_per_year`

### `kpi_data_volume` — Daily downlink volume — closure

> Does enough data reach the ground each day?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_data_volume_required`, `com_daily_volume`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_data_volume_required` — Daily downlink volume — required

> What does the customer require for daily downlink volume?

| | |
|---|---|
| symbol | `R_req` |
| type | `DataVolume` |
| unit | Gbit |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 500` |
| source | `orbitt_case_c1` |
| declared value | **500** Gbit |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 10000000 Gbit |

- **lower bound** — below one gigabit a day the mission returns nothing useful
- **upper bound** — above 10 petabits a day the ground segment is not the one costed
- **read by** — `kpi_data_volume`

### `kpi_geolocation` — Emitter geolocation accuracy — closure

> Are emitters located accurately enough?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c2` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_geolocation_required`, `pay_geolocation_error`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_geolocation_required` — Emitter geolocation accuracy — required

> What does the customer require for emitter geolocation accuracy?

| | |
|---|---|
| symbol | `R_req` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 500` |
| source | `orbitt_case_c2` |
| declared value | **500** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 100000 m |

- **lower bound** — below one metre no time-difference system in this class achieves it
- **upper bound** — above 100 km the product carries no information
- **read by** — `kpi_geolocation`

### `kpi_gsd` — Ground sample distance — closure

> Is the imagery sharp enough?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_gsd_required`, `pay_gsd`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_gsd_required` — Ground sample distance — required

> What does the customer require for ground sample distance?

| | |
|---|---|
| symbol | `R_req` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 1` |
| source | `orbitt_case_c1` |
| declared value | **1** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 100 m |

- **lower bound** — below 5 cm no optical payload in this mass class achieves it
- **upper bound** — above 100 m the product is not the one being sold
- **read by** — `kpi_gsd`

### `kpi_latency` — End to end latency — closure

> Does the product reach the customer fast enough?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_latency_required`, `mis_latency`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_latency_required` — End to end latency — required

> What does the customer require for end to end latency?

| | |
|---|---|
| symbol | `R_req` |
| type | `Time` |
| unit | min |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 30` |
| source | `orbitt_case_c1` |
| declared value | **30** min |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 2880 min |

- **lower bound** — below a minute no ground segment in this design delivers
- **upper bound** — above two days the product is not the near-real-time one being sold
- **read by** — `kpi_latency`

### `kpi_mass_margin` — Mass margin — closure

> Does the spacecraft fit its mass allocation with margin?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `ecss_e_st_10_02` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_mass_margin_required`, `mass_margin`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_mass_margin_required` — Mass margin — required

> What does the customer require for mass margin?

| | |
|---|---|
| symbol | `R_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 0.1` |
| source | `ecss_e_st_10_02` |
| declared value | **0.1** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.9 - |

- **lower bound** — a negative required margin is not a requirement
- **upper bound** — above 90% required margin the allocation is nine times the design
- **read by** — `kpi_mass_margin`

### `kpi_power_margin` — Power margin — closure

> Is there enough power, with the margin the standard requires?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `ecss_e_st_10_02` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_power_margin_required`, `pwr_margin`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_power_margin_required` — Power margin — required

> What does the customer require for power margin?

| | |
|---|---|
| symbol | `R_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 0.2` |
| source | `ecss_e_st_10_02` |
| declared value | **0.2** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 1 - |

- **lower bound** — a negative required margin is not a requirement
- **upper bound** — above 100% required margin the design is being asked to double its power for no stated reason
- **read by** — `kpi_power_margin`

### `kpi_revisit` — Mean revisit time — closure

> Does the constellation revisit often enough?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_revisit_required`, `mis_revisit`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_revisit_required` — Mean revisit time — required

> What does the customer require for mean revisit time?

| | |
|---|---|
| symbol | `R_req` |
| type | `Time` |
| unit | h |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 6` |
| source | `orbitt_case_c1` |
| declared value | **6** h |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 720 h |

- **lower bound** — below six minutes no constellation in this class achieves it
- **upper bound** — above 30 days the service is not the one being sold
- **read by** — `kpi_revisit`, `mis_satellites_required`

### `kpi_service_lifetime` — Service lifetime — closure

> Does the satellite stay up long enough with no propulsion at all?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `orbitt_case_c1` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_service_lifetime_required`, `orbit_lifetime_uncontrolled`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Deliberately measured against the uncontrolled lifetime rather than the propelled one, so the answer does not depend on the propulsion system working.

### `kpi_service_lifetime_required` — Service lifetime — required

> What does the customer require for service lifetime?

| | |
|---|---|
| symbol | `R_req` |
| type | `Time` |
| unit | yr |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 5` |
| source | `orbitt_case_c1` |
| declared value | **5** yr |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 25 yr |

- **lower bound** — below five weeks the satellite is not a mission
- **upper bound** — above 25 years the debris mitigation standard is violated
- **read by** — `kpi_service_lifetime`

### `kpi_thermal_margin` — Thermal margin — closure

> Is the spacecraft comfortably inside its temperature limit?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `ecss_e_st_10_02` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_thermal_margin_required`, `thm_margin`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `kpi_thermal_margin_required` — Thermal margin — required

> What does the customer require for thermal margin?

| | |
|---|---|
| symbol | `R_req` |
| type | `Temperature` |
| unit | K |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 10` |
| source | `ecss_e_st_10_02` |
| declared value | **10** K |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 100 K |

- **lower bound** — a negative required margin is not a requirement
- **upper bound** — above 100 K the requirement is not one this design is written against
- **read by** — `kpi_thermal_margin`

### `kpi_thrust_margin` — Thrust to drag closure — closure

> Does thrust exceed drag — does the design close at all?

| | |
|---|---|
| symbol | `M` |
| type | `Ratio` |
| unit | - |
| kind | kpi |
| owner | systems |
| evidence tier | A |
| relation | `M = (achieved - required)/required, in the sense the requirement is stated` |
| source | `romano2021` |
| valid over | -100 … 1000 - |

- **lower bound** — a margin below -10000% means the inputs are not the ones the requirement was written against
- **upper bound** — a margin above 100000% means the requirement is not the one that binds
- **reads** — `kpi_thrust_margin_required`, `prop_thrust_to_drag`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The one closure the whole programme exists to reach. Everything else is a detail until this is at or above zero margin.

### `kpi_thrust_margin_required` — Thrust to drag closure — required

> What does the customer require for thrust to drag closure?

| | |
|---|---|
| symbol | `R_req` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `R_req = 1` |
| source | `romano2021` |
| declared value | **1** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 5 - |

- **lower bound** — a required ratio below 0.1 is not a closure requirement
- **upper bound** — above 5 the requirement is not one an air-breathing design is written against
- **read by** — `kpi_thrust_margin`


## `mass` — Mass and aero

### `mass_avionics` — Avionics mass

> What do the on-board computer, harness electronics and data handling weigh?

| | |
|---|---|
| symbol | `m_av` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | avionics |
| evidence tier | A |
| relation | `m_av = 9` |
| source | `orbitt_case_c1` |
| declared value | **9** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 200 kg |

- **lower bound** — below half a kilogram no avionics set exists
- **upper bound** — above 200 kg the avionics are not in this class
- **read by** — `mass_dry`

### `mass_comms_hw` — Communications hardware mass

> What do the transceivers and antennas weigh?

| | |
|---|---|
| symbol | `m_com` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | comms |
| evidence tier | A |
| relation | `m_com = 7.5` |
| source | `orbitt_case_c1` |
| declared value | **7.5** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.2 … 200 kg |

- **lower bound** — below 200 grams no transceiver and antenna exist
- **upper bound** — above 200 kg the communications payload is not in this class
- **read by** — `mass_dry`

### `mass_disposal_propellant` — Disposal propellant mass

> How much stored propellant must be carried for the disposal manoeuvre?

| | |
|---|---|
| symbol | `m_dis` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `m = m_dry*(exp(dv/v_e) - 1)` |
| source | `iso24113` |
| valid over | 0 … 1000 kg |

- **lower bound** — a propellant mass cannot be negative
- **upper bound** — above a tonne the disposal budget is larger than the spacecraft
- **reads** — `mass_dry`, `orbit_deorbit_delta_v`
- **read by** — `mass_wet`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

An air-breathing design does not reach zero here. Debris mitigation is a requirement, not a courtesy.

### `mass_dry` — Dry mass

> What does the spacecraft weigh with no propellant on board?

| | |
|---|---|
| symbol | `m_dry` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | mass |
| evidence tier | A |
| relation | `m_dry = (sum of subsystem masses)*(1 + M_sys)` |
| source | `ecss_e_st_10_02` |
| valid over | 1 … 5000 kg |

- **lower bound** — below a kilogram there is no spacecraft
- **upper bound** — above 5 tonnes the vehicle is not in this class
- **reads** — `mass_structure`, `mass_propulsion_hw`, `pwr_subsystem_mass`, `mass_harness`, `mass_avionics`, `mass_comms_hw`, `mass_gnc_hw`, `mass_payload`, `mass_system_margin`
- **read by** — `cost_bus_recurring`, `cost_non_recurring`, `mass_disposal_propellant`, `mass_wet`
- **contributes to** — kpi_mass_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mass_gnc_hw` — GNC hardware mass

> What do the sensors, wheels and torque rods weigh?

| | |
|---|---|
| symbol | `m_gnc` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | gnc |
| evidence tier | A |
| relation | `m_gnc = 11` |
| source | `orbitt_case_c1` |
| declared value | **11** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 200 kg |

- **lower bound** — below half a kilogram no sensor and actuator set exists
- **upper bound** — above 200 kg the control hardware is not in this class
- **read by** — `mass_dry`

### `mass_harness` — Harness and thermal hardware mass

> What do the harness, multi-layer insulation and radiators weigh?

| | |
|---|---|
| symbol | `m_har` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `m_har = 12` |
| source | `orbitt_case_c1` |
| declared value | **12** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.2 … 300 kg |

- **lower bound** — below 200 grams no harness exists
- **upper bound** — above 300 kg the harness is not in this class
- **read by** — `mass_dry`

### `mass_limit` — Mass limit

> What is the mass the launch slot or the bus allows?

| | |
|---|---|
| symbol | `m_lim` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `m_lim = 250` |
| source | `orbitt_case_c1` |
| declared value | **250** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 5 … 5000 kg |

- **lower bound** — below 5 kg no multipayload mission fits
- **upper bound** — above 5 tonnes the launch and the class are not the ones costed here
- **read by** — `mass_margin`

### `mass_margin` — Mass margin

> Does the spacecraft fit the mass it is allowed?

| | |
|---|---|
| symbol | `M_mass` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | mass |
| evidence tier | A |
| relation | `M = (m_lim - m_wet)/m_lim` |
| source | `ecss_e_st_10_02` |
| valid over | -5 … 1 - |

- **lower bound** — a margin below -500% means the design is five times over and the inputs are wrong
- **upper bound** — a margin of one means the spacecraft weighs nothing
- **reads** — `mass_limit`, `mass_wet`
- **read by** — `kpi_mass_margin`
- **contributes to** — kpi_mass_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mass_payload` — Payload mass

> What does the whole service payload set weigh?

| | |
|---|---|
| symbol | `m_pay` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `m_pay = 55` |
| source | `orbitt_case_c1` |
| declared value | **55** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 1000 kg |

- **lower bound** — below half a kilogram no service payload exists
- **upper bound** — above a tonne the payload set is not in this class
- **read by** — `cost_payload_recurring`, `mass_dry`

### `mass_propulsion_hw` — Propulsion hardware mass

> What do the intake, thruster and power processing unit weigh?

| | |
|---|---|
| symbol | `m_prop` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `m_prop = 26` |
| source | `orbitt_case_c1` |
| declared value | **26** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 500 kg |

- **lower bound** — below half a kilogram no intake, thruster and power processing unit exist
- **upper bound** — above 500 kg the propulsion system is the spacecraft
- **read by** — `mass_dry`

### `mass_structure` — Structure and mechanisms mass

> What do the primary structure, panels and mechanisms weigh?

| | |
|---|---|
| symbol | `m_str` |
| type | `Mass` |
| unit | kg |
| kind | declared |
| owner | mass |
| evidence tier | A |
| relation | `m_str = 38` |
| source | `orbitt_case_c1` |
| declared value | **38** kg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 2000 kg |

- **lower bound** — below a kilogram there is no structure
- **upper bound** — above two tonnes the vehicle is not in this class
- **read by** — `mass_dry`

### `mass_system_margin` — System-level mass margin

> What system-level margin is held above the sum of the subsystem masses?

| | |
|---|---|
| symbol | `M_sys` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | mass |
| evidence tier | A |
| relation | `M_sys = 0.2` |
| source | `ecss_e_st_10_02` |
| declared value | **0.2** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.5 - |

- **lower bound** — a margin cannot be negative
- **upper bound** — above 50% the design is a sketch and the number is not a budget
- **read by** — `mass_dry`

Separate from and additional to the per-item maturity allowances. It covers what is not on the list yet, which on any real programme is the term that bites.

### `mass_wet` — Wet mass

> What does the spacecraft weigh at separation?

| | |
|---|---|
| symbol | `m_wet` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | mass |
| evidence tier | A |
| relation | `m_wet = m_dry + m_prop` |
| source | `ecss_e_st_10_02` |
| valid over | 1 … 6000 kg |

- **lower bound** — below a kilogram there is no spacecraft
- **upper bound** — above 6 tonnes the vehicle is not in this class
- **reads** — `mass_dry`, `mass_disposal_propellant`
- **read by** — `aero_ballistic_coefficient`, `aero_drag_acceleration`, `mass_margin`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `massaero` — Mass and aero

### `l3_massaero_ach_01` — Spacecraft dry mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_02` — Body shape

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_03` — Panel layout

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_04` — Panel stiffness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_05` — Accommodation coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_06` — Erosion yield

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_07` — Launch load factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_08` — Mass margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_09` — Frontal area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_10` — Drag coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_11` — Ballistic coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_12` — Relative velocity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_13` — Drag acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_14` — Centre of pressure offset

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_15` — Launch mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_16` — Centre of mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_17` — Moments of inertia

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_18` — Structural mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_ach_19` — Erosion depth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_interface` — Mass and aero — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_01` — Spacecraft dry mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_02` — Body shape

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_03` — Panel layout

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_04` — Panel stiffness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_05` — Accommodation coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_06` — Erosion yield

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_07` — Launch load factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_08` — Mass margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_09` — Frontal area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_10` — Drag coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_11` — Ballistic coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_12` — Relative velocity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_13` — Drag acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_14` — Centre of pressure offset

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_15` — Launch mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_16` — Centre of mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_17` — Moments of inertia

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_18` — Structural mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_massaero_req_19` — Erosion depth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `mgt` — Component level

### `mgt_component_level_eee_part_screening` — EEE part screening

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_component_level_materials_and_processes` — Materials and processes

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_component_level_outgassing_and_contamination` — Outgassing and contamination

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_component_level_part_qualification_and_lot_acceptance` — Part qualification and lot acceptance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_component_level_radiation_tid_and_see` — Radiation · TID and SEE

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_concept_decision_constellation` — Constellation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_concept_decision_hosted_payload` — Hosted payload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_concept_decision_single_satellite` — Single satellite

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_configuration_and_data_baselines` — Baselines

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_configuration_and_data_change_control` — Change control

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_configuration_and_data_data_rights` — Data rights

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_configuration_and_data_document_tree` — Document tree

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_cost_and_price_cost_model_over_the_design` — Cost model over the design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_cost_and_price_payment_milestones` — Payment milestones

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_cost_and_price_price_to_the_customer` — Price to the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_1_vleo_multipayload` — VLEO Multipayload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_concept_decision_constellation` — Constellation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_concept_decision_hosted_payload` — Hosted payload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_concept_decision_single_satellite` — Single satellite

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_cost_and_price_cost_model_over_the_design` — Cost model over the design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_cost_and_price_payment_milestones` — Payment milestones

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_cost_and_price_price_to_the_customer` — Price to the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_kpi_definition_measurement_method` — Measurement method

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_kpi_definition_segment_kpis` — Segment KPIs

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_kpi_definition_target_values` — Target values

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_requirement_acceptance_criteria` — Acceptance criteria

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_requirement_requirement_discussion` — Requirement discussion

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_requirement_requirement_document` — Requirement document

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_2_value_proposition_held_internally` — Held internally

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_value_proposition_shared_with_the_customer` — Shared with the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_value_proposition_the_case_to_proceed` — The case to proceed

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_2_vleo_multipayload` — VLEO Multipayload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_concept_decision_constellation` — Constellation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_concept_decision_hosted_payload` — Hosted payload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_concept_decision_single_satellite` — Single satellite

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_cost_and_price_cost_model_over_the_design` — Cost model over the design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_cost_and_price_payment_milestones` — Payment milestones

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_cost_and_price_price_to_the_customer` — Price to the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_kpi_definition_measurement_method` — Measurement method

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_kpi_definition_segment_kpis` — Segment KPIs

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_kpi_definition_target_values` — Target values

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_requirement_acceptance_criteria` — Acceptance criteria

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_requirement_requirement_discussion` — Requirement discussion

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_requirement_requirement_document` — Requirement document

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_customer_3_value_proposition_held_internally` — Held internally

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_value_proposition_shared_with_the_customer` — Shared with the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_value_proposition_the_case_to_proceed` — The case to proceed

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_customer_3_vleo_multipayload` — VLEO Multipayload

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_debris_and_disposal_25_year_rule_and_successor` — 25-year rule and successor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_debris_and_disposal_collision_avoidance_duty` — Collision avoidance duty

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_debris_and_disposal_end_of_life_disposal_plan` — End-of-life disposal plan

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_debris_and_disposal_iso_24113_debris_mitigation` — ISO 24113 · debris mitigation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_engineering_capacity_concurrent_projects` — Concurrent projects

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_engineering_capacity_team_and_skills` — Team and skills

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_engineering_capacity_tooling_and_the_design_tool` — Tooling and the design tool

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_export_control_end_user_certification` — End-user certification

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_export_control_export_classification` — Export classification

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_export_control_supplier_flow_down` — Supplier flow-down

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_export_control_technology_transfer_control` — Technology transfer control

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_emc_test_house` — EMC test house

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_independent_verification_authority` — Independent verification authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_launch_provider_fit_check` — Launch provider fit-check

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_rf_anechoic_chamber` — RF anechoic chamber

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_thermal_vacuum_chamber` — Thermal vacuum chamber

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_and_bodies_vibration_and_shock_facility` — Vibration and shock facility

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_cleanroom` — Cleanroom

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_integration_bay` — Integration bay

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_facilities_test_access` — Test access

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_ground_segment_archive_and_delivery` — Archive and delivery

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_ground_segment_data_processing` — Data processing

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_ground_segment_ground_stations` — Ground stations

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_ground_segment_mission_control` — Mission control

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_heritage_flown_hardware` — Flown hardware

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_heritage_lessons_carried_forward` — Lessons carried forward

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_heritage_qualified_designs` — Qualified designs

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_insurance_and_liability_in_orbit_insurance` — In-orbit insurance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_insurance_and_liability_indemnity_to_the_state` — Indemnity to the state

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_insurance_and_liability_launch_insurance` — Launch insurance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_insurance_and_liability_third_party_liability` — Third-party liability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_data_protocol_and_command_dictionary` — Data protocol and command dictionary

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_electrical_harness_continuity` — Electrical harness continuity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_launch_vehicle_interface_verification` — Launch vehicle interface verification

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_mechanical_fit_check_to_the_dispenser` — Mechanical fit check to the dispenser

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_payload_to_bus_interface_test` — Payload-to-bus interface test

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_interface_level_rf_compatibility_with_the_ground_segment` — RF compatibility with the ground segment

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_kpi_definition_measurement_method` — Measurement method

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_kpi_definition_segment_kpis` — Segment KPIs

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_kpi_definition_target_values` — Target values

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_and_range_safety_hazardous_material_declaration` — Hazardous material declaration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_and_range_safety_launch_provider_icd` — Launch provider ICD

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_and_range_safety_payload_safety_review` — Payload safety review

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_and_range_safety_range_safety_approval` — Range safety approval

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_segment_dispenser_and_separation` — Dispenser and separation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_segment_launch_campaign` — Launch campaign

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_segment_launch_provider` — Launch provider

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_launch_segment_launch_vehicle_interface` — Launch vehicle interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_a_feasibility` — Phase A · feasibility

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_b_preliminary_design` — Phase B · preliminary design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_c_detailed_design` — Phase C · detailed design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_d_build_test_launch` — Phase D · build, test, launch

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_e_operations` — Phase E · operations

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_phase_f_disposal` — Phase F · disposal

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_life_cycle_and_gates_pre_phase_a_concept` — Pre-Phase A · concept

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_long_lead_items_optics_and_detectors` — Optics and detectors

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_long_lead_items_propulsion_hardware` — Propulsion hardware

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_long_lead_items_radiation_tolerant_parts` — Radiation-tolerant parts

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_make_or_buy_bought_in_scope` — Bought-in scope

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_make_or_buy_in_house_scope` — In-house scope

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_make_or_buy_rationale` — Rationale

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_qualification_approach_acceptance_levels_and_margins` — Acceptance levels and margins

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_qualification_approach_protoflight_approach` — Protoflight approach

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_qualification_approach_qualification_model` — Qualification model

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_qualification_approach_test_matrix_and_coverage` — Test matrix and coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_qualification_approach_verification_by_analysis_review_or_similarity` — Verification by analysis, review or similarity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_quality_and_product_assurance_as9100_quality_system` — AS9100 · quality system

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_quality_and_product_assurance_ecss_q_product_assurance` — ECSS-Q · product assurance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_quality_and_product_assurance_non_conformance_handling` — Non-conformance handling

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_quality_and_product_assurance_parts_materials_and_processes` — Parts, materials and processes

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_regulatory_authorisation_liability_convention` — Liability convention

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_regulatory_authorisation_national_space_authorisation` — National space authorisation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_regulatory_authorisation_operator_licence` — Operator licence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_regulatory_authorisation_registration_of_the_object` — Registration of the object

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_requirement_acceptance_criteria` — Acceptance criteria

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_requirement_requirement_discussion` — Requirement discussion

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_requirement_requirement_document` — Requirement document

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_cdr_critical_design` — CDR · critical design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_dr_decommissioning` — DR · decommissioning

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_frr_flight_readiness` — FRR · flight readiness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_mcr_mission_concept` — MCR · mission concept

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_mdr_mission_definition` — MDR · mission definition

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_orr_operational_readiness` — ORR · operational readiness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_pdr_preliminary_design` — PDR · preliminary design

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_plar_post_launch` — PLAR · post-launch

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_sir_system_integration` — SIR · system integration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_srr_system_requirements` — SRR · system requirements

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_review_gates_trr_test_readiness` — TRR · test readiness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_risk_register_regulatory_risk` — Regulatory risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_risk_register_schedule_risk` — Schedule risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_risk_register_supply_risk` — Supply risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_risk_register_technical_risk` — Technical risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_schedule_and_critical_path_critical_path` — Critical path

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_schedule_and_critical_path_float_and_slip` — Float and slip

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_schedule_and_critical_path_master_schedule` — Master schedule

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_schedule_and_critical_path_recovery_plan` — Recovery plan

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_space_segment_in_orbit_spares` — In-orbit spares

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_space_segment_the_constellation` — The constellation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_space_segment_the_spacecraft` — The spacecraft

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_spectrum_and_frequency_coordination_with_incumbents` — Coordination with incumbents

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_spectrum_and_frequency_interference_analysis` — Interference analysis

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_spectrum_and_frequency_itu_filing` — ITU filing

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_spectrum_and_frequency_national_spectrum_licence` — National spectrum licence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_subsystem_level_emc_and_emi` — EMC and EMI

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_subsystem_level_functional_test` — Functional test

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_subsystem_level_thermal_cycling` — Thermal cycling

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_subsystem_level_thermal_vacuum_bake_out` — Thermal vacuum bake-out

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_subsystem_level_vibration_and_shock` — Vibration and shock

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_suppliers_and_qualification_qualification_status` — Qualification status

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_suppliers_and_qualification_second_sources` — Second sources

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_suppliers_and_qualification_supplier_survey` — Supplier survey

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_acoustic_and_shock` — Acoustic and shock

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_alignment_and_boresight` — Alignment and boresight

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_deployment_and_mechanisms` — Deployment and mechanisms

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_full_functional_and_performance` — Full functional and performance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_leak_and_pressure` — Leak and pressure

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_magnetic_and_electrostatic_cleanliness` — Magnetic and electrostatic cleanliness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_mass_properties_and_balance` — Mass properties and balance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_sine_and_random_vibration` — Sine and random vibration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_system_level_thermal_vacuum_and_thermal_balance` — Thermal vacuum and thermal balance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | programme |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_user_segment_service_interface` — Service interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_user_segment_tasking_and_delivery` — Tasking and delivery

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_user_segment_user_terminals` — User terminals

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `mgt_value_proposition_held_internally` — Held internally

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_value_proposition_shared_with_the_customer` — Shared with the customer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mgt_value_proposition_the_case_to_proceed` — The case to proceed

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `mis` — Environment and orbit — addition

### `mis_access_area` — Instantaneous access area

> How much ground does one satellite see at any instant?

| | |
|---|---|
| symbol | `A_acc` |
| type | `Area` |
| unit | m^2 |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `A = 2*pi*Re^2*(1 - cos(lambda))` |
| source | `larson_wertz` |
| valid over | 0 … 520000000000000 m^2 |

- **lower bound** — an area cannot be negative
- **upper bound** — cannot exceed the surface area of the Earth
- **reads** — `orbit_earth_central_angle`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mis_availability` — Constellation service availability

> What fraction of the time is the service available?

| | |
|---|---|
| symbol | `A_svc` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | operations |
| evidence tier | B |
| relation | `A = P(at least ceil(0.8*N) of N satellites up)` |
| source | `larson_wertz` |
| valid over | 0 … 1 - |

- **lower bound** — an availability cannot be negative
- **upper bound** — an availability cannot exceed one
- **reads** — `mis_unit_availability`, `mis_satellites`
- **read by** — `kpi_availability`
- **contributes to** — kpi_availability
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mis_coverage_fraction` — Instantaneous coverage fraction

> What fraction of the Earth does one satellite see at any instant?

| | |
|---|---|
| symbol | `f_cov` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `f = (1 - cos(lambda))/2` |
| source | `larson_wertz` |
| valid over | 0 … 1 - |

- **lower bound** — a fraction cannot be negative
- **upper bound** — a satellite cannot see more than the whole Earth
- **reads** — `orbit_earth_central_angle`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mis_delivery_time` — Delivery time to the customer

> How long does the product take to reach the customer once processed?

| | |
|---|---|
| symbol | `t_del` |
| type | `Time` |
| unit | min |
| kind | declared |
| owner | operations |
| evidence tier | A |
| relation | `t_del = 3` |
| source | `orbitt_case_c1` |
| declared value | **3** min |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 1440 min |

- **lower bound** — below six seconds no delivery path in this design runs
- **upper bound** — above a day the product is not the near-real-time one being sold
- **read by** — `mis_latency`
- **contributes to** — kpi_latency

### `mis_latency` — End to end latency

> How long from collection to the product reaching the customer?

| | |
|---|---|
| symbol | `t_lat` |
| type | `Time` |
| unit | min |
| kind | computed |
| owner | operations |
| evidence tier | B |
| relation | `t = t_wait + t_downlink + t_process + t_deliver` |
| source | `orbitt_case_c1` |
| valid over | 0 … 2880 min |

- **lower bound** — a latency cannot be negative
- **upper bound** — above two days the product is not the near-real-time one being sold
- **reads** — `mis_time_to_downlink`, `com_contact_time`, `mis_processing_time`, `mis_delivery_time`
- **read by** — `kpi_latency`
- **contributes to** — kpi_latency
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The wait term dominates, and it is the one a constellation design can actually move.

### `mis_processing_time` — Ground processing time

> How long does ground processing take after the data lands?

| | |
|---|---|
| symbol | `t_proc` |
| type | `Time` |
| unit | min |
| kind | declared |
| owner | operations |
| evidence tier | A |
| relation | `t_proc = 8` |
| source | `orbitt_case_c1` |
| declared value | **8** min |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 1440 min |

- **lower bound** — below six seconds no ground processing chain in this design runs
- **upper bound** — above a day the product is not the near-real-time one being sold
- **read by** — `mis_latency`
- **contributes to** — kpi_latency

### `mis_revisit` — Mean revisit time

> How often does the constellation come back to a given place?

| | |
|---|---|
| symbol | `t_rev` |
| type | `Time` |
| unit | h |
| kind | computed |
| owner | systems |
| evidence tier | C |
| relation | `t = A_earth/(N*W*V_g)` |
| source | `larson_wertz` |
| valid over | 0 … 2400 h |

- **lower bound** — a revisit time cannot be negative
- **upper bound** — above 100 days the constellation does not provide a service
- **reads** — `pay_swath`, `orbit_ground_track_speed`, `mis_satellites`
- **read by** — `kpi_revisit`
- **contributes to** — kpi_revisit
- **assumes** Uniform coverage of the whole globe, no latitude weighting — fails when a Sun-synchronous constellation revisits the poles far more often than the equator, so this mean is optimistic at low latitudes and pessimistic at high ones
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mis_satellites` — Number of satellites

> How many satellites are in the constellation?

| | |
|---|---|
| symbol | `N_sat` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `N_sat = 24` |
| source | `orbitt_case_c1` |
| declared value | **24** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 500 - |

- **lower bound** — one satellite is the single-satellite concept, the limit of the trade
- **upper bound** — above 500 the constellation is not the one costed here
- **read by** — `cost_production`, `cost_programme`, `cost_replacement_avoided`, `mis_availability`, `mis_revisit`
- **contributes to** — kpi_revisit, kpi_cost_per_year

### `mis_satellites_required` — Satellites required for the revisit target

> How many satellites would the revisit requirement need?

| | |
|---|---|
| symbol | `N_req` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | systems |
| evidence tier | C |
| relation | `N = A_earth/(t_target*W*V_g)` |
| source | `larson_wertz` |
| valid over | 0 … 100000 - |

- **lower bound** — a satellite count cannot be negative
- **upper bound** — above 100000 the concept is not the one being designed
- **reads** — `kpi_revisit_required`, `pay_swath`, `orbit_ground_track_speed`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The inverse question, and the one a concept trade actually asks.

### `mis_time_to_downlink` — Mean time to next downlink

> How long does data wait on board before it can come down?

| | |
|---|---|
| symbol | `t_dl` |
| type | `Time` |
| unit | min |
| kind | computed |
| owner | operations |
| evidence tier | A |
| relation | `t = 86400/(2*N_pass)` |
| source | `larson_wertz` |
| valid over | 0 … 1440 min |

- **lower bound** — a wait cannot be negative
- **upper bound** — above a day the mission is not operable
- **reads** — `com_passes_per_day`
- **read by** — `mis_latency`
- **contributes to** — kpi_latency
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `mis_unit_availability` — Single satellite availability

> What fraction of the time is one satellite fully operational?

| | |
|---|---|
| symbol | `A_unit` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | operations |
| evidence tier | A |
| relation | `A_unit = 0.97` |
| source | `orbitt_case_c1` |
| declared value | **0.97** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 0.9999 - |

- **lower bound** — below 50% the satellite is unavailable more often than not
- **upper bound** — above 0.9999 no small satellite has demonstrated that availability
- **read by** — `mis_availability`
- **contributes to** — kpi_availability


## `multipay` — Multi-payload

### `l3_multipay_ach_01` — Number of payloads

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_02` — Boresight separation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_03` — Payload mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_04` — Accommodation area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_05` — Shared power allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_06` — Shared downlink allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_07` — Thermal dissipation share

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_08` — Simultaneity window

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_ach_09` — Operating conflict rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_interface` — Multi-payload — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_n_01` — Multi-payload · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_02` — Multi-payload · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_03` — Multi-payload · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_04` — Multi-payload · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_05` — Multi-payload · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_06` — Multi-payload · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_07` — Multi-payload · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_08` — Multi-payload · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_09` — Multi-payload · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_10` — Multi-payload · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_11` — Multi-payload · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_12` — Multi-payload · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_13` — Multi-payload · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_14` — Multi-payload · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_15` — Multi-payload · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_16` — Multi-payload · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_17` — Multi-payload · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_18` — Multi-payload · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_n_19` — Multi-payload · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_multipay_req_01` — Number of payloads

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_02` — Boresight separation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_03` — Payload mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_04` — Accommodation area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_05` — Shared power allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_06` — Shared downlink allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_07` — Thermal dissipation share

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_08` — Simultaneity window

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_multipay_req_09` — Operating conflict rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `navod` — Navigation & OD

### `l3_navod_ach_01` — Orbit determination accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_02` — Navigation filter rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_03` — Phasing tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_04` — Along-track control tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_05` — Position error growth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_06` — Ephemeris prediction accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_07` — Prediction horizon

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_08` — Ephemeris upload cadence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_ach_09` — Orbit determination cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_interface` — Navigation & OD — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_n_01` — Navigation & OD · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_02` — Navigation & OD · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_03` — Navigation & OD · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_04` — Navigation & OD · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_05` — Navigation & OD · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_06` — Navigation & OD · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_07` — Navigation & OD · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_08` — Navigation & OD · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_09` — Navigation & OD · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_10` — Navigation & OD · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_11` — Navigation & OD · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_12` — Navigation & OD · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_13` — Navigation & OD · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_14` — Navigation & OD · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_15` — Navigation & OD · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_16` — Navigation & OD · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_17` — Navigation & OD · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_18` — Navigation & OD · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_n_19` — Navigation & OD · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navod_req_01` — Orbit determination accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_02` — Navigation filter rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_03` — Phasing tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_04` — Along-track control tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_05` — Position error growth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_06` — Ephemeris prediction accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_07` — Prediction horizon

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_08` — Ephemeris upload cadence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navod_req_09` — Orbit determination cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `navsense` — Navigation sensing

### `l3_navsense_ach_01` — GNSS solution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_ach_02` — Accelerometer class

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_ach_03` — Observed acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_ach_04` — Density estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_ach_05` — Scale factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_ach_06` — Unmodelled acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_interface` — Navigation sensing — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_n_01` — Navigation sensing · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_02` — Navigation sensing · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_03` — Navigation sensing · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_04` — Navigation sensing · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_05` — Navigation sensing · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_06` — Navigation sensing · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_07` — Navigation sensing · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_08` — Navigation sensing · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_09` — Navigation sensing · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_10` — Navigation sensing · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_11` — Navigation sensing · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_12` — Navigation sensing · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_13` — Navigation sensing · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_14` — Navigation sensing · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_15` — Navigation sensing · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_16` — Navigation sensing · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_17` — Navigation sensing · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_18` — Navigation sensing · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_n_19` — Navigation sensing · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_navsense_req_01` — GNSS solution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_req_02` — Accelerometer class

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_req_03` — Observed acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_req_04` — Density estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_req_05` — Scale factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_navsense_req_06` — Unmodelled acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `orbit` — Environment and orbit — addition

### `orbit_altitude` — Orbit altitude

> At what altitude does the constellation fly? This is the one parameter the whole architecture is shaped around.

| | |
|---|---|
| symbol | `h` |
| type | `Length` |
| unit | km |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `h = 250` |
| source | `orbitt_case_c1` |
| declared value | **250** km |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 150 … 450 km |

- **lower bound** — below 150 km the flow stops being free-molecular for a body of this size and every aerodynamic node in the tree is outside its envelope
- **upper bound** — above 450 km there is not enough air for the air-breathing concept to be the reason for the design
- **read by** — `env_atomic_oxygen_density`, `env_local_temperature`, `env_mass_density`, `env_mean_molar_mass`, `env_number_density`, `env_scale_height`, `orbit_deorbit_delta_v`, `orbit_lifetime_uncontrolled`, `orbit_radius`, `pay_gsd_detector`, `pay_gsd_diffraction`
- **contributes to** — kpi_revisit, kpi_gsd, kpi_service_lifetime

Move this one value and 54 other nodes become stale. That is the requirement the tool exists to satisfy.

### `orbit_beta_angle` — Beta angle

> What angle does the Sun make with the orbit plane? It decides eclipse duration and therefore both the power and the thermal design.

| | |
|---|---|
| symbol | `beta` |
| type | `Angle` |
| unit | deg |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `beta = 30` |
| source | `larson_wertz` |
| declared value | **30** deg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | -90 … 90 deg |

- **lower bound** — the angle between the orbit plane and the Sun is defined on -90..90
- **upper bound** — the angle between the orbit plane and the Sun is defined on -90..90
- **read by** — `orbit_eclipse_fraction`

### `orbit_decay_rate` — Orbital decay rate

> How fast does the orbit fall if nothing compensates the drag?

| | |
|---|---|
| symbol | `da_dt` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | systems |
| evidence tier | B |
| relation | `da/dt = -rho*sqrt(mu*a)/BC` |
| source | `vallado2013` |
| valid over | -10 … 0 m/s |

- **lower bound** — a decay faster than 10 m/s of semi-major axis per second is re-entry, not an orbit
- **upper bound** — drag cannot raise an orbit; a positive value here is a sign error upstream
- **reads** — `env_mass_density`, `aero_ballistic_coefficient`, `orbit_radius`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_service_lifetime
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `orbit_deorbit_delta_v` — Disposal delta-v

> What does the end-of-life disposal manoeuvre cost, as required by the debris mitigation standard?

| | |
|---|---|
| symbol | `dv_dis` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `dv = |V_circ - V_transfer(r, a_t)|` |
| source | `iso24113` |
| valid over | 0 … 500 m/s |

- **lower bound** — a disposal manoeuvre cannot cost negative delta-v
- **upper bound** — above 500 m/s the disposal budget dominates the design and the altitude choice should be revisited
- **reads** — `orbit_altitude`
- **read by** — `mass_disposal_propellant`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Carried as a design variable rather than discovered at the end, because ISO 24113 makes it a requirement and not a courtesy.

### `orbit_earth_central_angle` — Earth central half-angle

> How much of the Earth's surface is in view at the working elevation?

| | |
|---|---|
| symbol | `lambda` |
| type | `Angle` |
| unit | deg |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `lambda = acos((Re/r)*cos(eps)) - eps` |
| source | `larson_wertz` |
| valid over | 0 … 86 deg |

- **lower bound** — a negative half-angle means the target is below the horizon
- **upper bound** — beyond 85 degrees the whole visible hemisphere is in view, which does not occur in this band
- **reads** — `orbit_radius`, `orbit_min_elevation`
- **read by** — `com_contact_time`, `mis_access_area`, `mis_coverage_fraction`, `orbit_swath_width`
- **evidence** 250 km, 10 deg elevation — expect 0.150429309 ± 0.000001 relative, from `larson_wertz` (independent-derivation)

### `orbit_eccentricity` — Orbit eccentricity

> How circular is the orbit?

| | |
|---|---|
| symbol | `e` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `e = 0` |
| source | `orbitt_case_c1` |
| declared value | **0** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.05 - |

- **lower bound** — a negative eccentricity is not an orbit
- **upper bound** — above 0.05 the perigee of a 250 km orbit is below 200 km, where the drag per revolution stops being a small perturbation and the circular-orbit relations used throughout this tree no longer apply
- **read by** — `orbit_nodal_regression`, `orbit_sun_sync_inclination`

Moving this reaches 3 nodes. Moving the altitude reaches 54. The difference is the answer to how much of the design a change just invalidated.

### `orbit_eclipse_fraction` — Eclipse fraction of the orbit

> What fraction of each revolution is spent in the Earth's shadow?

| | |
|---|---|
| symbol | `f_ecl` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `f_ecl = (1/pi)*acos(sqrt(r^2 - Re^2)/(r*cos(beta)))` |
| source | `larson_wertz` |
| valid over | 0 … 0.45 - |

- **lower bound** — a negative eclipse fraction is not a fraction
- **upper bound** — no circular orbit in this band spends more than 45% of a revolution in shadow
- **reads** — `orbit_radius`, `orbit_beta_angle`
- **read by** — `pwr_available`, `pwr_battery_energy`
- **assumes** Cylindrical shadow, no penumbra — fails when the penumbra adds about 10 seconds per orbit, which matters for a precise thermal transient and not for a power budget
- **evidence** 250 km, beta = 30 deg — expect 0.398283597 ± 0.000001 relative, from `larson_wertz` (independent-derivation)

### `orbit_ground_track_speed` — Ground track speed

> How fast does the sub-satellite point move over the ground?

| | |
|---|---|
| symbol | `V_g` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `V_g = V*Re/r` |
| source | `larson_wertz` |
| valid over | 6000 … 8000 m/s |

- **lower bound** — below 6 km/s is outside this tool's altitude band
- **upper bound** — the ground track can never move faster than the orbital speed
- **reads** — `orbit_velocity`, `orbit_radius`
- **read by** — `com_doppler`, `mis_revisit`, `mis_satellites_required`, `pay_dwell_time`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Not the orbital speed. The difference is what sets sensor dwell time and therefore signal-to-noise.

### `orbit_inclination` — Orbit inclination

> What is the angle between the orbit plane and the equator?

| | |
|---|---|
| symbol | `i` |
| type | `Angle` |
| unit | deg |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `i = 96.6` |
| source | `orbitt_case_c1` |
| declared value | **96.6** deg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 180 deg |

- **lower bound** — inclination is defined on 0..180 degrees
- **upper bound** — inclination is defined on 0..180 degrees
- **read by** — `orbit_nodal_regression`

### `orbit_lifetime_uncontrolled` — Uncontrolled orbital lifetime

> If propulsion stops, how long before the satellite re-enters?

| | |
|---|---|
| symbol | `t_life` |
| type | `Time` |
| unit | d |
| kind | computed |
| owner | systems |
| evidence tier | C |
| relation | `t = INT da/(da/dt) from h to 120 km` |
| source | `vallado2013` |
| valid over | 0 … 36500 d |

- **lower bound** — a lifetime cannot be negative
- **upper bound** — above 100 years the 25-year debris rule is violated and the number's precision is irrelevant
- **reads** — `orbit_altitude`, `aero_ballistic_coefficient`, `env_exospheric_temperature`
- **read by** — `kpi_service_lifetime`
- **contributes to** — kpi_service_lifetime
- **assumes** Atmospheric conditions fixed at the current activity for the whole decay — fails when over a solar cycle the density at a given altitude changes by more than an order of magnitude, so this is an order-of-magnitude answer and is reported as one
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `orbit_makeup_delta_v` — Drag make-up delta-v per year

> How much delta-v a year does holding this altitude cost?

| | |
|---|---|
| symbol | `dv_dm` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | systems |
| evidence tier | B |
| relation | `dv = (D/m)*t` |
| source | `vallado2013` |
| valid over | 0 … 100000 m/s |

- **lower bound** — make-up delta-v cannot be negative
- **upper bound** — above 100 km/s a year no propulsion system of any kind closes, and the answer is that the altitude is wrong
- **reads** — `aero_drag_acceleration`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

This is the number an air-breathing system exists to make free. A stored-propellant design at 250 km spends several kilometres per second a year on it.

### `orbit_min_elevation` — Minimum useful elevation angle

> Below what elevation is a target or a ground station no longer usable?

| | |
|---|---|
| symbol | `eps` |
| type | `Angle` |
| unit | deg |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `eps = 10` |
| source | `larson_wertz` |
| declared value | **10** deg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 90 deg |

- **lower bound** — at the horizon the slant range is longest, the atmosphere is thickest and no useful link or image is obtained; zero is the limit, not a working value
- **upper bound** — 90 degrees is the zenith, at which the access circle has no area
- **read by** — `orbit_earth_central_angle`, `orbit_slant_range`

### `orbit_mission_duration` — Mission duration

> How long must one satellite operate?

| | |
|---|---|
| symbol | `T_mis` |
| type | `Time` |
| unit | yr |
| kind | declared |
| owner | systems |
| evidence tier | A |
| relation | `T_mis = 5` |
| source | `orbitt_case_c1` |
| declared value | **5** yr |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 15 yr |

- **lower bound** — below six months the programme cannot amortise a satellite, so it is not the mission being designed
- **upper bound** — above 15 years the cost model, the degradation model and the battery cycle model are all extrapolated well past their fits
- **read by** — `aero_ao_fluence`, `cost_per_year`, `cost_programme`, `pwr_battery_cycles`, `pwr_degradation`, `sw_storm_return_level`
- **contributes to** — kpi_cost_per_year

### `orbit_nodal_regression` — Nodal regression rate

> How fast does the orbit plane rotate under the second zonal harmonic?

| | |
|---|---|
| symbol | `dOmega` |
| type | `AngularRate` |
| unit | deg/d |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `dOmega/dt = -1.5*J2*sqrt(mu)*Re^2*cos(i)/((1-e^2)^2*a^(7/2))` |
| source | `vallado2013` |
| valid over | -15 … 15 deg/d |

- **lower bound** — a regression faster than 15 degrees per day does not occur in this altitude band
- **upper bound** — a regression faster than 15 degrees per day does not occur in this altitude band
- **reads** — `orbit_radius`, `orbit_eccentricity`, `orbit_inclination`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** 250 km, 96.6 deg — expect 0.000000202216606 ± 0.000001 relative, from `vallado2013` (independent-derivation)

### `orbit_period` — Orbital period

> How long does one revolution take?

| | |
|---|---|
| symbol | `T_orb` |
| type | `Time` |
| unit | min |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `T = 2*pi*sqrt(a^3/mu)` |
| source | `vallado2013` |
| valid over | 83 … 104 min |

- **lower bound** — a period below 5000 s corresponds to an orbit inside the Earth
- **upper bound** — 6200 s corresponds to about 1000 km, outside this tool's band
- **reads** — `orbit_radius`
- **read by** — `com_contact_time`, `gnc_momentum_storage`, `pwr_battery_cycles`, `pwr_battery_energy`
- **evidence** 250 km circular — expect 5370.29564631 ± 0.000000001 relative, from `vallado2013` (independent-derivation)

### `orbit_radius` — Geocentric radius

> How far is the spacecraft from the centre of the Earth?

| | |
|---|---|
| symbol | `r` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `r = R_earth + h` |
| source | `wgs84` |
| valid over | 6400000 … 7000000 m |

- **lower bound** — below the Earth's mean radius is not an orbit
- **upper bound** — above 7000 km is outside the band this tool is scoped to
- **reads** — `orbit_altitude`
- **read by** — `env_magnetic_field`, `gnc_gravity_gradient_torque`, `orbit_decay_rate`, `orbit_earth_central_angle`, `orbit_eclipse_fraction`, `orbit_ground_track_speed`, `orbit_nodal_regression`, `orbit_period`, `orbit_slant_range`, `orbit_sun_sync_inclination`, `orbit_velocity`, `thm_view_factor`
- **evidence** 250 km — expect 6628137 ± 0.000000000001 relative, from `wgs84` (independent-derivation)

### `orbit_slant_range` — Slant range at working elevation

> How far away is a target or ground station at the edge of the access circle?

| | |
|---|---|
| symbol | `d` |
| type | `Length` |
| unit | km |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `d = sqrt(Re^2 + r^2 - 2*Re*r*cos(lambda))` |
| source | `larson_wertz` |
| valid over | 100 … 4000 km |

- **lower bound** — the slant range is at least the altitude
- **upper bound** — beyond 4000 km the geometry is outside this tool's band
- **reads** — `orbit_radius`, `orbit_min_elevation`
- **read by** — `com_path_loss`, `gnc_ground_pointing_error`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `orbit_sun_sync_inclination` — Sun-synchronous inclination

> What inclination makes the orbit plane keep pace with the Sun?

| | |
|---|---|
| symbol | `i_ss` |
| type | `Angle` |
| unit | deg |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `cos(i) = -Omega_sun*(1-e^2)^2*a^(7/2)/(1.5*J2*sqrt(mu)*Re^2)` |
| source | `vallado2013` |
| valid over | 85 … 115 deg |

- **lower bound** — a Sun-synchronous inclination is always retrograde and above about 95 degrees in this band
- **upper bound** — above 115 degrees no Sun-synchronous solution exists below 2000 km
- **reads** — `orbit_radius`, `orbit_eccentricity`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** 250 km circular — expect 1.68420864 ± 0.000001 relative, from `vallado2013` (independent-derivation)

### `orbit_swath_width` — Ground swath width

> How wide a strip of ground does one pass cover?

| | |
|---|---|
| symbol | `W` |
| type | `Length` |
| unit | km |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `W = 2*lambda*Re` |
| source | `larson_wertz` |
| valid over | 1 … 40000 km |

- **lower bound** — a swath below one kilometre would not be a swath
- **upper bound** — a swath cannot exceed the Earth's circumference
- **reads** — `orbit_earth_central_angle`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_revisit
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `orbit_velocity` — Circular orbital speed

> How fast is the spacecraft moving through the atmosphere?

| | |
|---|---|
| symbol | `V` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | systems |
| evidence tier | A |
| relation | `V = sqrt(mu/r)` |
| source | `vallado2013` |
| valid over | 7000 … 8200 m/s |

- **lower bound** — below 7 km/s corresponds to an orbit above 1000 km, outside this tool's band
- **upper bound** — 8.2 km/s exceeds the surface circular speed and is not reachable
- **reads** — `orbit_radius`
- **read by** — `aero_ao_fluence`, `aero_drag_coefficient`, `aero_dynamic_pressure`, `aero_speed_ratio`, `orbit_ground_track_speed`, `prop_capture_efficiency`, `prop_collected_flow`, `prop_compression_ratio`, `prop_incident_flux`, `thm_aero_heating`
- **assumes** Two-body, circular — fails when at e = 0.05 the speed varies by 5% around the orbit and a drag figure computed at the mean underestimates the perigee pass
- **evidence** 250 km circular — expect 7754.84549737 ± 0.000000001 relative, from `vallado2013` (independent-derivation)
- **evidence** 400 km circular — expect 7668.55817541 ± 0.000000001 relative, from `vallado2013` (independent-derivation)


## `orbmaint` — Orbit maintenance

### `l3_orbmaint_ach_01` — Thrust modulation resolution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_02` — Propellant reserve

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_03` — Collision avoidance delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_04` — Station-keeping box

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_05` — Station-keeping delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_06` — Manoeuvre frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_07` — Manoeuvre duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_08` — Thrust duty limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_09` — Deorbit delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_ach_10` — Abort altitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_interface` — Orbit maintenance — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_n_01` — Orbit maintenance · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_02` — Orbit maintenance · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_03` — Orbit maintenance · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_04` — Orbit maintenance · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_05` — Orbit maintenance · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_06` — Orbit maintenance · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_07` — Orbit maintenance · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_08` — Orbit maintenance · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_09` — Orbit maintenance · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_10` — Orbit maintenance · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_11` — Orbit maintenance · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_12` — Orbit maintenance · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_13` — Orbit maintenance · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_14` — Orbit maintenance · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_15` — Orbit maintenance · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_16` — Orbit maintenance · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_17` — Orbit maintenance · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_18` — Orbit maintenance · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_n_19` — Orbit maintenance · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_orbmaint_req_01` — Thrust modulation resolution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_02` — Propellant reserve

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_03` — Collision avoidance delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_04` — Station-keeping box

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_05` — Station-keeping delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_06` — Manoeuvre frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_07` — Manoeuvre duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_08` — Thrust duty limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_09` — Deorbit delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_orbmaint_req_10` — Abort altitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `pay` — Payload

### `pay_aperture` — Optical aperture diameter

> How large is the telescope's entrance pupil?

| | |
|---|---|
| symbol | `D_o` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `D_o = 0.3` |
| source | `orbitt_case_c1` |
| declared value | **0.3** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.02 … 1.5 m |

- **lower bound** — below 2 cm the diffraction limit is coarser than any useful ground sample distance from this altitude
- **upper bound** — above 1.5 m the telescope does not fit the launch envelope of this class
- **read by** — `pay_gsd_diffraction`, `pay_signal_electrons`
- **contributes to** — kpi_gsd

### `pay_bits_per_pixel` — Bits per pixel

> How many bits does each sample carry?

| | |
|---|---|
| symbol | `b_px` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `b_px = 12` |
| source | `orbitt_case_c1` |
| declared value | **12** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 8 … 16 - |

- **lower bound** — below 8 bits the dynamic range is not usable for radiometry
- **upper bound** — above 16 bits exceeds the detector's own noise floor and the extra bits carry noise
- **read by** — `pay_scene_volume`

### `pay_compression` — Image compression ratio

> By what factor is the imagery compressed before downlink?

| | |
|---|---|
| symbol | `CR_img` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `CR_img = 3` |
| source | `ccsds122` |
| declared value | **3** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 20 - |

- **lower bound** — unity is no compression, the limit
- **upper bound** — above 20:1 the compression is lossy enough to destroy the radiometry the product is sold on
- **read by** — `pay_scene_volume`

### `pay_dwell_time` — Per-sample dwell time

> How long does the sensor have to collect photons from one ground sample?

| | |
|---|---|
| symbol | `t_dwell` |
| type | `Time` |
| unit | s |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `t = GSD/V_g` |
| source | `larson_wertz` |
| valid over | 0.000000001 … 1 s |

- **lower bound** — below a nanosecond no detector integrates
- **upper bound** — above one second the ground has moved many samples during the integration
- **reads** — `pay_gsd`, `orbit_ground_track_speed`
- **read by** — `pay_signal_electrons`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The constraint low altitude imposes and higher orbits do not: the ground moves under the sensor faster in angular terms, which partly gives back the signal-to-noise the shorter range won.

### `pay_focal_length` — Optical focal length

> What is the effective focal length of the telescope?

| | |
|---|---|
| symbol | `f_o` |
| type | `Length` |
| unit | m |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `f_o = 2.4` |
| source | `orbitt_case_c1` |
| declared value | **2.4** m |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 20 m |

- **lower bound** — below 5 cm the detector-limited sample is coarser than anything useful
- **upper bound** — above 20 m no folded optical path of this class is that long
- **read by** — `pay_gsd_detector`, `pay_signal_electrons`

### `pay_geolocation_error` — RF geolocation error

> How accurately can an emitter on the ground be located?

| | |
|---|---|
| symbol | `e_geo` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | payload |
| evidence tier | B |
| relation | `e = c*sigma_tau*GDOP` |
| source | `larson_wertz` |
| valid over | 0.01 … 10000000 m |

- **lower bound** — below a centimetre no time-difference system in this design performs that well
- **upper bound** — above 10000 km the answer carries no information
- **reads** — `pay_toa_uncertainty`, `pay_rf_gdop`
- **read by** — `kpi_geolocation`
- **contributes to** — kpi_geolocation
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_gsd` — Achieved ground sample distance

> What ground sample distance does the system actually achieve?

| | |
|---|---|
| symbol | `GSD` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `GSD = max(GSD_diffraction, GSD_detector)` |
| source | `larson_wertz` |
| valid over | 0.001 … 1000 m |

- **lower bound** — below a millimetre the inputs are not physical
- **upper bound** — above a kilometre no product in this design is being made
- **reads** — `pay_gsd_diffraction`, `pay_gsd_detector`
- **read by** — `kpi_gsd`, `pay_dwell_time`, `pay_swath`
- **contributes to** — kpi_gsd
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Taking the better of the two produces a resolution claim the optics cannot deliver and the detector cannot sample, and it survives review because both inputs are individually correct.

### `pay_gsd_detector` — Detector-limited ground sample

> What is the finest detail the detector can sample, whatever the optics?

| | |
|---|---|
| symbol | `GSD_p` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `GSD = h*p/f` |
| source | `larson_wertz` |
| valid over | 0.001 … 1000 m |

- **lower bound** — below a millimetre the relation is being fed a focal length that is not physical
- **upper bound** — above a kilometre no product in this design is being made
- **reads** — `orbit_altitude`, `pay_pixel_pitch`, `pay_focal_length`
- **read by** — `pay_gsd`
- **evidence** 250 km, 5.5 um pixel, 2.4 m focal length — expect 0.572917 ± 0.00001 relative, from `larson_wertz` (independent-derivation)

### `pay_gsd_diffraction` — Diffraction-limited ground sample

> What is the finest detail the optics can resolve, whatever the detector?

| | |
|---|---|
| symbol | `GSD_d` |
| type | `Length` |
| unit | m |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `GSD = 1.22*lambda*h/D` |
| source | `larson_wertz` |
| valid over | 0.001 … 1000 m |

- **lower bound** — below a millimetre the relation is being fed an aperture that is not physical
- **upper bound** — above a kilometre no product in this design is being made
- **reads** — `orbit_altitude`, `pay_wavelength`, `pay_aperture`
- **read by** — `pay_gsd`
- **contributes to** — kpi_gsd
- **evidence** 250 km, 550 nm, 0.3 m aperture — expect 0.55917 ± 0.0001 relative, from `larson_wertz` (independent-derivation)

### `pay_pixel_pitch` — Detector pixel pitch

> How large is one detector pixel?

| | |
|---|---|
| symbol | `p_x` |
| type | `Length` |
| unit | um |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `p_x = 5.5` |
| source | `orbitt_case_c1` |
| declared value | **5.5** um |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 30 um |

- **lower bound** — below one micrometre no space-qualified detector has pixels that small
- **upper bound** — above 30 micrometres the detector-limited sample dominates and the aperture is wasted
- **read by** — `pay_gsd_detector`, `pay_signal_electrons`

### `pay_pixels_across` — Detector pixels across track

> How many pixels does the detector have across the swath?

| | |
|---|---|
| symbol | `N_x` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `N_x = 8192` |
| source | `orbitt_case_c1` |
| declared value | **8192** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 256 … 40000 - |

- **lower bound** — below 256 pixels the swath is too narrow to be useful
- **upper bound** — above 40000 pixels no single space detector array is that wide
- **read by** — `pay_scene_volume`, `pay_swath`

### `pay_power` — Payload orbit-average power

> How much power does the whole payload set draw, averaged over an orbit?

| | |
|---|---|
| symbol | `P_pay` |
| type | `Power` |
| unit | W |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `P_pay = 120` |
| source | `orbitt_case_c1` |
| declared value | **120** W |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 3000 W |

- **lower bound** — a payload load cannot be negative
- **upper bound** — above 3 kW the payload is the spacecraft
- **read by** — `prop_throttle`, `pwr_demand`
- **contributes to** — kpi_power_margin

### `pay_quantum_efficiency` — Detector quantum efficiency

> What fraction of incident photons become photoelectrons?

| | |
|---|---|
| symbol | `QE` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `QE = 0.75` |
| source | `orbitt_case_c1` |
| declared value | **0.75** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 0.95 - |

- **lower bound** — below 5% the detector is not one considered here
- **upper bound** — above 95% no back-illuminated detector reaches it across the band
- **read by** — `pay_signal_electrons`

### `pay_radiance` — At-aperture spectral radiance

> How bright is the scene at the aperture, per unit wavelength?

| | |
|---|---|
| symbol | `L_ap` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `L_ap = 60` |
| source | `modtran_ref` |
| declared value | **60** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 500 - |

- **lower bound** — below 0.1 W/m2/sr/um the scene is darker than any night-time reference used here
- **upper bound** — above 500 W/m2/sr/um the scene is brighter than a desert at noon
- **read by** — `pay_signal_electrons`

### `pay_read_noise` — Detector read noise

> How many electrons of noise does one read add?

| | |
|---|---|
| symbol | `n_read` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `n_read = 8` |
| source | `orbitt_case_c1` |
| declared value | **8** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 100 - |

- **lower bound** — below one electron no space detector in this class reads that quietly
- **upper bound** — above 100 electrons the detector is not usable for this product
- **read by** — `pay_snr`

### `pay_rf_bandwidth` — Intercept receiver bandwidth

> Over what bandwidth does the geolocation payload observe?

| | |
|---|---|
| symbol | `B_rf` |
| type | `Frequency` |
| unit | MHz |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `B_rf = 20` |
| source | `orbitt_case_c2` |
| declared value | **20** MHz |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 500 MHz |

- **lower bound** — below 100 kHz the timing precision is too coarse for any geolocation requirement here
- **upper bound** — above 500 MHz the digitiser and the downlink neither fit nor close
- **read by** — `pay_toa_uncertainty`

### `pay_rf_gdop` — Geolocation geometric dilution

> How much does the formation geometry amplify timing error into position error?

| | |
|---|---|
| symbol | `GDOP_rf` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `GDOP_rf = 4` |
| source | `orbitt_case_c2` |
| declared value | **4** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 1 … 50 - |

- **lower bound** — a dilution below one is not geometrically possible
- **upper bound** — above 50 the formation geometry is degenerate and no solution is produced
- **read by** — `pay_geolocation_error`
- **contributes to** — kpi_geolocation

### `pay_rf_integration` — Intercept integration time

> How long is the emitter observed for one measurement?

| | |
|---|---|
| symbol | `t_int_rf` |
| type | `Time` |
| unit | s |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `t_int_rf = 0.01` |
| source | `orbitt_case_c2` |
| declared value | **0.01** s |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.000001 … 10 s |

- **lower bound** — below a microsecond the emitter is not observed long enough to time it
- **upper bound** — above 10 seconds the emitter and the geometry both move during the observation
- **read by** — `pay_toa_uncertainty`

### `pay_rf_snr` — Intercept signal to noise ratio

> What signal to noise ratio does the intercepted emitter present, as a linear ratio?

| | |
|---|---|
| symbol | `SNR_rf` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `SNR_rf = 10` |
| source | `orbitt_case_c2` |
| declared value | **10** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 1000 - |

- **lower bound** — below 0.1 the signal is not detectable at all
- **upper bound** — above 1000 the emitter is so strong that geolocation is not the limiting problem
- **read by** — `pay_toa_uncertainty`

### `pay_scene_volume` — Data volume per scene

> How much data is one imaged scene?

| | |
|---|---|
| symbol | `V_scene` |
| type | `DataVolume` |
| unit | Gbit |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `V = N_x*N_y*b*bands/CR` |
| source | `ccsds122` |
| valid over | 0 … 1000000 Gbit |

- **lower bound** — a data volume cannot be negative
- **upper bound** — above a petabit no single scene in this design is produced
- **reads** — `pay_pixels_across`, `pay_bits_per_pixel`, `pay_compression`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_data_volume
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_signal_electrons` — Signal electrons per sample

> How many photoelectrons does one ground sample produce?

| | |
|---|---|
| symbol | `N_e` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | payload |
| evidence tier | B |
| relation | `N_e = L*Omega*A_px*B*tau*QE*t/(h*c/lambda)` |
| source | `larson_wertz` |
| valid over | 0 … 1000000000 - |

- **lower bound** — an electron count cannot be negative
- **upper bound** — above a billion electrons the well is saturated many times over
- **reads** — `pay_radiance`, `pay_aperture`, `pay_focal_length`, `pay_pixel_pitch`, `pay_transmission`, `pay_quantum_efficiency`, `pay_dwell_time`, `pay_wavelength`
- **read by** — `pay_snr`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_snr` — Optical signal to noise ratio

> How clean is one sample?

| | |
|---|---|
| symbol | `SNR_o` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | payload |
| evidence tier | B |
| relation | `SNR = N_e/sqrt(N_e + N_dark + n_read^2)` |
| source | `larson_wertz` |
| valid over | 0 … 10000 - |

- **lower bound** — a signal to noise ratio cannot be negative
- **upper bound** — above 10000 the detector is far outside its linear range
- **reads** — `pay_signal_electrons`, `pay_read_noise`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — kpi_gsd
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_swath` — Optical swath width

> How wide a strip does one optical pass image?

| | |
|---|---|
| symbol | `W_o` |
| type | `Length` |
| unit | km |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `W = GSD*N_x` |
| source | `larson_wertz` |
| valid over | 0.1 … 1000 km |

- **lower bound** — below 100 m the swath is not a swath
- **upper bound** — above 1000 km the field of view is not one this telescope has
- **reads** — `pay_gsd`, `pay_pixels_across`
- **read by** — `mis_revisit`, `mis_satellites_required`
- **contributes to** — kpi_revisit
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_toa_uncertainty` — Time of arrival uncertainty

> How precisely can the payload time an intercepted signal?

| | |
|---|---|
| symbol | `sig_tau` |
| type | `Time` |
| unit | s |
| kind | computed |
| owner | payload |
| evidence tier | A |
| relation | `sigma = 1/(2*pi*B*sqrt(2*SNR*B*T))` |
| source | `larson_wertz` |
| valid over | 0.000000000000001 … 1 s |

- **lower bound** — below a femtosecond no receiver in this design times that precisely
- **upper bound** — above one second the measurement carries no geolocation information
- **reads** — `pay_rf_bandwidth`, `pay_rf_snr`, `pay_rf_integration`
- **read by** — `pay_geolocation_error`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pay_transmission` — Optical transmission

> What fraction of light entering the aperture reaches the detector?

| | |
|---|---|
| symbol | `tau_o` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `tau_o = 0.72` |
| source | `orbitt_case_c1` |
| declared value | **0.72** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 0.98 - |

- **lower bound** — below 10% the optical train is not one considered here
- **upper bound** — above 98% no multi-element telescope achieves it
- **read by** — `pay_signal_electrons`

### `pay_wavelength` — Centre wavelength

> At what wavelength does the imager work?

| | |
|---|---|
| symbol | `lam_o` |
| type | `Length` |
| unit | nm |
| kind | declared |
| owner | payload |
| evidence tier | A |
| relation | `lam_o = 550` |
| source | `orbitt_case_c1` |
| declared value | **550** nm |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 350 … 2500 nm |

- **lower bound** — below 350 nm the atmosphere absorbs most of the signal
- **upper bound** — above 2500 nm the detector is a cooled infrared array, which is not the payload described here
- **read by** — `pay_gsd_diffraction`, `pay_signal_electrons`


## `payload` — Payload

### `l3_payload_ach_01` — Payload mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_02` — Peak payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_03` — Average payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_04` — Payload data rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_05` — Payload dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_06` — Payload envelope

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_07` — Pointing requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_08` — Pointing knowledge requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_09` — Alignment stability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_10` — Operating temperature range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_11` — Field of view keep-out

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_ach_12` — Payload duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_interface` — Payload — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_n_01` — Payload · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_02` — Payload · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_03` — Payload · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_04` — Payload · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_05` — Payload · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_06` — Payload · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_07` — Payload · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_08` — Payload · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_09` — Payload · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_10` — Payload · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_11` — Payload · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_12` — Payload · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_13` — Payload · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_14` — Payload · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_15` — Payload · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_16` — Payload · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_n_17` — Payload · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_payload_req_01` — Payload mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_02` — Peak payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_03` — Average payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_04` — Payload data rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_05` — Payload dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_06` — Payload envelope

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_07` — Pointing requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_08` — Pointing knowledge requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_09` — Alignment stability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_10` — Operating temperature range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_11` — Field of view keep-out

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_payload_req_12` — Payload duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `pointing` — Pointing error budget

### `l3_pointing_ach_01` — Absolute performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_ach_02` — Relative performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_ach_03` — Absolute knowledge error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_ach_04` — Absolute measurement error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_ach_05` — Control error contribution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_ach_06` — Disturbance torque budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_interface` — Pointing error budget — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_n_01` — Pointing error budget · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_02` — Pointing error budget · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_03` — Pointing error budget · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_04` — Pointing error budget · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_05` — Pointing error budget · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_06` — Pointing error budget · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_07` — Pointing error budget · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_08` — Pointing error budget · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_09` — Pointing error budget · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_10` — Pointing error budget · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_11` — Pointing error budget · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_12` — Pointing error budget · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_13` — Pointing error budget · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_14` — Pointing error budget · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_15` — Pointing error budget · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_16` — Pointing error budget · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_17` — Pointing error budget · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_18` — Pointing error budget · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_n_19` — Pointing error budget · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_pointing_req_01` — Absolute performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_req_02` — Relative performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_req_03` — Absolute knowledge error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_req_04` — Absolute measurement error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_req_05` — Control error contribution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_pointing_req_06` — Disturbance torque budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `power` — Power

### `l3_power_ach_01` — Cell efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_02` — Bus power demand

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_03` — Depth of discharge

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_04` — Bus voltage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_05` — Array degradation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_06` — SADA angle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_07` — Harness loss

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_08` — Power margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_09` — PPU power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_10` — Electrical power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_11` — Array area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_12` — Battery capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_ach_13` — End of life power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_interface` — Power — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_n_01` — Power · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_02` — Power · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_03` — Power · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_04` — Power · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_05` — Power · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_06` — Power · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_07` — Power · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_08` — Power · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_09` — Power · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_10` — Power · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_11` — Power · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_12` — Power · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_13` — Power · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_14` — Power · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_15` — Power · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_16` — Power · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_n_17` — Power · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_power_req_01` — Cell efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_02` — Bus power demand

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_03` — Depth of discharge

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_04` — Bus voltage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_05` — Array degradation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_06` — SADA angle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_07` — Harness loss

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_08` — Power margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_09` — PPU power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_10` — Electrical power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_11` — Array area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_12` — Battery capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_power_req_13` — End of life power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_array_area` — Solar array area

> How large is the deployed solar array?

| | |
|---|---|
| symbol | `A_arr` |
| type | `Area` |
| unit | m^2 |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `A_arr = 6` |
| source | `orbitt_case_c1` |
| declared value | **6** m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 40 m^2 |

- **lower bound** — below 0.5 m2 no useful power is produced at any efficiency
- **upper bound** — above 40 m2 the array's own drag exceeds the thrust the intake can produce at every altitude in the band
- **read by** — `pwr_array_mass`, `pwr_array_power_bol`
- **contributes to** — kpi_power_margin

A design variable rather than a computed one, deliberately: it is coupled to drag through the appendage area, so a solver that sized it from the power demand alone would hide the coupling the tree exists to show.

### `pwr_array_areal_density` — Array areal density

> What does a square metre of deployed array weigh?

| | |
|---|---|
| symbol | `sigma_a` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `sigma_a = 2.8` |
| source | `larson_wertz` |
| declared value | **2.8** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 10 - |

- **lower bound** — below 0.5 kg/m2 no deployable array structure of this size is that light
- **upper bound** — above 10 kg/m2 the array mass alone breaks the mass budget
- **read by** — `pwr_array_mass`

### `pwr_array_incidence` — Mean solar incidence angle on the array

> At what mean angle does the sunlight strike the array over an orbit?

| | |
|---|---|
| symbol | `theta_s` |
| type | `Angle` |
| unit | deg |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `theta_s = 25` |
| source | `larson_wertz` |
| declared value | **25** deg |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 85 deg |

- **lower bound** — zero is normal incidence, the best case and the limit
- **upper bound** — above 85 degrees the cosine loss leaves almost nothing and the array is not pointed at the Sun in any useful sense
- **read by** — `gnc_solar_torque`, `pwr_array_power_bol`, `thm_absorbed_solar`

### `pwr_array_mass` — Array mass

> What does the deployed array weigh?

| | |
|---|---|
| symbol | `m_arr` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `m = A*sigma_a` |
| source | `larson_wertz` |
| valid over | 0 … 1000 kg |

- **lower bound** — a mass cannot be negative
- **upper bound** — above a tonne the array is not part of this spacecraft
- **reads** — `pwr_array_area`, `pwr_array_areal_density`
- **read by** — `pwr_subsystem_mass`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_array_power_bol` — Array power, beginning of life

> How much does the array produce when new, in sunlight?

| | |
|---|---|
| symbol | `P_bol` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `P_bol = S*A*eta_cell*f_pack*cos(theta)` |
| source | `larson_wertz` |
| valid over | 0 … 50000 W |

- **lower bound** — array output cannot be negative
- **upper bound** — above 50 kW the array is not the one described by these inputs
- **reads** — `pwr_array_area`, `pwr_cell_efficiency`, `pwr_packing`, `pwr_array_incidence`
- **read by** — `pwr_array_power_eol`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_array_power_eol` — Array power, end of life

> How much does the array produce at the end of the mission?

| | |
|---|---|
| symbol | `P_eol` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `P_eol = P_bol*f_deg*f_T` |
| source | `larson_wertz` |
| valid over | 0 … 50000 W |

- **lower bound** — array output cannot be negative
- **upper bound** — cannot exceed the beginning-of-life output
- **reads** — `pwr_array_power_bol`, `pwr_degradation`, `pwr_cell_derating`
- **read by** — `pwr_available`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Every power budget in this tree closes against this number, not against the beginning-of-life one.

### `pwr_available` — Orbit-average power available

> How much power is actually available to the loads, averaged over an orbit?

| | |
|---|---|
| symbol | `P_avail` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | power |
| evidence tier | B |
| relation | `P_avail = P_eol*(1 - f_ecl)*eta_direct + P_eol*f_ecl*eta_batt` |
| source | `larson_wertz` |
| valid over | 0 … 50000 W |

- **lower bound** — available power cannot be negative
- **upper bound** — above 50 kW the array is not the one described by these inputs
- **reads** — `pwr_array_power_eol`, `orbit_eclipse_fraction`, `pwr_discharge_efficiency`
- **read by** — `prop_throttle`, `pwr_margin`
- **assumes** The array is sized so that the sunlit period both runs the loads and recharges the battery — fails when if it is not, the battery state of charge walks down over successive orbits and this number is optimistic from the second day onwards
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_avionics_load` — Avionics and housekeeping load

> What does the on-board computer, the harness electronics and the housekeeping draw?

| | |
|---|---|
| symbol | `P_av` |
| type | `Power` |
| unit | W |
| kind | declared |
| owner | avionics |
| evidence tier | A |
| relation | `P_av = 35` |
| source | `orbitt_case_c1` |
| declared value | **35** W |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 5 … 500 W |

- **lower bound** — below 5 W no spacecraft avionics set runs
- **upper bound** — above 500 W the avionics dominate the bus, which no design in this class does
- **read by** — `prop_throttle`, `pwr_demand`

### `pwr_battery_cycles` — Battery charge cycles

> How many charge and discharge cycles does the battery see?

| | |
|---|---|
| symbol | `N_cyc` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `N = t_mission/T_orbit` |
| source | `larson_wertz` |
| valid over | 0 … 1000000 - |

- **lower bound** — a cycle count cannot be negative
- **upper bound** — above a million cycles no cell chemistry has data
- **reads** — `orbit_mission_duration`, `orbit_period`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_battery_energy` — Battery energy required

> How much energy must the battery hold to carry the load through eclipse?

| | |
|---|---|
| symbol | `E_batt` |
| type | `Energy` |
| unit | Wh |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `E = P_ecl*t_ecl/(DoD*eta_dis)` |
| source | `larson_wertz` |
| valid over | 0 … 27778 Wh |

- **lower bound** — battery energy cannot be negative
- **upper bound** — above 27 kWh the battery is heavier than the whole spacecraft
- **reads** — `pwr_demand`, `orbit_eclipse_fraction`, `orbit_period`, `pwr_dod`, `pwr_discharge_efficiency`
- **read by** — `pwr_battery_mass`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_battery_mass` — Battery mass

> What does the battery weigh?

| | |
|---|---|
| symbol | `m_batt` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `m = E/e_batt` |
| source | `larson_wertz` |
| valid over | 0 … 1000 kg |

- **lower bound** — a mass cannot be negative
- **upper bound** — above a tonne the battery is not part of this spacecraft
- **reads** — `pwr_battery_energy`, `pwr_battery_specific_energy`
- **read by** — `pwr_subsystem_mass`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_battery_specific_energy` — Battery specific energy

> How much energy does a kilogram of battery store?

| | |
|---|---|
| symbol | `e_batt` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `e_batt = 150` |
| source | `larson_wertz` |
| declared value | **150** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 50 … 350 - |

- **lower bound** — below 50 Wh/kg no cell considered is that poor
- **upper bound** — above 350 Wh/kg is beyond any space-qualified lithium-ion cell at this baseline
- **read by** — `pwr_battery_mass`

### `pwr_cell_derating` — Array temperature derating factor

> How much output does the array lose because it is hotter than its rating?

| | |
|---|---|
| symbol | `f_T` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `f_T = 1 + k_T*(T_eq - 298.15)` |
| source | `larson_wertz` |
| valid over | 0 … 1.5 - |

- **lower bound** — a derating factor cannot be negative
- **upper bound** — above 1.5 the array would be producing half again its rating, which the coefficient does not permit
- **reads** — `pwr_cell_temperature_coefficient`, `thm_equilibrium_temperature`
- **read by** — `pwr_array_power_eol`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

This node closes the loop. Without it power, thermal and propulsion are three independent chains; with it they are one system, and the resolver has to relax them together.

### `pwr_cell_efficiency` — Solar cell efficiency

> How efficiently does one cell convert sunlight?

| | |
|---|---|
| symbol | `eta_cell` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `eta_cell = 0.3` |
| source | `larson_wertz` |
| declared value | **0.3** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 0.4 - |

- **lower bound** — below 10% no cell technology considered for this programme performs that badly
- **upper bound** — above 40% is beyond any qualified space cell available at the time of this baseline
- **read by** — `pwr_array_power_bol`

### `pwr_cell_temperature_coefficient` — Cell power temperature coefficient

> How much output does the array lose per kelvin above its reference temperature?

| | |
|---|---|
| symbol | `k_T` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `k_T = -0.0025` |
| source | `larson_wertz` |
| declared value | **-0.0025** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | -0.01 … 0 - |

- **lower bound** — below -1% per kelvin no cell technology degrades that fast
- **upper bound** — a positive coefficient would mean a cell that improves when heated, which does not happen
- **read by** — `pwr_cell_derating`

### `pwr_degradation` — Array degradation factor

> How much of the array's beginning-of-life output survives to the end of the mission?

| | |
|---|---|
| symbol | `f_deg` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `f_deg = (1 - d_yr)^t` |
| source | `larson_wertz` |
| valid over | 0 … 1 - |

- **lower bound** — a degradation factor cannot be negative
- **upper bound** — an array cannot end life producing more than it began with
- **reads** — `pwr_degradation_rate`, `orbit_mission_duration`
- **read by** — `pwr_array_power_eol`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_degradation_rate` — Annual array degradation rate

> How much output does the array lose each year?

| | |
|---|---|
| symbol | `d_yr` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `d_yr = 0.025` |
| source | `larson_wertz` |
| declared value | **0.025** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.15 - |

- **lower bound** — a degradation rate cannot be negative
- **upper bound** — above 15% a year the array is gone within the mission and the design does not close
- **read by** — `pwr_degradation`

VLEO is below the inner belt, so the dominant mechanism is atomic-oxygen erosion of coverglass adhesive and thermal cycling, not displacement damage. This is the one place a VLEO power design differs from the textbook figure.

### `pwr_demand` — Total bus power demand

> How much power does the whole spacecraft need?

| | |
|---|---|
| symbol | `P_dem` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `P_dem = (P_prop + P_pay + P_av + P_com + P_th)*(1 + L_h)` |
| source | `larson_wertz` |
| valid over | 0 … 50000 W |

- **lower bound** — demand cannot be negative
- **upper bound** — above 50 kW nothing in this mass class supplies it
- **reads** — `prop_delivered_bus_power`, `pay_power`, `pwr_avionics_load`, `com_tx_power`, `pwr_thermal_load`, `pwr_harness_loss`
- **read by** — `pwr_battery_energy`, `pwr_margin`, `thm_dissipation`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Written as one function with named arguments rather than a chain of additions, so a forgotten term is a missing argument the compiler names.

### `pwr_discharge_efficiency` — Battery discharge path efficiency

> How efficiently does stored energy reach the loads?

| | |
|---|---|
| symbol | `eta_dis` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `eta_dis = 0.92` |
| source | `larson_wertz` |
| declared value | **0.92** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.6 … 0.99 - |

- **lower bound** — below 60% the discharge path dissipates more than the loads receive
- **upper bound** — above 99% has not been demonstrated end to end including the regulator
- **read by** — `pwr_available`, `pwr_battery_energy`

### `pwr_dod` — Battery depth of discharge

> How deeply is the battery discharged each eclipse?

| | |
|---|---|
| symbol | `DoD` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `DoD = 0.2` |
| source | `larson_wertz` |
| declared value | **0.2** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 0.8 - |

- **lower bound** — below 5% the battery is absurdly oversized
- **upper bound** — above 80% no lithium-ion cell survives 29000 cycles, which is what a five-year VLEO mission is
- **read by** — `pwr_battery_energy`

A lifetime decision rather than an energy one: 92-minute orbits over five years is roughly 29000 cycles.

### `pwr_harness_loss` — Harness and distribution loss

> What fraction is lost between the source and the loads?

| | |
|---|---|
| symbol | `L_h` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `L_h = 0.06` |
| source | `larson_wertz` |
| declared value | **0.06** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.25 - |

- **lower bound** — a loss fraction cannot be negative
- **upper bound** — above 25% the harness is the largest load on the bus
- **read by** — `prop_throttle`, `pwr_demand`

### `pwr_margin` — Power margin

> Does the power system supply what the spacecraft asks for?

| | |
|---|---|
| symbol | `M_pwr` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | power |
| evidence tier | A |
| relation | `M = (P_avail - P_dem)/P_dem` |
| source | `ecss_e_st_10_02` |
| valid over | -1 … 100 - |

- **lower bound** — a margin below -100% would mean the demand is more than twice the supply and the design is not a design
- **upper bound** — above 10000% the array is absurdly oversized
- **reads** — `pwr_available`, `pwr_demand`
- **read by** — `kpi_power_margin`
- **contributes to** — kpi_power_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

A margin silently corrected to zero is a design that drifted without anyone deciding to. This one is signed.

### `pwr_packing` — Array packing factor

> What fraction of the panel area is active cell?

| | |
|---|---|
| symbol | `f_pack` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `f_pack = 0.88` |
| source | `larson_wertz` |
| declared value | **0.88** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 0.98 - |

- **lower bound** — below 50% the panel is mostly structure
- **upper bound** — above 98% leaves no room for interconnects or the panel edge
- **read by** — `pwr_array_power_bol`

### `pwr_subsystem_mass` — Power subsystem mass

> What does the whole power subsystem weigh?

| | |
|---|---|
| symbol | `m_pwr` |
| type | `Mass` |
| unit | kg |
| kind | computed |
| owner | power |
| evidence tier | B |
| relation | `m = m_arr + m_batt + m_pcdu` |
| source | `larson_wertz` |
| valid over | 0 … 2000 kg |

- **lower bound** — a mass cannot be negative
- **upper bound** — above two tonnes the power system is not part of this spacecraft
- **reads** — `pwr_array_mass`, `pwr_battery_mass`
- **read by** — `mass_dry`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `pwr_thermal_load` — Thermal control load

> What do the heaters and thermal control electronics draw?

| | |
|---|---|
| symbol | `P_th` |
| type | `Power` |
| unit | W |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `P_th = 15` |
| source | `orbitt_case_c1` |
| declared value | **15** W |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 300 W |

- **lower bound** — a heater load cannot be negative
- **upper bound** — above 300 W the thermal design is heating rather than rejecting, which does not happen in this orbit
- **read by** — `prop_throttle`, `pwr_demand`


## `prop` — Propulsion · ICP plasma thruster

### `l3_prop_ach_01` — Specific impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_02` — Thruster duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_03` — Intake capture area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_04` — PPU efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_05` — Thrust vector alignment

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_06` — Propellant type

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_07` — Number of thrusters

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_08` — Throttle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_09` — Thrust authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_10` — Delta-v budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_11` — Propellant mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_12` — Decay rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_13` — Re-entry time unpowered

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_14` — Total impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_15` — Thrust to drag ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_16` — Minimum impulse bit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_17` — Tank volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_18` — Compression ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_ach_19` — Achievable lifetime

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_interface` — Propulsion · ICP plasma thruster — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_n_01` — Propulsion · ICP plasma thruster · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_02` — Propulsion · ICP plasma thruster · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_03` — Propulsion · ICP plasma thruster · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_04` — Propulsion · ICP plasma thruster · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_05` — Propulsion · ICP plasma thruster · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_06` — Propulsion · ICP plasma thruster · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_07` — Propulsion · ICP plasma thruster · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_08` — Propulsion · ICP plasma thruster · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_09` — Propulsion · ICP plasma thruster · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_10` — Propulsion · ICP plasma thruster · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_11` — Propulsion · ICP plasma thruster · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_12` — Propulsion · ICP plasma thruster · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_13` — Propulsion · ICP plasma thruster · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_14` — Propulsion · ICP plasma thruster · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_15` — Propulsion · ICP plasma thruster · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_16` — Propulsion · ICP plasma thruster · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_17` — Propulsion · ICP plasma thruster · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_18` — Propulsion · ICP plasma thruster · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_19` — Propulsion · ICP plasma thruster · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_20` — Propulsion · ICP plasma thruster · internal — to be named (20)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_21` — Propulsion · ICP plasma thruster · internal — to be named (21)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_22` — Propulsion · ICP plasma thruster · internal — to be named (22)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_23` — Propulsion · ICP plasma thruster · internal — to be named (23)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_24` — Propulsion · ICP plasma thruster · internal — to be named (24)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_25` — Propulsion · ICP plasma thruster · internal — to be named (25)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_26` — Propulsion · ICP plasma thruster · internal — to be named (26)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_27` — Propulsion · ICP plasma thruster · internal — to be named (27)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_n_28` — Propulsion · ICP plasma thruster · internal — to be named (28)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_prop_req_01` — Specific impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_02` — Thruster duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_03` — Intake capture area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_04` — PPU efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_05` — Thrust vector alignment

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_06` — Propellant type

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_07` — Number of thrusters

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_08` — Throttle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_09` — Thrust authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_10` — Delta-v budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_11` — Propellant mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_12` — Decay rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_13` — Re-entry time unpowered

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_14` — Total impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_15` — Thrust to drag ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_16` — Minimum impulse bit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_17` — Tank volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_18` — Compression ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_prop_req_19` — Achievable lifetime

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_beam_voltage` — Beam accelerating voltage

> Through what potential is the ion beam accelerated?

| | |
|---|---|
| symbol | `V_b` |
| type | `Voltage` |
| unit | V |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `V_b = 300` |
| source | `romano2021` |
| declared value | **300** V |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 50 … 2000 V |

- **lower bound** — below 50 V the exhaust velocity is too low to produce useful thrust from this mass flow
- **upper bound** — above 2000 V grid erosion by atomic oxygen becomes the life-limiting mechanism, and the design has no demonstrated basis
- **read by** — `prop_exhaust_velocity`

### `prop_beta_backflow` — Duct back-flow transmission

> What fraction of thermal molecules in the chamber find their way back out of the mouth?

| | |
|---|---|
| symbol | `beta` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `beta = 0.06` |
| source | `romano2021` |
| declared value | **0.06** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.001 … 0.5 - |

- **lower bound** — below 0.001 no manufacturable duct is that good at trapping thermal molecules
- **upper bound** — above 0.5 the duct traps nothing and the compression ratio collapses to one
- **read by** — `prop_capture_efficiency`, `prop_compression_ratio`

This single number is what an intake design is for. A long, narrow, honeycombed duct makes it small; that is the whole device.

### `prop_bus_power` — Propulsion demand on the bus

> How much power does the propulsion system take from the bus?

| | |
|---|---|
| symbol | `P_prop` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `P_prop = P_in/eta_ppu` |
| source | `larson_wertz` |
| valid over | 0 … 100000 W |

- **lower bound** — bus demand cannot be negative
- **upper bound** — above 100 kW no bus in this mass class supplies it
- **reads** — `prop_input_power`, `prop_ppu_efficiency`
- **read by** — `prop_delivered_bus_power`, `prop_throttle`, `prop_total_efficiency`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The largest single load on the bus, and the reason the power chapter is where an air-breathing design usually fails.

### `prop_capture_efficiency` — Intake collection efficiency

> What fraction of the flow entering the mouth actually reaches the thruster?

| | |
|---|---|
| symbol | `eta_c` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `eta_c = A_out*eta_geo/(A_out + beta*A_in)` |
| source | `romano2021` |
| valid over | 0 … 1 - |

- **lower bound** — a collection efficiency cannot be negative
- **upper bound** — an intake cannot deliver more than enters its mouth
- **reads** — `prop_intake_area`, `prop_throat_area`, `prop_eta_geo`, `prop_beta_backflow`, `env_chamber_temperature`, `env_mean_molar_mass`, `env_number_density`, `orbit_velocity`
- **read by** — `prop_collected_flow`
- **contributes to** — kpi_thrust_margin
- **assumes** Free-molecular flow throughout the duct and the chamber — fails when if the chamber compresses far enough to become collisional the balance is no longer a simple flux balance, which happens above a compression ratio of roughly 1e4
- **assumes** The chamber gas is fully thermalised at the chamber temperature — fails when a short duct passes part of the beam straight through, which raises the delivered flow above this and is not modelled
- **evidence** IRS-class baseline intake — expect 0.40909 ± 0.0001 relative, from `romano2021` (independent-derivation)

Read the relation slowly: the collection efficiency does not depend on the flight speed at all, only on geometry. Speed buys compression, not capture. A design trying to raise capture by flying faster is optimising the wrong term.

### `prop_chamber_density` — Collection chamber number density

> How dense is the gas the thruster is asked to ionise?

| | |
|---|---|
| symbol | `n_c` |
| type | `NumberDensity` |
| unit | 1/m^3 |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `n_c = CR*n` |
| source | `romano2021` |
| valid over | 1000000000000 … 10000000000000000000000 1/m^3 |

- **lower bound** — below 1e12 per cubic metre no inductively coupled source will strike a discharge
- **upper bound** — above 1e22 the gas is at atmospheric pressure, which no intake in this band can produce
- **reads** — `env_number_density`, `prop_compression_ratio`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

This is the number that decides whether a plasma can be struck at all, and it is why compression matters even though it does not appear in the capture efficiency.

### `prop_collected_flow` — Collected mass flow

> How much mass per second reaches the thruster?

| | |
|---|---|
| symbol | `mdot` |
| type | `MassFlow` |
| unit | mg/s |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `mdot = rho*V*A_in*eta_c` |
| source | `romano2021` |
| valid over | 0 … 10 mg/s |

- **lower bound** — a mass flow cannot be negative
- **upper bound** — above 10 mg/s no intake in this band collects, at any mouth size the vehicle can carry
- **reads** — `env_mass_density`, `orbit_velocity`, `prop_intake_area`, `prop_capture_efficiency`
- **read by** — `prop_ion_flow`, `prop_specific_impulse`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_compression_ratio` — Intake compression ratio

> By how much does the intake concentrate the gas before it reaches the thruster?

| | |
|---|---|
| symbol | `CR` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `CR = 4*V*A_in*eta_geo/(v_bar*(A_out + beta*A_in))` |
| source | `romano2021` |
| valid over | 1 … 100000 - |

- **lower bound** — an intake cannot dilute the flow; a compression ratio below one means the geometry is inverted
- **upper bound** — above 1e5 the chamber gas becomes collisional and the free-molecular balance no longer holds
- **reads** — `prop_intake_area`, `prop_throat_area`, `prop_eta_geo`, `prop_beta_backflow`, `env_chamber_temperature`, `env_mean_molar_mass`, `env_number_density`, `orbit_velocity`
- **read by** — `prop_chamber_density`
- **evidence** IRS-class baseline intake — expect 308.09 ± 0.001 relative, from `romano2021` (independent-derivation)

### `prop_delivered_bus_power` — Propulsion power actually drawn

> How much power does the thruster actually take, after throttling?

| | |
|---|---|
| symbol | `P_del` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `P = k*P_prop` |
| source | `orbitt_case_c1` |
| valid over | 0 … 100000 W |

- **lower bound** — power drawn cannot be negative
- **upper bound** — above 100 kW no bus in this mass class supplies it
- **reads** — `prop_bus_power`, `prop_throttle`
- **read by** — `pwr_demand`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_delivered_thrust` — Thrust actually delivered

> How much thrust is produced once the power available is taken into account?

| | |
|---|---|
| symbol | `T_del` |
| type | `Force` |
| unit | mN |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `T = k*T` |
| source | `orbitt_case_c1` |
| valid over | 0 … 1000 mN |

- **lower bound** — thrust cannot be negative
- **upper bound** — above 1 N no air-breathing thruster in this band produces thrust
- **reads** — `prop_thrust`, `prop_throttle`
- **read by** — `prop_thrust_to_drag`
- **contributes to** — kpi_thrust_margin
- **assumes** Thrust scales linearly with delivered power at fixed specific impulse — fails when a real thruster's efficiency falls away from its design point, so a deeply throttled system delivers less than this; the relation is optimistic below about half throttle
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_div_efficiency` — Beam divergence efficiency

> How much thrust is lost because the beam is not perfectly axial?

| | |
|---|---|
| symbol | `alpha_div` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `alpha_div = 0.97` |
| source | `romano2021` |
| declared value | **0.97** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.7 … 1 - |

- **lower bound** — below 0.7 the beam is a cloud rather than a beam
- **upper bound** — unity is a perfectly collimated beam, which does not exist
- **read by** — `prop_thrust`

### `prop_double_ion_factor` — Doubly-charged ion correction

> How much thrust is lost to doubly-charged ions carrying charge without proportional mass?

| | |
|---|---|
| symbol | `alpha_pp` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `alpha_pp = 0.97` |
| source | `romano2021` |
| declared value | **0.97** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.8 … 1 - |

- **lower bound** — below 0.8 the plasma is dominated by multiply-charged species and the single-charge model does not apply
- **upper bound** — unity means no doubly-charged ions at all, which no real source achieves
- **read by** — `prop_thrust`

### `prop_eta_geo` — Intake geometric transmission

> What fraction of the mouth is open duct, and what fraction of a hyperthermal beam does that duct pass?

| | |
|---|---|
| symbol | `eta_geo` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `eta_geo = 0.9` |
| source | `romano2021` |
| declared value | **0.9** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 1 - |

- **lower bound** — below 10% the duct blocks more than it passes and is not an intake
- **upper bound** — unity means a perfectly open mouth with no structure, which is not manufacturable
- **read by** — `prop_capture_efficiency`, `prop_compression_ratio`

### `prop_exhaust_velocity` — Beam exhaust velocity

> How fast does the accelerated beam leave the thruster?

| | |
|---|---|
| symbol | `v_e` |
| type | `Velocity` |
| unit | m/s |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `v_e = sqrt(2*q*V_b/m_i)` |
| source | `romano2021` |
| valid over | 1000 … 500000 m/s |

- **lower bound** — below 1 km/s the exhaust is slower than a cold gas thruster and the concept has no advantage
- **upper bound** — above 500 km/s the relativistic and space-charge assumptions in the relation break down
- **reads** — `prop_beam_voltage`, `env_mean_molar_mass`
- **read by** — `prop_thrust`
- **evidence** 300 V, atomic oxygen mixture — expect 55610.058386 ± 0.000000001 relative, from `codata2018` (independent-derivation)

Air is lighter than xenon, so the same voltage buys a far higher exhaust velocity. It is the one respect in which air-breathing propulsion is easier rather than harder.

### `prop_incident_flux` — Incident mass flux

> How much mass arrives on a square metre facing the flow each second?

| | |
|---|---|
| symbol | `phi` |
| type | `MassFlux` |
| unit | kg/s |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `phi = rho*V` |
| source | `romano2021` |
| valid over | 0 … 1 kg/s |

- **lower bound** — an incident flux cannot be negative
- **upper bound** — above 1 kg/m2/s the flow is continuum and the intake model does not apply
- **reads** — `env_mass_density`, `orbit_velocity`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The ceiling on everything downstream. At 200 km it is about 2 milligrams per square metre per second.

### `prop_input_power` — Thruster input power

> How much electrical power does the thruster itself draw?

| | |
|---|---|
| symbol | `P_in` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `P_in = (P_jet + P_ion)/(1 - L_other)` |
| source | `romano2021` |
| valid over | 0 … 100000 W |

- **lower bound** — input power cannot be negative
- **upper bound** — above 100 kW no bus in this mass class supplies it
- **reads** — `prop_jet_power`, `prop_ionisation_power`, `prop_other_losses`
- **read by** — `prop_bus_power`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_intake_area` — Intake mouth area

> How large is the opening the intake presents to the flow?

| | |
|---|---|
| symbol | `A_in` |
| type | `Area` |
| unit | m^2 |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `A_in = 0.2` |
| source | `romano2021` |
| declared value | **0.2** m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.01 … 5 m^2 |

- **lower bound** — below 100 cm2 the collected flow is below the ionisation threshold of any thruster considered
- **upper bound** — above 5 m2 the mouth is itself more drag than the thrust it can produce, at every altitude in the band
- **read by** — `prop_capture_efficiency`, `prop_collected_flow`, `prop_compression_ratio`
- **contributes to** — kpi_thrust_margin

Both the numerator and part of the denominator of the closure. A bigger mouth collects more and drags more, which is why there is an optimum rather than a maximum.

### `prop_ion_flow` — Ionised mass flow

> How much of the collected mass is actually accelerable?

| | |
|---|---|
| symbol | `mdot_i` |
| type | `MassFlow` |
| unit | mg/s |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `mdot_i = eta_u*mdot` |
| source | `romano2021` |
| valid over | 0 … 10 mg/s |

- **lower bound** — an ion mass flow cannot be negative
- **upper bound** — cannot exceed the collected flow it comes from
- **reads** — `prop_collected_flow`, `prop_utilisation`
- **read by** — `prop_ionisation_power`, `prop_jet_power`, `prop_thrust`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_ionisation_cost` — Energy cost per ion

> How much energy does the source spend to make one ion, including every loss?

| | |
|---|---|
| symbol | `eps_i` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `eps_i = 250` |
| source | `romano2021` |
| declared value | **250** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 20 … 1000 - |

- **lower bound** — below 20 eV per ion is under twice the 13.6 eV ionisation potential of atomic oxygen; no source has approached it
- **upper bound** — above 1000 eV per ion the ionisation power alone exceeds any plausible bus, whatever the beam does
- **read by** — `prop_ionisation_power`

Not the ionisation potential. A real source spends 2 to 30 times the theoretical minimum on excitation, radiation and wall losses.

### `prop_ionisation_power` — Ionisation power

> How much power does making the ions cost, before any of them is accelerated?

| | |
|---|---|
| symbol | `P_ion` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `P_ion = (mdot_i/m_i)*eps_i*q` |
| source | `romano2021` |
| valid over | 0 … 100000 W |

- **lower bound** — ionisation power cannot be negative
- **upper bound** — above 100 kW no bus in this mass class supplies it
- **reads** — `prop_ion_flow`, `env_mean_molar_mass`, `prop_ionisation_cost`
- **read by** — `prop_input_power`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_jet_power` — Jet power

> How much power ends up in the beam?

| | |
|---|---|
| symbol | `P_jet` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `P_jet = T^2/(2*mdot_i)` |
| source | `romano2021` |
| valid over | 0 … 100000 W |

- **lower bound** — jet power cannot be negative
- **upper bound** — above 100 kW no bus in this mass class supplies it
- **reads** — `prop_thrust`, `prop_ion_flow`
- **read by** — `prop_input_power`, `prop_total_efficiency`, `thm_dissipation`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_other_losses` — Other thruster losses

> What fraction of the thruster's input power goes to losses other than ionisation and the beam?

| | |
|---|---|
| symbol | `L_other` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `L_other = 0.15` |
| source | `romano2021` |
| declared value | **0.15** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 0.6 - |

- **lower bound** — a loss fraction cannot be negative
- **upper bound** — above 0.6 the thruster is a heater with a beam attached
- **read by** — `prop_input_power`

### `prop_ppu_efficiency` — Power processing unit efficiency

> How efficiently does the power processing unit convert bus power into thruster power?

| | |
|---|---|
| symbol | `eta_ppu` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | power |
| evidence tier | A |
| relation | `eta_ppu = 0.9` |
| source | `larson_wertz` |
| declared value | **0.9** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.5 … 0.98 - |

- **lower bound** — below 50% the power processing unit dissipates more than the thruster uses and the thermal design does not close
- **upper bound** — above 98% no flown power processing unit has been demonstrated
- **read by** — `prop_bus_power`

### `prop_specific_impulse` — Specific impulse

> What is the specific impulse, referred honestly to the collected flow rather than the ionised part?

| | |
|---|---|
| symbol | `Isp` |
| type | `Time` |
| unit | s |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `Isp = T/(mdot*g0)` |
| source | `romano2021` |
| valid over | 100 … 50000 s |

- **lower bound** — below 100 s an air-breathing system offers nothing a cold gas thruster does not
- **upper bound** — above 50000 s the relation is being fed an ion flow that is not physical
- **reads** — `prop_thrust`, `prop_collected_flow`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Referring this to the ion flow alone flatters the number by the reciprocal of the utilisation, which is how brochure figures are made.

### `prop_throat_area` — Intake throat area

> How large is the opening from the collection chamber into the thruster?

| | |
|---|---|
| symbol | `A_out` |
| type | `Area` |
| unit | m^2 |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `A_out = 0.01` |
| source | `romano2021` |
| declared value | **0.01** m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.0001 … 1 m^2 |

- **lower bound** — below 1 cm2 the throat chokes the flow to the thruster whatever the mouth collects
- **upper bound** — the throat cannot be larger than the mouth in any useful intake
- **read by** — `prop_capture_efficiency`, `prop_compression_ratio`

### `prop_throttle` — Propulsion throttle

> How much of what the thruster asks for can the bus actually give it?

| | |
|---|---|
| symbol | `k_thr` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `k = clamp((P_avail - P_other)/P_prop, 0, 1)` |
| source | `orbitt_case_c1` |
| valid over | 0 … 1 - |

- **lower bound** — a throttle cannot be negative
- **upper bound** — the thruster cannot be given more than it asked for
- **reads** — `pwr_available`, `prop_bus_power`, `pay_power`, `pwr_avionics_load`, `com_tx_power`, `pwr_thermal_load`, `pwr_harness_loss`
- **read by** — `prop_delivered_bus_power`, `prop_delivered_thrust`
- **contributes to** — kpi_thrust_margin, kpi_power_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The node that makes the power budget bite on the physics rather than being reported beside it.

### `prop_thrust` — Thrust

> How much thrust does the thruster produce from the air it is given?

| | |
|---|---|
| symbol | `T` |
| type | `Force` |
| unit | mN |
| kind | computed |
| owner | propulsion |
| evidence tier | B |
| relation | `T = mdot_i*v_e*alpha_div*alpha_pp` |
| source | `romano2021` |
| valid over | 0 … 1000 mN |

- **lower bound** — thrust cannot be negative
- **upper bound** — above 1 N no air-breathing thruster in this band produces thrust, at any intake size
- **reads** — `prop_ion_flow`, `prop_exhaust_velocity`, `prop_div_efficiency`, `prop_double_ion_factor`
- **read by** — `prop_delivered_thrust`, `prop_jet_power`, `prop_specific_impulse`
- **contributes to** — kpi_thrust_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_thrust_to_drag` — Thrust to drag ratio

> Does the thrust the air produces exceed the drag the air imposes? This is the closure the whole design exists to reach.

| | |
|---|---|
| symbol | `T_D` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | systems |
| evidence tier | B |
| relation | `T/D` |
| source | `romano2021` |
| valid over | 0 … 100 - |

- **lower bound** — a negative ratio means a sign error in thrust or drag
- **upper bound** — above 100 the vehicle is a launch stage, not a satellite
- **reads** — `prop_delivered_thrust`, `aero_drag_force`
- **read by** — `cost_replacement_avoided`, `kpi_thrust_margin`
- **contributes to** — kpi_thrust_margin, kpi_service_lifetime
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

At or above one the orbit holds indefinitely. Below one the mission has a lifetime rather than an altitude, and every other number in the design is a detail.

### `prop_total_efficiency` — Total propulsion efficiency

> What fraction of the power drawn from the bus ends up in the beam?

| | |
|---|---|
| symbol | `eta_T` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | propulsion |
| evidence tier | A |
| relation | `eta_T = P_jet/P_prop` |
| source | `romano2021` |
| valid over | 0 … 1 - |

- **lower bound** — an efficiency cannot be negative
- **upper bound** — an efficiency above one is a violation of energy conservation and a sign of an error upstream
- **reads** — `prop_jet_power`, `prop_bus_power`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `prop_utilisation` — Propellant utilisation efficiency

> What fraction of the collected mass is actually ionised and accelerated?

| | |
|---|---|
| symbol | `eta_u` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | propulsion |
| evidence tier | A |
| relation | `eta_u = 0.4` |
| source | `romano2021` |
| declared value | **0.4** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 0.95 - |

- **lower bound** — below 5% the thruster ionises almost nothing and the concept does not work
- **upper bound** — above 95% no inductively coupled source has demonstrated utilisation this high on atomic oxygen
- **read by** — `prop_ion_flow`
- **contributes to** — kpi_thrust_margin


## `solar` — Solar weather — addition

### `l3_solar_ach_01` — Solar flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_ach_02` — F10.7

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_ach_03` — Ap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_interface` — Solar weather — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_req_01` — Solar flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_req_02` — F10.7

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_solar_req_03` — Ap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sw_activity_band` — Flux activity band

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_ap_design` — Ap design value

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_band_coverage` — Stated band coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_central_expectation` — Central expectation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_cycle_number` — Solar cycle number

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_cycle_phase` — Solar cycle phase

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_cycle_repeatability` — Cycle repeatability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_event_duration` — Burst duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_f107_81day` — F10.7 81-day centred mean

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_f107_design` — F10.7 design value

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_forecast_bias` — Forecast bias

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_forecast_skill` — Forecast skill

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_horizon_climatology` — Horizon against climatology

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_horizon_persistence` — Horizon against persistence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_kp_from_ap` — Kp from daily Ap

> What Kp does this daily planetary Ap mean?

| | |
|---|---|
| symbol | `Kp` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `Kp = piecewise_linear(ap_28 -> kp_28, clamp(Ap, 0, 400))` |
| source | `iaga_kp_ap` |
| valid over | 0 … 9 - |

- **lower bound** — Kp is defined on 0..9 and the table's first point is Kp 0 at ap 0; a negative index is a sign error, not a quiet day
- **upper bound** — Kp is defined on 0..9 and the table's last point is Kp 9 at ap 400. This is the same guard env_kp carries, and it catches an ap value reaching a consumer that wanted Kp
- **reads** — `sw_storm_return_level`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **assumes** The table is defined for the three-hourly ap and is being fed a daily mean Ap — fails when Kp(ap) is concave, so by Jensen's inequality the table run on a daily mean returns a Kp above the mean of the eight three-hourly Kp and well below the daily peak. Measured on the solar-weather record over 10,297 days with both an Ap and all eight Kp: against the 24-hour mean the table reads high by 0.083 Kp (median), against the daily peak it reads low by 1.000 Kp; on disturbed days (Ap >= 48, 131 of them) those become 0.397 high and 1.606 low. Both signs are what the concavity argument predicts. A design sized on the peak slot through this node alone is sized on a sky 1.6 Kp quieter than the record's, and that is on exactly the days a drag design is sized by. sw_kp_slot_bias measures and publishes both offsets; until it is written this node's answer carries them uncorrected.
- **assumes** Straight lines between the 28 tabulated points — fails when the published scale is a discrete table, so every value strictly between two anchors is this node's choice and not the source's. ap grows roughly geometrically with Kp, so interpolating linearly in ap rather than in its logarithm understates Kp inside a bin; the worst departure between the two over the whole domain is 0.017 Kp, in the 2-to-3 bin. That is the size of the arbitrariness, and it is smaller than the slot bias above by two orders of magnitude.
- **assumes** Clipped to the table's ends: below ap 0 and above ap 400 the answer is 0 and 9 — fails when above ap 400 every storm returns exactly 9, so the largest storm on record and a merely severe one become the same number and any relation reading Kp stops responding. The largest daily Ap in the solar-weather record is 273, so nothing in this record reaches the clip — but a scenario multiplier applied to a disturbed day can, and it will do so silently.
- **evidence** Kp 0 — ap 0, the table's first point — expect 0 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 1 — ap 4 — expect 1 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 2 — ap 7 — expect 2 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 3 — ap 15, the legacy study's own climatology constant, and what env_kp declares by hand — expect 3 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 4 — ap 27 — expect 4 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 5 — ap 48, the storm threshold — expect 5 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 6 — ap 80 — expect 6 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 7 — ap 132 — expect 7 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 8 — ap 207 — expect 8 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** Kp 9 — ap 400, the table's last point — expect 9 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)
- **evidence** above the table — ap 1000 holds at Kp 9 rather than extrapolating — expect 9 ± 0.000000000001 relative, from `iaga_kp_ap` (published-source)

The atmosphere model wants Kp; the design product carries Ap. This is that conversion and only that — the published table, applied as published. What flows in is the design storm from sw_storm_return_level, so what flows out is the Kp of that storm and not of an average day. The bias the table carries when a daily mean is fed to a three-hourly scale is measured separately, in sw_kp_slot_bias, and is not corrected here.

### `sw_kp_slot_bias` — Kp slot bias

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_mean_cycle_level` — Mean-cycle F10.7 level

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_recurrence_lag` — Rotation recurrence lag

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_recurrence_strength` — Rotation recurrence strength

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_regime` — Geomagnetic regime

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_semiannual_amplitude` — Semiannual amplitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_spike_threshold` — Spike threshold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_storm_rate` — Storm rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sw_storm_return_level` — Ap at a storm return period

> What daily planetary Ap recurs once per mission lifetime?

| | |
|---|---|
| symbol | `Ap_T` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | environment |
| evidence tier | A |
| relation | `Ap(T) = 92.5155 + 40.9265*ln(T / 1 yr)` |
| source | `noaa_swpc` |
| valid over | 20 … 400 - |

- **lower bound** — ap is an equivalent amplitude in nanotesla. Below 20 the answer is not a storm at all — the record's median day is 7 — so a return level under it means the input or the fit reached somewhere neither was meant to go
- **upper bound** — 400 is the last point of the published ap/Kp table, above which Kp is no longer distinguished, and the largest daily Ap in 28.2 years of record is 273. A return level above 400 is the fit extrapolating past everything that supports it
- **reads** — `orbit_mission_duration`
- **read by** — `sw_kp_from_ap`
- **assumes** The tail is log-linear in the return period, with the two coefficients fitted on this record — fails when the form is a choice, not the source's. Fitted over ranks 2 to 56 of the record — exactly the ranks the declared input range 0.5 to 15 years reaches — it carries a residual rms of 4.95 Ap against the empirical curve, worst +8.6 and -12.9. A power law on the same points is more than twice as bad (rms 9.10, worst -47.7), which is why this form and not that one. Anyone who needs the empirical step rather than a smooth curve should read the record, not this row.
- **assumes** 28.2 years of record support the whole curve, and its top end rests on two observations — fails when the empirical method cannot see past its own record length. At T = 15 years the answer is fitted through the second-largest daily Ap in 28.2 years, and at T = 10 years the third; the record's largest value, Ap 273, has an apparent return period of exactly 28.2 years for no reason other than that it is the largest thing in 28.2 years. So the fit is an interpolation of the tail and never an extrapolation past it, and a mission at the 15-year bound is being sized on a curve whose top is two data points. A design that needs the once-per-century storm needs a longer record or a fitted extreme-value model, not this row.
- **assumes** Every day in the record is treated as an independent draw — fails when storms cluster — a coronal hole returns once per solar rotation and a single event runs for more than one day — so the record holds fewer independent storms than it holds storm days. Clustering does not bias the exceedance level itself, which is a quantile of the marginal distribution, but it does mean the effective sample behind the tail is smaller than N and the uncertainty on the answer is wider than the residual above suggests. sw_event_duration and sw_recurrence_lag measure the clustering; neither is written yet.
- **evidence** half a year — rank 56 of the record, the shortest mission the tree allows — expect 66 ± 2 relative, from `noaa_swpc` (independent-derivation)
- **evidence** one year — rank 28, one storm season — expect 96 ± 3.6 relative, from `noaa_swpc` (independent-derivation)
- **evidence** two years — rank 14 — expect 118 ± 3 relative, from `noaa_swpc` (independent-derivation)
- **evidence** three years — rank 9, and the fit's worst point: ties at ranks 9 and 10 that the curve smooths through — expect 127 ± 10.6 relative, from `noaa_swpc` (independent-derivation)
- **evidence** five years — rank 6, the mission orbit_mission_duration currently declares — expect 162 ± 3.7 relative, from `noaa_swpc` (independent-derivation)
- **evidence** ten years — rank 3, near the top of what 28.2 years of record can say anything about — expect 189 ± 2.3 relative, from `noaa_swpc` (independent-derivation)

Geomagnetic activity has no usable long-term forecast, so a design does not predict the storm — it sizes for the worst one the mission is likely to meet. That makes the mission length the input: a five-year mission and a fifteen-year one are owed different skies, and this row moves when orbit_mission_duration moves.

### `sw_uncertainty_growth` — Uncertainty growth with lead

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.


## `struct` — Structure

### `l3_struct_ach_01` — Bus dimensions

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_ach_02` — Primary structure material

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_ach_03` — Panel thickness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_ach_04` — Accommodation volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_interface` — Structure — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_n_01` — Structure · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_02` — Structure · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_03` — Structure · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_04` — Structure · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_05` — Structure · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_06` — Structure · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_07` — Structure · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_08` — Structure · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_09` — Structure · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_10` — Structure · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_11` — Structure · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_12` — Structure · internal — to be named (12)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_13` — Structure · internal — to be named (13)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_14` — Structure · internal — to be named (14)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_15` — Structure · internal — to be named (15)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_16` — Structure · internal — to be named (16)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_17` — Structure · internal — to be named (17)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_18` — Structure · internal — to be named (18)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_19` — Structure · internal — to be named (19)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_20` — Structure · internal — to be named (20)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_21` — Structure · internal — to be named (21)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_22` — Structure · internal — to be named (22)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_23` — Structure · internal — to be named (23)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_24` — Structure · internal — to be named (24)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_25` — Structure · internal — to be named (25)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_26` — Structure · internal — to be named (26)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_27` — Structure · internal — to be named (27)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_28` — Structure · internal — to be named (28)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_29` — Structure · internal — to be named (29)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_30` — Structure · internal — to be named (30)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_31` — Structure · internal — to be named (31)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_32` — Structure · internal — to be named (32)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_33` — Structure · internal — to be named (33)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_34` — Structure · internal — to be named (34)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_35` — Structure · internal — to be named (35)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_36` — Structure · internal — to be named (36)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_37` — Structure · internal — to be named (37)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_38` — Structure · internal — to be named (38)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_n_39` — Structure · internal — to be named (39)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_struct_req_01` — Bus dimensions

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_req_02` — Primary structure material

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_req_03` — Panel thickness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_struct_req_04` — Accommodation volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `sys` — Attitude control sizing

### `sys_attitude_control_sizing_aerodynamic_trim_angle` — Aerodynamic trim angle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_attitude_deadband` — Attitude deadband

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_attitude_control_sizing_control_bandwidth` — Control bandwidth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_control_torque_authority` — Control torque authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_attitude_control_sizing_magnetic_dipole_authority`, `sys_attitude_hardware_earth_magnetic_field`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_magnetic_dipole_authority` — Magnetic dipole authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_control_sizing_control_torque_authority`

### `sys_attitude_control_sizing_momentum_dumping_frequency` — Momentum dumping frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_momentum_storage_capacity` — Momentum storage capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_attitude_hardware_number_of_reaction_wheels`, `sys_attitude_hardware_wheel_speed_limit`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_settling_time` — Settling time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_control_sizing_slew_rate_capability` — Slew rate capability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_eo_isr_swath, sys_kpi_achieved_eo_isr_collection_capacity

### `sys_attitude_hardware_aerodynamic_torque` — Aerodynamic torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_drag_acceleration`, `sys_mass_and_aero_centre_of_pressure_offset`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_hardware_earth_magnetic_field` — Earth magnetic field

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_control_sizing_control_torque_authority`

### `sys_attitude_hardware_first_mode_frequency` — First mode frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_hardware_gravity_gradient_torque` — Gravity gradient torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_moments_of_inertia`, `sys_orbit_geometry_altitude`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_hardware_gyro_drift` — Gyro drift

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_attitude_hardware_number_of_reaction_wheels` — Number of reaction wheels

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_control_sizing_momentum_storage_capacity`

### `sys_attitude_hardware_solar_radiation_torque` — Solar radiation torque

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_attitude_hardware_star_tracker_accuracy` — Star tracker accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_attitude_hardware_wheel_speed_limit` — Wheel speed limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_control_sizing_momentum_storage_capacity`

### `sys_avionics_and_software_clock_distribution_accuracy` — Clock distribution accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_avionics_and_software_memory_size` — Memory size

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_avionics_and_software_processor_throughput` — Processor throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_comms_service_antenna_beamwidth` — Antenna beamwidth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_comms_service_beam_footprint_diameter`
- **contributes to** — sys_kpi_achieved_comms_throughput, sys_kpi_achieved_comms_devices_per_beam, sys_kpi_achieved_eo_isr_min_detectable_eirp
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_beam_footprint_diameter` — Beam footprint diameter

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_comms_service_antenna_beamwidth`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_devices_per_beam` — Devices per beam

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_doppler_shift` — Doppler shift

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_comms_service_service_carrier_frequency`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_handover_rate` — Handover rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_number_of_beams` — Number of beams

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_throughput, sys_kpi_achieved_comms_devices_per_beam

### `sys_comms_service_service_carrier_frequency` — Service carrier frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_comms_service_doppler_shift`
- **contributes to** — sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin, sys_kpi_achieved_eo_isr_min_detectable_eirp, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90

### `sys_comms_service_service_link_margin` — Service link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_comms_service_service_transmit_eirp`, `sys_comms_service_service_receive_g_t`, `sys_comms_service_slant_range`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_service_receive_g_t` — Service receive G/T

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_comms_service_service_link_margin`
- **contributes to** — sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin

### `sys_comms_service_service_transmit_eirp` — Service transmit EIRP

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_comms_service_service_link_margin`
- **contributes to** — sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin

### `sys_comms_service_slant_range` — Slant range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_orbit_geometry_minimum_elevation`
- **read by** — `sys_comms_service_service_link_margin`, `sys_tt_and_c_and_downlink_tt_and_c_link_margin`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_comms_service_user_throughput` — User throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_access_gap_achieved` — Access gap achieved

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_collision_probability` — Collision probability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_constellation_size` — Constellation size

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_coverage, sys_kpi_achieved_eo_isr_coverage, sys_kpi_achieved_pnt_coverage, sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_comms_conjunction_rate, sys_kpi_achieved_eo_isr_conjunction_rate, sys_kpi_achieved_pnt_conjunction_rate, sys_kpi_achieved_pnt_gdop
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_coverage_achieved` — Coverage achieved

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_instantaneous_fold` — Instantaneous fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_inter_satellite_range` — Inter-satellite range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_rf_geolocation_service_cluster_baseline`
- **contributes to** — sys_kpi_achieved_comms_latency, sys_kpi_achieved_pnt_latency, sys_kpi_achieved_comms_time_transfer, sys_kpi_achieved_pnt_time_transfer, sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_number_of_planes` — Number of planes

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_pnt_service_geometric_dilution`
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_coverage, sys_kpi_achieved_eo_isr_coverage, sys_kpi_achieved_pnt_coverage, sys_kpi_achieved_comms_access_gap, sys_kpi_achieved_pnt_access_gap, sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time, sys_kpi_achieved_comms_conjunction_rate, sys_kpi_achieved_eo_isr_conjunction_rate, sys_kpi_achieved_pnt_conjunction_rate, sys_kpi_achieved_comms_handover_rate, sys_kpi_achieved_pnt_gdop, sys_kpi_achieved_pnt_fix_availability, sys_kpi_achieved_pnt_time_to_first_fix, sys_kpi_achieved_pnt_integrity_risk
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_phasing_factor` — Phasing factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_conjunction_rate, sys_kpi_achieved_eo_isr_conjunction_rate, sys_kpi_achieved_pnt_conjunction_rate, sys_kpi_achieved_comms_handover_rate, sys_kpi_achieved_pnt_gdop
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_raan_spread` — RAAN spread

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_coverage, sys_kpi_achieved_eo_isr_coverage, sys_kpi_achieved_pnt_coverage, sys_kpi_achieved_comms_latency, sys_kpi_achieved_pnt_latency
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_revisit_achieved` — Revisit achieved

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_satellites_per_plane` — Satellites per plane

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_pnt_service_geometric_dilution`
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_access_gap, sys_kpi_achieved_pnt_access_gap, sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time, sys_kpi_achieved_comms_conjunction_rate, sys_kpi_achieved_eo_isr_conjunction_rate, sys_kpi_achieved_pnt_conjunction_rate, sys_kpi_achieved_comms_latency, sys_kpi_achieved_pnt_latency, sys_kpi_achieved_pnt_gdop, sys_kpi_achieved_pnt_fix_availability, sys_kpi_achieved_pnt_time_to_first_fix
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_constellation_trade_output_walker_pattern_type` — Walker pattern type

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_eo_optical_service_aperture_diameter` — Aperture diameter

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_rf_geolocation_service_dwell_time`
- **contributes to** — sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_image_snr, sys_kpi_achieved_eo_isr_dwell_time

### `sys_eo_optical_service_detector_ifov` — Detector IFOV

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_eo_optical_service_ground_sample_distance`, `sys_eo_optical_service_swath_width`, `sys_orbit_geometry_off_nadir_angle`
- **contributes to** — sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_swath, sys_kpi_achieved_eo_isr_image_snr, sys_kpi_achieved_eo_isr_smear

### `sys_eo_optical_service_ground_sample_distance` — Ground sample distance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_eo_optical_service_detector_ifov`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_eo_optical_service_integration_time` — Integration time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_eo_optical_service_motion_smear`
- **contributes to** — sys_kpi_achieved_eo_isr_image_snr, sys_kpi_achieved_eo_isr_smear

### `sys_eo_optical_service_motion_smear` — Motion smear

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_eo_optical_service_integration_time`, `sys_orbit_geometry_orbital_velocity`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_eo_optical_service_sun_elevation_at_target` — Sun elevation at target

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_eo_optical_service_swath_width` — Swath width

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_eo_optical_service_detector_ifov`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_access_gap` — Access gap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_contact_time` — Contact time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_delivery_latency` — Delivery latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_devices_per_beam` — Devices per beam

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_handover_rate` — Handover rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_latency` — Latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_throughput` — Throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_comms_time_transfer` — Time transfer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_cep90` — CEP90

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_collection_capacity` — Collection capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_contact_time` — Contact time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_delivery_latency` — Delivery latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_dwell_time` — Dwell time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_gsd` — GSD

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_image_snr` — Image SNR

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_intercept_prob` — Intercept prob.

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_min_detectable_eirp` — Min detectable EIRP

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_revisit` — Revisit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_smear` — Smear

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_eo_isr_swath` — Swath

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_access_gap` — Access gap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_cep90` — CEP90

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_fix_availability` — Fix availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_gdop` — GDOP

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_integrity_risk` — Integrity risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_latency` — Latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_position_accuracy` — Position accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_revisit` — Revisit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_time_to_first_fix` — Time to first fix

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_time_to_geolocate` — Time to geolocate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_achieved_pnt_time_transfer` — Time transfer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_access_gap` — Access gap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_contact_time` — Contact time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_delivery_latency` — Delivery latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_devices_per_beam` — Devices per beam

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_handover_rate` — Handover rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_latency` — Latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_throughput` — Throughput

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_comms_time_transfer` — Time transfer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_cep90` — CEP90

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_collection_capacity` — Collection capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_contact_time` — Contact time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_delivery_latency` — Delivery latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_dwell_time` — Dwell time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_gsd` — GSD

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_image_snr` — Image SNR

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_intercept_prob` — Intercept prob.

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_min_detectable_eirp` — Min detectable EIRP

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_revisit` — Revisit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_smear` — Smear

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_eo_isr_swath` — Swath

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_access_gap` — Access gap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_availability` — Availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_cep90` — CEP90

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_conjunction_rate` — Conjunction rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_coverage` — Coverage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_downlink_data_volume` — Downlink data volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_fix_availability` — Fix availability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_fold` — Fold

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_gdop` — GDOP

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_integrity_risk` — Integrity risk

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_latency` — Latency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_link_margin` — Link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_position_accuracy` — Position accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_revisit` — Revisit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_time_to_first_fix` — Time to first fix

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_time_to_geolocate` — Time to geolocate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_kpi_required_pnt_time_transfer` — Time transfer

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_launch_ground_and_disposal_daily_contact_time` — Daily contact time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_launch_ground_and_disposal_pass_duration`, `sys_launch_ground_and_disposal_passes_per_day`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_launch_ground_and_disposal_disposal_altitude` — Disposal altitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_launch_ground_and_disposal_ground_station_network` — Ground station network

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time, sys_kpi_achieved_comms_availability, sys_kpi_achieved_eo_isr_availability, sys_kpi_achieved_pnt_availability, sys_kpi_achieved_comms_latency, sys_kpi_achieved_pnt_latency, sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume, sys_kpi_achieved_comms_delivery_latency, sys_kpi_achieved_eo_isr_delivery_latency

### `sys_launch_ground_and_disposal_injection_accuracy` — Injection accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_launch_ground_and_disposal_pass_duration` — Pass duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_orbit_geometry_minimum_elevation`
- **read by** — `sys_launch_ground_and_disposal_daily_contact_time`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_launch_ground_and_disposal_passes_per_day` — Passes per day

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_orbit_geometry_inclination`
- **read by** — `sys_launch_ground_and_disposal_daily_contact_time`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_launch_ground_and_disposal_satellites_per_launch` — Satellites per launch

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mass_and_aero_accommodation_coefficient` — Accommodation coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_drag_coefficient`

### `sys_mass_and_aero_ballistic_coefficient` — Ballistic coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_spacecraft_dry_mass`, `sys_mass_and_aero_frontal_area`, `sys_mass_and_aero_drag_coefficient`
- **read by** — `sys_mass_and_aero_drag_acceleration`, `sys_propulsion_decay_rate`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_body_shape` — Body shape

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_drag_coefficient`, `sys_mass_and_aero_frontal_area`

### `sys_mass_and_aero_centre_of_mass` — Centre of mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_centre_of_pressure_offset` — Centre of pressure offset

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_hardware_aerodynamic_torque`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_drag_acceleration` — Drag acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_space_environment_atmospheric_density`, `sys_mass_and_aero_relative_velocity`, `sys_mass_and_aero_ballistic_coefficient`
- **read by** — `sys_attitude_hardware_aerodynamic_torque`, `sys_propulsion_thrust_to_drag_ratio`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_drag_coefficient` — Drag coefficient

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_body_shape`, `sys_mass_and_aero_accommodation_coefficient`
- **read by** — `sys_mass_and_aero_ballistic_coefficient`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_erosion_depth` — Erosion depth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_space_environment_atomic_oxygen_fluence`, `sys_mass_and_aero_erosion_yield`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_erosion_yield` — Erosion yield

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_erosion_depth`

### `sys_mass_and_aero_frontal_area` — Frontal area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_body_shape`, `sys_mass_and_aero_panel_layout`
- **read by** — `sys_mass_and_aero_ballistic_coefficient`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_launch_load_factor` — Launch load factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mass_and_aero_launch_mass` — Launch mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_mass_margin` — Mass margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mass_and_aero_moments_of_inertia` — Moments of inertia

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_hardware_gravity_gradient_torque`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_panel_layout` — Panel layout

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_frontal_area`

### `sys_mass_and_aero_panel_stiffness` — Panel stiffness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mass_and_aero_relative_velocity` — Relative velocity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_orbital_velocity`, `sys_space_environment_thermospheric_wind`
- **read by** — `sys_mass_and_aero_drag_acceleration`, `sys_thermal_free_molecular_heat_flux`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mass_and_aero_spacecraft_dry_mass` — Spacecraft dry mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_ballistic_coefficient`, `sys_multi_payload_payload_mass_fraction`

### `sys_mass_and_aero_structural_mass_fraction` — Structural mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_mission_concept_constellation_in_low_orbit` — Constellation in low orbit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_concept_single_large_satellite` — Single large satellite

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_access_gap_requirement` — Access gap requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_availability_requirement` — Availability requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_coverage_latitude_band` — Coverage latitude band

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_coverage_requirement` — Coverage requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_data_latency_requirement` — Data latency requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_launch_window` — Launch window

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_mission_duration` — Mission duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_orbit_maintenance_station_keeping_delta_v`

### `sys_mission_requirements_mission_epoch` — Mission epoch

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_mission_requirements_revisit_requirement` — Revisit requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | systems |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_multi_payload_accommodation_area` — Accommodation area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_boresight_separation` — Boresight separation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_multi_payload_number_of_payloads` — Number of payloads

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_multi_payload_operating_conflict_rate` — Operating conflict rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_payload_mass_fraction` — Payload mass fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_payload_interface_payload_mass`, `sys_mass_and_aero_spacecraft_dry_mass`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_shared_downlink_allocation` — Shared downlink allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_tt_and_c_and_downlink_downlink_rate`, `sys_payload_interface_payload_data_rate`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_shared_power_allocation` — Shared power allocation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_power_electrical_power`, `sys_payload_interface_peak_payload_power`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_simultaneity_window` — Simultaneity window

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90, sys_kpi_achieved_pnt_gdop, sys_kpi_achieved_pnt_time_to_first_fix, sys_kpi_achieved_pnt_time_to_geolocate
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_multi_payload_thermal_dissipation_share` — Thermal dissipation share

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_thermal_radiator_area_estimate`, `sys_payload_interface_payload_dissipation`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_along_track_control_tolerance` — Along-track control tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_ephemeris_prediction_accuracy` — Ephemeris prediction accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_ephemeris_upload_cadence` — Ephemeris upload cadence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_navigation_filter_rate` — Navigation filter rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_navigation_and_orbit_determination_position_error_growth`

### `sys_navigation_and_orbit_determination_orbit_determination_accuracy` — Orbit determination accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_navigation_and_orbit_determination_orbit_determination_cycle` — Orbit determination cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_phasing_tolerance` — Phasing tolerance

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_navigation_and_orbit_determination_position_error_growth` — Position error growth

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_navigation_sensing_unmodelled_acceleration`, `sys_navigation_and_orbit_determination_navigation_filter_rate`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_and_orbit_determination_prediction_horizon` — Prediction horizon

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_sensing_accelerometer_class` — Accelerometer class

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_navigation_sensing_density_estimate` — Density estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_sensing_gnss_solution` — GNSS solution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_navigation_sensing_observed_acceleration` — Observed acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_sensing_scale_factor` — Scale factor

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_navigation_sensing_unmodelled_acceleration` — Unmodelled acceleration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_navigation_and_orbit_determination_position_error_growth`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_geometry_altitude` — Altitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_attitude_hardware_gravity_gradient_torque`, `sys_comms_service_beam_footprint_diameter`, `sys_comms_service_doppler_shift`, `sys_comms_service_slant_range`, `sys_eo_optical_service_ground_sample_distance`, `sys_eo_optical_service_swath_width`, `sys_launch_ground_and_disposal_pass_duration`, `sys_launch_ground_and_disposal_passes_per_day`, `sys_orbit_geometry_eclipse_fraction`, `sys_orbit_geometry_nodal_regression_rate`, `sys_orbit_geometry_off_nadir_angle`, `sys_orbit_geometry_orbital_period`, `sys_orbit_geometry_orbital_velocity`, `sys_propulsion_re_entry_time_unpowered`, `sys_rf_geolocation_service_dwell_time`
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_coverage, sys_kpi_achieved_eo_isr_coverage, sys_kpi_achieved_pnt_coverage, sys_kpi_achieved_comms_access_gap, sys_kpi_achieved_pnt_access_gap, sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time, sys_kpi_achieved_comms_conjunction_rate, sys_kpi_achieved_eo_isr_conjunction_rate, sys_kpi_achieved_pnt_conjunction_rate, sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin, sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_swath, sys_kpi_achieved_eo_isr_min_detectable_eirp, sys_kpi_achieved_eo_isr_dwell_time

### `sys_orbit_geometry_eccentricity` — Eccentricity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_eo_isr_gsd

### `sys_orbit_geometry_eclipse_fraction` — Eclipse fraction

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_orbit_geometry_inclination`, `sys_orbit_geometry_ltan`
- **read by** — `sys_power_battery_capacity`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_geometry_inclination` — Inclination

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_launch_ground_and_disposal_passes_per_day`, `sys_orbit_geometry_eclipse_fraction`, `sys_orbit_geometry_nodal_regression_rate`
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_coverage, sys_kpi_achieved_eo_isr_coverage, sys_kpi_achieved_pnt_coverage, sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time

### `sys_orbit_geometry_ltan` — LTAN

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_orbit_geometry_eclipse_fraction`
- **contributes to** — sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_eo_isr_collection_capacity

### `sys_orbit_geometry_minimum_elevation` — Minimum elevation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_comms_service_slant_range`, `sys_launch_ground_and_disposal_pass_duration`
- **contributes to** — sys_kpi_achieved_comms_fold, sys_kpi_achieved_eo_isr_fold, sys_kpi_achieved_pnt_fold, sys_kpi_achieved_comms_access_gap, sys_kpi_achieved_pnt_access_gap, sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_comms_contact_time, sys_kpi_achieved_eo_isr_contact_time, sys_kpi_achieved_comms_handover_rate, sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin, sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_pnt_gdop, sys_kpi_achieved_pnt_fix_availability, sys_kpi_achieved_pnt_position_accuracy, sys_kpi_achieved_pnt_time_to_first_fix, sys_kpi_achieved_pnt_integrity_risk

### `sys_orbit_geometry_nodal_regression_rate` — Nodal regression rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_orbit_geometry_inclination`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_geometry_off_nadir_angle` — Off-nadir angle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_eo_optical_service_detector_ifov`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_swath, sys_kpi_achieved_eo_isr_collection_capacity

### `sys_orbit_geometry_orbital_period` — Orbital period

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`
- **read by** — `sys_tt_and_c_and_downlink_data_volume_per_orbit`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_geometry_orbital_velocity` — Orbital velocity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`
- **read by** — `sys_eo_optical_service_motion_smear`, `sys_mass_and_aero_relative_velocity`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_geometry_repeat_cycle` — Repeat cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_revisit, sys_kpi_achieved_pnt_revisit, sys_kpi_achieved_eo_isr_collection_capacity
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_abort_altitude` — Abort altitude

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_collision_avoidance_delta_v` — Collision avoidance delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_orbit_maintenance_deorbit_delta_v` — Deorbit delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_manoeuvre_duration` — Manoeuvre duration

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_manoeuvre_frequency` — Manoeuvre frequency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_propellant_reserve` — Propellant reserve

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_orbit_maintenance_station_keeping_box` — Station-keeping box

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_station_keeping_delta_v` — Station-keeping delta-v

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_propulsion_decay_rate`, `sys_mission_requirements_mission_duration`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_thrust_duty_limit` — Thrust duty limit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_orbit_maintenance_thrust_modulation_resolution` — Thrust modulation resolution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_payload_interface_alignment_stability` — Alignment stability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_image_snr, sys_kpi_achieved_eo_isr_smear

### `sys_payload_interface_average_payload_power` — Average payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_payload_interface_field_of_view_keep_out` — Field of view keep-out

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_payload_interface_operating_temperature_range` — Operating temperature range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_payload_interface_payload_data_rate` — Payload data rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_multi_payload_shared_downlink_allocation`

### `sys_payload_interface_payload_dissipation` — Payload dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_multi_payload_thermal_dissipation_share`

### `sys_payload_interface_payload_duty_cycle` — Payload duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume, sys_kpi_achieved_eo_isr_intercept_prob, sys_kpi_achieved_eo_isr_dwell_time
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_payload_interface_payload_envelope` — Payload envelope

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_payload_interface_payload_mass` — Payload mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_multi_payload_payload_mass_fraction`

### `sys_payload_interface_peak_payload_power` — Peak payload power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_multi_payload_shared_power_allocation`
- **contributes to** — sys_kpi_achieved_comms_throughput, sys_kpi_achieved_eo_isr_collection_capacity, sys_kpi_achieved_eo_isr_dwell_time

### `sys_payload_interface_pointing_knowledge_requirement` — Pointing knowledge requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90, sys_kpi_achieved_pnt_position_accuracy

### `sys_payload_interface_pointing_requirement` — Pointing requirement

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_link_margin, sys_kpi_achieved_eo_isr_link_margin, sys_kpi_achieved_pnt_link_margin, sys_kpi_achieved_eo_isr_gsd, sys_kpi_achieved_eo_isr_smear

### `sys_pnt_service_clock_stability` — Clock stability

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_time_transfer, sys_kpi_achieved_pnt_time_transfer, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90, sys_kpi_achieved_pnt_position_accuracy, sys_kpi_achieved_pnt_time_to_first_fix, sys_kpi_achieved_pnt_integrity_risk

### `sys_pnt_service_geometric_dilution` — Geometric dilution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_constellation_trade_output_number_of_planes`, `sys_constellation_trade_output_satellites_per_plane`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_pnt_service_time_transfer_accuracy` — Time transfer accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_pointing_error_budget_absolute_knowledge_error` — Absolute knowledge error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_pointing_error_budget_absolute_measurement_error` — Absolute measurement error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_pointing_error_budget_absolute_performance_error` — Absolute performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_pointing_error_budget_control_error_contribution` — Control error contribution

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_pointing_error_budget_disturbance_torque_budget` — Disturbance torque budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_pointing_error_budget_relative_performance_error` — Relative performance error

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | gnc |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_power_array_area` — Array area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_power_electrical_power`, `sys_power_cell_efficiency`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_power_array_degradation` — Array degradation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_power_battery_capacity` — Battery capacity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_power_bus_power_demand`, `sys_orbit_geometry_eclipse_fraction`, `sys_power_depth_of_discharge`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_collection_capacity, sys_kpi_achieved_eo_isr_dwell_time
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_power_bus_power_demand` — Bus power demand

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_power_battery_capacity`, `sys_power_electrical_power`

### `sys_power_bus_voltage` — Bus voltage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_power_cell_efficiency` — Cell efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_power_array_area`

### `sys_power_depth_of_discharge` — Depth of discharge

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_power_battery_capacity`

### `sys_power_electrical_power` — Electrical power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_power_ppu_power`, `sys_power_bus_power_demand`
- **read by** — `sys_multi_payload_shared_power_allocation`, `sys_power_array_area`
- **contributes to** — sys_kpi_achieved_comms_throughput, sys_kpi_achieved_eo_isr_collection_capacity, sys_kpi_achieved_eo_isr_dwell_time
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_power_end_of_life_power` — End of life power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_power_harness_loss` — Harness loss

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_power_power_margin` — Power margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_power_ppu_power` — PPU power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_propulsion_thrust_authority`, `sys_propulsion_ppu_efficiency`
- **read by** — `sys_power_electrical_power`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_power_sada_angle_range` — SADA angle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | power |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_achievable_lifetime` — Achievable lifetime

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_compression_ratio` — Compression ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_decay_rate` — Decay rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_mass_and_aero_ballistic_coefficient`, `sys_space_environment_atmospheric_density`
- **read by** — `sys_orbit_maintenance_station_keeping_delta_v`, `sys_propulsion_re_entry_time_unpowered`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_delta_v_budget` — Delta-v budget

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_propulsion_propellant_mass`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_intake_capture_area` — Intake capture area

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_minimum_impulse_bit` — Minimum impulse bit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_number_of_thrusters` — Number of thrusters

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_ppu_efficiency` — PPU efficiency

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_power_ppu_power`

### `sys_propulsion_propellant_mass` — Propellant mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_propulsion_delta_v_budget`, `sys_propulsion_specific_impulse`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_propellant_type` — Propellant type

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_re_entry_time_unpowered` — Re-entry time unpowered

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_propulsion_decay_rate`, `sys_orbit_geometry_altitude`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_specific_impulse` — Specific impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_propulsion_propellant_mass`

### `sys_propulsion_tank_volume` — Tank volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_throttle_range` — Throttle range

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_thrust_authority` — Thrust authority

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_power_ppu_power`, `sys_propulsion_thrust_to_drag_ratio`
- **contributes to** — sys_kpi_achieved_comms_availability, sys_kpi_achieved_eo_isr_availability, sys_kpi_achieved_pnt_availability
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_thrust_to_drag_ratio` — Thrust to drag ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_propulsion_thrust_authority`, `sys_mass_and_aero_drag_acceleration`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_propulsion_thrust_vector_alignment` — Thrust vector alignment

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_thruster_duty_cycle` — Thruster duty cycle

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_propulsion_total_impulse` — Total impulse

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | propulsion |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_rf_geolocation_service_angle_of_arrival_accuracy` — Angle of arrival accuracy

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90, sys_kpi_achieved_pnt_time_to_geolocate

### `sys_rf_geolocation_service_cluster_baseline` — Cluster baseline

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_constellation_trade_output_inter_satellite_range`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_rf_geolocation_service_dwell_time` — Dwell time

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_orbit_geometry_altitude`, `sys_eo_optical_service_aperture_diameter`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_image_snr, sys_kpi_achieved_eo_isr_smear, sys_kpi_achieved_eo_isr_collection_capacity, sys_kpi_achieved_eo_isr_intercept_prob, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_rf_geolocation_service_geolocation_cep90` — Geolocation CEP90

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_rf_geolocation_service_timing_synchronisation` — Timing synchronisation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | payload |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_time_transfer, sys_kpi_achieved_pnt_time_transfer, sys_kpi_achieved_eo_isr_cep90, sys_kpi_achieved_pnt_cep90, sys_kpi_achieved_pnt_position_accuracy, sys_kpi_achieved_pnt_integrity_risk

### `sys_space_environment_ap` — Ap

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_space_environment_atmospheric_density` — Atmospheric density

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_drag_acceleration`, `sys_propulsion_decay_rate`, `sys_thermal_free_molecular_heat_flux`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_space_environment_atomic_oxygen_fluence` — Atomic oxygen fluence

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_erosion_depth`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_space_environment_f10_7` — F10.7

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_space_environment_solar_flux` — Solar flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_space_environment_thermospheric_wind` — Thermospheric wind

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | environment |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_mass_and_aero_relative_velocity`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_structure_accommodation_volume` — Accommodation volume

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_structure_bus_dimensions` — Bus dimensions

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_structure_panel_thickness` — Panel thickness

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_structure_primary_structure_material` — Primary structure material

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | mass |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_thermal_alpha_over_epsilon_ratio` — Alpha over epsilon ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_emissivity` — Emissivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_thermal_radiator_area_estimate`

### `sys_thermal_environmental_heat_load` — Environmental heat load

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_equipment_temperature_limits` — Equipment temperature limits

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_thermal_free_molecular_heat_flux` — Free-molecular heat flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_space_environment_atmospheric_density`, `sys_mass_and_aero_relative_velocity`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_heater_power_estimate` — Heater power estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_internal_dissipation` — Internal dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_thermal_radiator_area_estimate`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_mli_layer_count` — MLI layer count

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_thermal_radiator_area_estimate` — Radiator area estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_thermal_internal_dissipation`, `sys_thermal_emissivity`
- **read by** — `sys_multi_payload_thermal_dissipation_share`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_thermal_solar_absorptivity` — Solar absorptivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_thermal_thermal_mass` — Thermal mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_eo_isr_collection_capacity, sys_kpi_achieved_eo_isr_dwell_time
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_tt_and_c_and_downlink_data_volume_per_orbit` — Data volume per orbit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_tt_and_c_and_downlink_telemetry_rate`, `sys_orbit_geometry_orbital_period`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_tt_and_c_and_downlink_downlink_rate` — Downlink rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_multi_payload_shared_downlink_allocation`
- **contributes to** — sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume, sys_kpi_achieved_comms_delivery_latency, sys_kpi_achieved_eo_isr_delivery_latency
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_tt_and_c_and_downlink_on_board_storage` — On-board storage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **contributes to** — sys_kpi_achieved_comms_downlink_data_volume, sys_kpi_achieved_eo_isr_downlink_data_volume, sys_kpi_achieved_pnt_downlink_data_volume, sys_kpi_achieved_eo_isr_collection_capacity
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_tt_and_c_and_downlink_telemetry_rate` — Telemetry rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_tt_and_c_and_downlink_data_volume_per_orbit`

### `sys_tt_and_c_and_downlink_tt_and_c_antenna_gain` — TT&C antenna gain

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_tt_and_c_and_downlink_tt_and_c_link_margin`

### `sys_tt_and_c_and_downlink_tt_and_c_frequency_band` — TT&C frequency band

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `sys_tt_and_c_and_downlink_tt_and_c_link_margin` — TT&C link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | computed |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **reads** — `sys_tt_and_c_and_downlink_tt_and_c_transmit_power`, `sys_tt_and_c_and_downlink_tt_and_c_antenna_gain`, `sys_comms_service_slant_range`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `sys_tt_and_c_and_downlink_tt_and_c_transmit_power` — TT&C transmit power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — `sys_tt_and_c_and_downlink_tt_and_c_link_margin`


## `thermal` — Thermal

### `l3_thermal_ach_01` — Solar absorptivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_02` — Emissivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_03` — MLI layer count

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_04` — Equipment temperature limits

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_05` — Internal dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_06` — Environmental heat load

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_07` — Free-molecular heat flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_08` — Alpha over epsilon ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_09` — Radiator area estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_10` — Heater power estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_ach_11` — Thermal mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_interface` — Thermal — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_n_01` — Thermal · internal — to be named (1)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_02` — Thermal · internal — to be named (2)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_03` — Thermal · internal — to be named (3)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_04` — Thermal · internal — to be named (4)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_05` — Thermal · internal — to be named (5)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_06` — Thermal · internal — to be named (6)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_07` — Thermal · internal — to be named (7)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_08` — Thermal · internal — to be named (8)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_09` — Thermal · internal — to be named (9)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_10` — Thermal · internal — to be named (10)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_n_11` — Thermal · internal — to be named (11)

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | declared |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

### `l3_thermal_req_01` — Solar absorptivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_02` — Emissivity

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_03` — MLI layer count

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_04` — Equipment temperature limits

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_05` — Internal dissipation

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_06` — Environmental heat load

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_07` — Free-molecular heat flux

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_08` — Alpha over epsilon ratio

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_09` — Radiator area estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_10` — Heater power estimate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_thermal_req_11` — Thermal mass

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | thermal |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `thm` — Thermal

### `thm_absorbed_albedo` — Absorbed albedo

> How much reflected sunlight off the Earth does the spacecraft absorb?

| | |
|---|---|
| symbol | `Q_alb` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | thermal |
| evidence tier | B |
| relation | `Q = S*A*alpha_s*a_E*F` |
| source | `larson_wertz` |
| valid over | 0 … 100000 W |

- **lower bound** — absorbed power cannot be negative
- **upper bound** — above 100 kW the illuminated area is not this spacecraft
- **reads** — `aero_frontal_area`, `thm_absorptivity`, `thm_view_factor`
- **read by** — `thm_equilibrium_temperature`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `thm_absorbed_ir` — Absorbed Earth infrared

> How much of the Earth's own thermal emission does the spacecraft absorb?

| | |
|---|---|
| symbol | `Q_ir` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | thermal |
| evidence tier | B |
| relation | `Q = q_IR*A*eps*F` |
| source | `larson_wertz` |
| valid over | 0 … 100000 W |

- **lower bound** — absorbed power cannot be negative
- **upper bound** — above 100 kW the surface is not this spacecraft
- **reads** — `aero_frontal_area`, `thm_emissivity`, `thm_view_factor`
- **read by** — `thm_equilibrium_temperature`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Unlike albedo this does not switch off in eclipse, which is what stops a low-orbiting spacecraft getting as cold as an intuition built on higher orbits expects.

### `thm_absorbed_solar` — Absorbed direct solar

> How much heat does direct sunlight put into the spacecraft?

| | |
|---|---|
| symbol | `Q_sol` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | thermal |
| evidence tier | A |
| relation | `Q = S*A*alpha_s*cos(theta)` |
| source | `larson_wertz` |
| valid over | 0 … 100000 W |

- **lower bound** — absorbed power cannot be negative
- **upper bound** — above 100 kW the illuminated area is not this spacecraft
- **reads** — `aero_frontal_area`, `thm_absorptivity`, `pwr_array_incidence`
- **read by** — `thm_equilibrium_temperature`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `thm_absorptivity` — Solar absorptivity

> What fraction of incident sunlight does the external surface absorb?

| | |
|---|---|
| symbol | `alpha_s` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `alpha_s = 0.3` |
| source | `larson_wertz` |
| declared value | **0.3** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 1 - |

- **lower bound** — below 0.05 no space-qualified coating is that reflective after atomic-oxygen exposure
- **upper bound** — unity is a perfect black body, the limit rather than a coating
- **read by** — `thm_absorbed_albedo`, `thm_absorbed_solar`

### `thm_aero_heating` — Free-molecular aerodynamic heating

> How much does the air itself heat the spacecraft?

| | |
|---|---|
| symbol | `Q_aero` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | thermal |
| evidence tier | C |
| relation | `Q = 0.5*rho*V^3*A*C_h` |
| source | `larson_wertz` |
| valid over | 0 … 100000 W |

- **lower bound** — heating cannot be negative
- **upper bound** — above 100 kW the vehicle is re-entering
- **reads** — `env_mass_density`, `orbit_velocity`, `aero_frontal_area`
- **read by** — `thm_equilibrium_temperature`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

Usually neglected, and at 200 km it should not be. It is a real term in the balance of a small, fast, low-flying vehicle.

### `thm_dissipation` — Heat to reject

> How much heat does the spacecraft have to get rid of?

| | |
|---|---|
| symbol | `Q` |
| type | `Power` |
| unit | W |
| kind | computed |
| owner | thermal |
| evidence tier | B |
| relation | `Q = P_dem - P_rf - P_beam` |
| source | `larson_wertz` |
| valid over | 0 … 50000 W |

- **lower bound** — heat to reject cannot be negative
- **upper bound** — above 50 kW nothing in this mass class dissipates that much
- **reads** — `pwr_demand`, `prop_jet_power`, `com_tx_power`
- **read by** — `thm_equilibrium_temperature`, `thm_required_radiator_area`
- **assumes** All bus power that is not beam kinetic energy or radiated radio power becomes heat — fails when true to within the accuracy of this budget; it ignores the small fraction stored in the battery over a cycle, which averages to zero over an orbit
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `thm_emissivity` — Infrared emissivity

> How well does the external surface radiate in the infrared?

| | |
|---|---|
| symbol | `eps` |
| type | `Ratio` |
| unit | - |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `eps = 0.85` |
| source | `larson_wertz` |
| declared value | **0.85** - |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.05 … 1 - |

- **lower bound** — below 0.05 the surface cannot reject heat at all and no design closes
- **upper bound** — unity is a perfect emitter, the limit rather than a coating
- **read by** — `thm_absorbed_ir`, `thm_equilibrium_temperature`, `thm_required_radiator_area`

### `thm_equilibrium_temperature` — Equilibrium temperature

> What temperature does the spacecraft settle at?

| | |
|---|---|
| symbol | `T_eq` |
| type | `Temperature` |
| unit | K |
| kind | computed |
| owner | thermal |
| evidence tier | B |
| relation | `T = (Q_total/(eps*sigma*A_rad))^(1/4)` |
| source | `larson_wertz` |
| valid over | 100 … 1000 K |

- **lower bound** — below 100 K the spacecraft would be colder than deep space plus the Earth's infrared, which is not possible in this orbit
- **upper bound** — above 1000 K nothing survives and the balance is being fed a nonsense load
- **reads** — `thm_absorbed_solar`, `thm_absorbed_albedo`, `thm_absorbed_ir`, `thm_aero_heating`, `thm_dissipation`, `thm_radiator_area`, `thm_emissivity`
- **read by** — `pwr_cell_derating`, `thm_margin`
- **assumes** One node — the whole spacecraft is at one temperature — fails when a real design has a gradient across every panel; this sizes a radiator and bounds a temperature, and it does not predict a gradient
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `thm_internal_dissipation` — Internal dissipation

> How much of the electrical power ends up as heat inside the spacecraft?

| | |
|---|---|
| symbol | `Q_int` |
| type | `Power` |
| unit | W |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `Q_int = 0` |
| source | `larson_wertz` |
| declared value | **0** W |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0 … 20000 W |

- **lower bound** — dissipation cannot be negative
- **upper bound** — above 20 kW nothing in this mass class dissipates that much
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.

Left at zero and overridden by the computed dissipation node. It exists so a case can pin a measured value in place of the computed one.

### `thm_margin` — Thermal margin

> Is the spacecraft inside its temperature limit?

| | |
|---|---|
| symbol | `M_thm` |
| type | `Temperature` |
| unit | K |
| kind | computed |
| owner | thermal |
| evidence tier | A |
| relation | `M = T_max - T_eq` |
| source | `ecss_e_st_10_02` |
| valid over | -500 … 500 K |

- **lower bound** — a margin below -500 K means the balance is being fed a nonsense load
- **upper bound** — a margin above 500 K means the limit is not the one that binds
- **reads** — `thm_equilibrium_temperature`, `thm_temperature_limit`
- **read by** — `kpi_thermal_margin`
- **contributes to** — kpi_thermal_margin
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `thm_radiator_area` — Radiator area

> How much area is available to reject heat?

| | |
|---|---|
| symbol | `A_rad` |
| type | `Area` |
| unit | m^2 |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `A_rad = 2.5` |
| source | `orbitt_case_c1` |
| declared value | **2.5** m^2 |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 0.1 … 30 m^2 |

- **lower bound** — below 0.1 m2 no useful heat is rejected
- **upper bound** — above 30 m2 the radiator is the spacecraft
- **read by** — `thm_equilibrium_temperature`

### `thm_required_radiator_area` — Radiator area required

> How much radiator would be needed to hold the temperature limit?

| | |
|---|---|
| symbol | `A_req` |
| type | `Area` |
| unit | m^2 |
| kind | computed |
| owner | thermal |
| evidence tier | A |
| relation | `A = Q/(eps*sigma*(T^4 - T_sink^4))` |
| source | `larson_wertz` |
| valid over | 0 … 1000 m^2 |

- **lower bound** — a required area cannot be negative
- **upper bound** — above 1000 m2 the design does not close and the answer is that the dissipation is wrong
- **reads** — `thm_dissipation`, `thm_emissivity`, `thm_temperature_limit`
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

The inverse question, and the one a thermal design actually asks.

### `thm_temperature_limit` — Upper temperature limit

> What is the highest temperature the electronics may reach?

| | |
|---|---|
| symbol | `T_max` |
| type | `Temperature` |
| unit | K |
| kind | declared |
| owner | thermal |
| evidence tier | A |
| relation | `T_max = 323` |
| source | `ecss_e_st_10_02` |
| declared value | **323** K |
| confirmed by | A. Rai / 2026-09-01 |
| valid over | 273 … 400 K |

- **lower bound** — below 273 K no electronics box in this design is qualified to operate
- **upper bound** — above 400 K no electronics box in this design survives
- **read by** — `thm_margin`, `thm_required_radiator_area`

### `thm_view_factor` — Earth view factor

> How much of the sky, from a nadir-facing surface, is filled by the Earth?

| | |
|---|---|
| symbol | `F_E` |
| type | `Ratio` |
| unit | - |
| kind | computed |
| owner | thermal |
| evidence tier | A |
| relation | `F = (Re/r)^2` |
| source | `larson_wertz` |
| valid over | 0 … 1 - |

- **lower bound** — a view factor cannot be negative
- **upper bound** — a view factor cannot exceed one
- **reads** — `orbit_radius`
- **read by** — `thm_absorbed_albedo`, `thm_absorbed_ir`
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.


## `ttc` — TT&C and downlink

### `l3_ttc_ach_01` — Telemetry rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_02` — TT&C transmit power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_03` — TT&C frequency band

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_04` — TT&C antenna gain

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_05` — Downlink rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_06` — Data volume per orbit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_07` — On-board storage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_ach_08` — TT&C link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | achieved |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_interface` — TT&C and downlink — subsystem interface

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_01` — Telemetry rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_02` — TT&C transmit power

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_03` — TT&C frequency band

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_04` — TT&C antenna gain

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_05` — Downlink rate

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_06` — Data volume per orbit

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_07` — On-board storage

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

### `l3_ttc_req_08` — TT&C link margin

> 

| | |
|---|---|
| symbol | `` |
| type | `` |
| unit | ? |
| kind | required |
| owner | comms |
| evidence tier |  |
| relation | `` |
| source | `` |
| valid over | 0 … 0 ? |

- **lower bound** — 
- **upper bound** — 
- **read by** — nothing yet. Every one of these is a leaf of the design, or an oversight.
- **evidence** — none. Nothing outside this code has agreed with what it computes, so its validation credibility factor is zero, which governs the whole vector.

