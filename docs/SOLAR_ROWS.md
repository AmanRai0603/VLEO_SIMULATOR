# The solar subsystem, row by row
Generated by `tools/solar_rows.py`. Do not edit by hand.

## Where the three origins come from

**Ported** — the row is part of the chain that produces the study's driver set.`l3_solar_interface` reaches it, and the study's own output(`matlab/reference/mission_drivers.csv`) is twenty-five numbers: five scenariosover f107, f107bar, ap, kp_mean and kp_peak.

**Kernel** — the row carries the study's relation but computes nothing live.Its relation moved into `vleo-core` so a driver set could evaluate it at fivescenarios rather than one; the row remains as the provenance of that constant.

**Extra** — a question this tree asks that the study did not.

`✓` in the parity column means the row holds a `parity.csv`: the MATLAB tool'sown answer, recorded as a second opinion rather than as a fixture.

## What this adds up to

**No row here duplicates another.** Each was swept over seven drivers across their declared ranges and fingerprinted on the result; all 55 fingerprints are distinct. So nothing is removable on the grounds of being a repeat — the only question left for each row is whether its question is worth asking.

| | with a figure | with no figure | total |
|---|---|---|---|
| **Extra** — questions this tree added | 28 | 9 | 37 |
| **MATLAB** — the port of the study | 1 | 17 | 18 |

The shape of that table is the finding. Nearly every figure in this subsystem draws a row the study never had, and nearly every row that reproduces the study has no picture of it. Cutting extras costs pictures; the port is what is undrawn.

**9 rows are free to remove** — no figure draws them, they are not the port, and nothing outside the set reads them. They come out together: `l3_solar_ach_01`, `l3_solar_ach_02`, `l3_solar_ach_04`, `l3_solar_ach_05`, `l3_solar_req_01`, `l3_solar_req_02`, `l3_solar_req_04`, `l3_solar_req_05`, `sw_window_peak_level`.

---

# EXTRA — 37 rows the study never had

## Extra, drawn by a figure — 28 rows

Removing any of these means rewriting the panel that draws it.

| row | what it answers | value | number or curve | remove? | why |
|---|---|---|---|---|---|
| `l3_solar_ach_03` | Closure — Ap, survival | computed | **curve** over `l3_solar_req_03` (3203%), +1 | **COSTS A FIGURE** | the design panel draws it; removing it means rewriting that panel |
| `l3_solar_req_03` | Required — Ap, survival | 207 | **number** — a decision; it is what moves other rows | **KEEP** | 1 live row read it: `l3_solar_ach_03` |
| `sw_activity_band` | F10.7 activity band | computed | **curve** over `env_f107` (100%) | **COSTS A FIGURE** | the segmentation panel draws it; removing it means rewriting that panel |
| `sw_ap_design` | Ap design value | computed | **curve** over `sw_storm_design_level` (64%) | **KEEP** | 4 live rows read it: `sw_design_safe_duration`, `sw_exceedance_duration`, `sw_exceedance_phase`, `sw_exceedance_rate` |
| `sw_band_coverage` | Stated band coverage | 0.950927 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the predict panel draws it; removing it means rewriting that panel |
| `sw_cycle_phase` | Solar cycle phase | computed | **number here** — 1 upstream decision(s) refuse at a range end | **KEEP** | 1 live row read it: `sw_mean_cycle_level` |
| `sw_cycle_repeatability` | Cycle-to-cycle repeatability | 0.77014 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the repeatability panel draws it; removing it means rewriting that panel |
| `sw_design_safe_duration` | How long the design Ap lasts | computed | **curve** over `sw_storm_design_level` (87%) | **COSTS A FIGURE** | the design panel draws it; removing it means rewriting that panel |
| `sw_event_duration` | F10.7 burst duration | 286111 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the pattern panel draws it; removing it means rewriting that panel |
| `sw_exceedance_duration` | How long one exceedance lasts | computed | **curve** over `sw_storm_design_level` (11%) | **COSTS A FIGURE** | the design panel draws it; removing it means rewriting that panel |
| `sw_exceedance_phase` | When in the cycle exceedances fall | computed | **curve** over `sw_storm_design_level` (5.14%) | **COSTS A FIGURE** | the design panel draws it; removing it means rewriting that panel |
| `sw_exceedance_rate` | Days a year above the design level | computed | **curve** over `sw_storm_design_level` (1538%) | **COSTS A FIGURE** | the design panel draws it; removing it means rewriting that panel |
| `sw_f107a_ratio` | Daily F10.7 scatter about F10.7A | 0.12221 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the climate panel draws it; removing it means rewriting that panel |
| `sw_forecast_bias` | Issued-outlook F10.7 bias | computed | **curve** over `sw_recurrence_lag` (45%) | **COSTS A FIGURE** | the forecast panel draws it; removing it means rewriting that panel |
| `sw_forecast_skill` | Issued-outlook skill against persistence | computed | **curve** over `sw_recurrence_lag` (868%) | **COSTS A FIGURE** | the forecast panel draws it; removing it means rewriting that panel |
| `sw_horizon_climatology` | F10.7 error from climatology at a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (1.87%) | **COSTS A FIGURE** | the predict panel draws it; removing it means rewriting that panel |
| `sw_horizon_persistence` | F10.7 error from persistence at a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (33%) | **COSTS A FIGURE** | the predict panel draws it; removing it means rewriting that panel |
| `sw_kp_from_ap` ✓ | Kp from daily Ap | computed | **curve** over `sys_mission_requirements_mission_duration` (32%) | **COSTS A FIGURE** | the climate panel draws it; removing it means rewriting that panel |
| `sw_kp_slot_bias` | Kp slot bias, daily peak | computed | **curve** over `sys_mission_requirements_mission_duration` (8.59%) | **COSTS A FIGURE** | the climate panel draws it; removing it means rewriting that panel |
| `sw_mean_cycle_level` | Mean-cycle F10.7 at a phase | computed | **number here** — 1 upstream decision(s) refuse at a range end | **COSTS A FIGURE** | the climate/repeatability panel draws it; removing it means rewriting that panel |
| `sw_recurrence_lag` | Rotation recurrence lag | 2.2464e+06 | **number** — a decision; it is what moves other rows | **KEEP** | 2 live rows read it: `sw_forecast_bias`, `sw_forecast_skill` |
| `sw_recurrence_strength` | Rotation recurrence strength | 0.375988 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the pattern panel draws it; removing it means rewriting that panel |
| `sw_regime` ✓ | Geomagnetic regime | computed | **number** — all 1 decisions upstream leave it unchanged | **COSTS A FIGURE** | the segmentation panel draws it; removing it means rewriting that panel |
| `sw_semiannual_amplitude` | Semiannual Ap amplitude | 1.27811 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the climate panel draws it; removing it means rewriting that panel |
| `sw_spike_threshold` | F10.7 spike threshold | 1.30487 | **number** — a decision; it is what moves other rows | **COSTS A FIGURE** | the pattern panel draws it; removing it means rewriting that panel |
| `sw_storm_design_level` | Storm level designed for | 3 | **number** — a decision; it is what moves other rows | **KEEP** | 1 live row read it: `sw_ap_design` |
| `sw_storm_return_level` | Ap at a storm return period | computed | **curve** over `sys_mission_requirements_mission_duration` (88%) | **KEEP** | 4 live rows read it: `l3_solar_ach_03`, `sw_kp_from_ap`, `sw_kp_slot_bias`, `sw_regime` |
| `sw_uncertainty_growth` | F10.7 growth at 95% over a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (51%) | **COSTS A FIGURE** | the predict panel draws it; removing it means rewriting that panel |

## Extra, no figure — 9 rows

The cheapest removals in the subsystem, except where another row reads them.

| row | what it answers | value | number or curve | remove? | why |
|---|---|---|---|---|---|
| `l3_solar_ach_01` | Closure — F10.7, sustained | computed | **curve** over `l3_solar_req_01` (246%), +3 | **EASY** | nothing reads it and no figure draws it |
| `l3_solar_ach_02` | Closure — F10.7, single day | computed | **curve** over `l3_solar_req_02` (273%), +3 | **EASY** | nothing reads it and no figure draws it |
| `l3_solar_ach_04` | Closure — Ap, sustained | computed | **curve** over `sw_ap_central_expectation` (376%), +2 | **EASY** | nothing reads it and no figure draws it |
| `l3_solar_ach_05` | Closure — Ap, single day | computed | **curve** over `sw_ap_central_expectation` (331%), +2 | **EASY** | nothing reads it and no figure draws it |
| `l3_solar_req_01` | Required — F10.7, sustained | 260 | **number** — a decision; it is what moves other rows | **EASY, AS A PAIR** | free only if `l3_solar_ach_01` goes with it — it is read by nothing else |
| `l3_solar_req_02` | Required — F10.7, single day | 350 | **number** — a decision; it is what moves other rows | **EASY, AS A PAIR** | free only if `l3_solar_ach_02` goes with it — it is read by nothing else |
| `l3_solar_req_04` | Required — Ap, sustained | 48 | **number** — a decision; it is what moves other rows | **EASY, AS A PAIR** | free only if `l3_solar_ach_04` goes with it — it is read by nothing else |
| `l3_solar_req_05` | Required — Ap, single day | 132 | **number** — a decision; it is what moves other rows | **EASY, AS A PAIR** | free only if `l3_solar_ach_05` goes with it — it is read by nothing else |
| `sw_window_peak_level` | F10.7 peak level inside the mission window | computed | **curve** over `sys_mission_requirements_mission_duration` (75%), +1 | **EASY** | nothing reads it and no figure draws it |

---

# MATLAB — 18 rows that port the study

These produce the twenty-five numbers of the driver set. `✓` marks a `parity.csv`: the MATLAB tool's own answer, kept as a second opinion rather than as a fixture. **None of these should be removed** — together they are the port, and the tree has nothing else that answers what they answer.

## Ported, drawn by a figure — 1 rows

| row | what it answers | value | number or curve | remove? | why |
|---|---|---|---|---|---|
| `sw_central_expectation` | F10.7 central expectation at a lead | computed | **curve** over `sys_mission_requirements_mission_epoch` (30%), +1 | **KEEP** | 3 live rows read it: `l3_solar_interface`, `sw_f107_cold_long`, `sw_f107_design_long` |

## Ported, no figure — 17 rows

Not removal candidates. This is the list of figures that have not been drawn yet.

| row | what it answers | value | number or curve | remove? | why |
|---|---|---|---|---|---|
| `l3_solar_interface` | Solar weather — subsystem interface | computed | **curve** over `sys_mission_requirements_mission_epoch` (25%), +1 | **KEEP** | 5 live rows read it: `sys_space_environment_ap`, `sys_space_environment_f10_7`, `sys_space_environment_f10_7_81day`, `sys_space_environment_kp` |
| `sw_ap_central_expectation` ✓ | Expected Ap over the mission window | 22.0954 | **number** — a decision; it is what moves other rows | **KEEP** | 4 live rows read it: `l3_solar_interface`, `sw_ap_cold_long`, `sw_ap_design_long`, `sw_kp_scenarios` |
| `sw_ap_cold_long` ✓ | Sustained cold Ap to design to | computed | **number here** — 2 upstream decision(s) refuse at a range end | **KEEP** | 4 live rows read it: `l3_solar_interface`, `sw_ap_cold_short`, `sw_ap_daily_band_drop`, `sw_kp_scenarios` |
| `sw_ap_cold_short` ✓ | Single-day cold Ap to design to | computed | **number here** — 2 upstream decision(s) refuse at a range end | **KEEP** | 2 live rows read it: `l3_solar_interface`, `sw_kp_scenarios` |
| `sw_ap_daily_band_drop` | Within-rotation daily Ap drop | computed | **number here** — 2 upstream decision(s) refuse at a range end | **KEEP** | 1 live row read it: `sw_ap_cold_short` |
| `sw_ap_daily_band_spread` | Within-rotation daily Ap spread | computed | **curve** over `sw_ap_central_expectation` (90%), +1 | **KEEP** | 1 live row read it: `sw_ap_design_short` |
| `sw_ap_design_long` ✓ | Sustained Ap to design to | computed | **curve** over `sw_ap_central_expectation` (300%), +1 | **KEEP** | 5 live rows read it: `l3_solar_ach_04`, `l3_solar_interface`, `sw_ap_daily_band_spread`, `sw_ap_design_short` |
| `sw_ap_design_short` ✓ | Single-day Ap to design to | computed | **curve** over `sw_ap_central_expectation` (152%), +1 | **KEEP** | 3 live rows read it: `l3_solar_ach_05`, `l3_solar_interface`, `sw_kp_scenarios` |
| `sw_ap_mean_band_spread` ✓ | Ap rotation-forecast residual spread | 3.59369 | **number** — a decision; it is what moves other rows | **KEEP** | 2 live rows read it: `sw_ap_cold_long`, `sw_ap_design_long` |
| `sw_daily_band_drop` | Within-rotation daily drop | computed | **curve** over `sys_mission_requirements_mission_epoch` (247%), +1 | **KEEP** | 1 live row read it: `sw_f107_cold_short` |
| `sw_daily_band_spread` | Within-rotation daily spread | computed | **curve** over `sw_mean_band_spread` (130%), +2 | **KEEP** | 1 live row read it: `sw_f107_design_short` |
| `sw_f107_cold_long` ✓ | Sustained cold F10.7 to design to | computed | **curve** over `sys_mission_requirements_mission_epoch` (38%), +1 | **KEEP** | 3 live rows read it: `l3_solar_interface`, `sw_daily_band_drop`, `sw_f107_cold_short` |
| `sw_f107_cold_short` ✓ | Single-day cold F10.7 to design to | computed | **curve** over `sys_mission_requirements_mission_epoch` (24%), +1 | **KEEP** | 1 live row read it: `l3_solar_interface` |
| `sw_f107_design_long` ✓ | Sustained F10.7 to design to | computed | **curve** over `sw_mean_band_spread` (74%), +2 | **KEEP** | 4 live rows read it: `l3_solar_ach_01`, `l3_solar_interface`, `sw_daily_band_spread`, `sw_f107_design_short` |
| `sw_f107_design_short` ✓ | Single-day F10.7 to design to | computed | **curve** over `sw_mean_band_spread` (83%), +2 | **KEEP** | 2 live rows read it: `l3_solar_ach_02`, `l3_solar_interface` |
| `sw_kp_scenarios` ✓ | Kp in both slots, for all five scenarios | computed | **number here** — 2 upstream decision(s) refuse at a range end | **KEEP** | 1 live row read it: `l3_solar_interface` |
| `sw_mean_band_spread` ✓ | Rotation-forecast residual spread | 13.4544 | **number** — a decision; it is what moves other rows | **KEEP** | 2 live rows read it: `sw_f107_cold_long`, `sw_f107_design_long` |

---

## The two kernel rows

`sw_kp_from_ap` and `sw_regime` carry the study's relation but compute nothing live: the relation moved into `vleo-core` so a driver set could evaluate it at five scenarios rather than one. They sit under EXTRA above because `l3_solar_interface` does not reach them, and they are the provenance of a kernel constant rather than dead weight.
