# The solar subsystem, row by row

Generated by `tools/solar_rows.py`. Do not edit by hand.

## The two questions this answers

**Is it a port of the legacy MATLAB?** The legacy tool is `01_kernel/sw_study` — eight tabs, 38 analysis methods, ~57 views over one database. A row counts as a port when it holds a `parity.csv` (the legacy run's own answer for that row), or when `docs/MATLAB_PORT_PLAN.md` names the legacy routine it came from, or when it sits in the chain that reproduces the legacy driver table. The rest this tree added.

**Does a figure go with it?** The eight legacy tabs were ported as *panels*, not as rows — §12 of the plan, on the grounds that a wrong number crashes a test while a wrong chart looks beautiful, so a panel carries a reference image and an automated check instead. Most panels plot the NOAA record and name the rows they argue about, so for those 'has a figure' means **a panel cites it**. The two Part D panels are the exception and plot ROW ANSWERS directly: `drivers` draws the twenty-five variables `l3_solar_interface` publishes against the legacy run's own, and `closure` draws each required/achieved pair and the margin between them, swept over whichever decision `/v1/levers` reports as spending that margin fastest.

**No row here duplicates another.** All 55 were swept over seven drivers across their declared ranges and fingerprinted; every fingerprint is distinct. Nothing is removable for being a repeat.

| | with a figure | no figure | total |
|---|---|---|---|
| **Ported** from the legacy MATLAB | 37 | 0 | 37 |
| **Added** by this tree | 18 | 0 | 18 |

**0 rows are free to remove**: none.

---

# 1 · Ported, and a figure goes with it — 37 rows

The legacy tab these belong to was ported as a panel, so the picture came across with the row. None of these is a removal candidate.

| row | what it answers | value | number or curve | ported from | remove? |
|---|---|---|---|---|---|
| `l3_solar_interface` | Solar weather — subsystem interface | computed | **curve** over `sys_mission_requirements_mission_epoch` (25%), +1 | the chain that reproduces the legacy driver table | **KEEP** — it is the port |
| `sw_activity_band` | F10.7 activity band | computed | **curve** over `env_f107` (100%) | `prf_segment` | **KEEP** — it is the port |
| `sw_ap_central_expectation` | Expected Ap over the mission window | 22.0954 | **number** — a decision; it moves other rows | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_ap_cold_long` | Sustained cold Ap to design to | computed | **number here** — 2 upstream decision(s) refuse at a range end | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_ap_cold_short` | Single-day cold Ap to design to | computed | **number here** — 2 upstream decision(s) refuse at a range end | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_ap_daily_band_drop` | Within-rotation daily Ap drop | computed | **number here** — 2 upstream decision(s) refuse at a range end | the chain that reproduces the legacy driver table | **KEEP** — it is the port |
| `sw_ap_daily_band_spread` | Within-rotation daily Ap spread | computed | **curve** over `sw_ap_central_expectation` (90%), +1 | the chain that reproduces the legacy driver table | **KEEP** — it is the port |
| `sw_ap_design` | Ap design value | computed | **curve** over `sw_storm_design_level` (64%) | `prf_design` | **KEEP** — it is the port |
| `sw_ap_design_long` | Sustained Ap to design to | computed | **curve** over `sw_ap_central_expectation` (300%), +1 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_ap_design_short` | Single-day Ap to design to | computed | **curve** over `sw_ap_central_expectation` (152%), +1 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_ap_mean_band_spread` | Ap rotation-forecast residual spread | 3.59369 | **number** — a decision; it moves other rows | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_band_coverage` | Stated band coverage | 0.950927 | **number** — a decision; it moves other rows | `F.band_cov` | **KEEP** — it is the port |
| `sw_central_expectation` | F10.7 central expectation at a lead | computed | **curve** over `sys_mission_requirements_mission_epoch` (30%), +1 | `prf_design` | **KEEP** — it is the port |
| `sw_cycle_phase` | Solar cycle phase | computed | **number here** — 1 upstream decision(s) refuse at a range end | `prf_cycles` | **KEEP** — it is the port |
| `sw_cycle_repeatability` | Cycle-to-cycle repeatability | 0.77014 | **number** — a decision; it moves other rows | `C.repeat` | **KEEP** — it is the port |
| `sw_daily_band_drop` | Within-rotation daily drop | computed | **curve** over `sys_mission_requirements_mission_epoch` (247%), +1 | the chain that reproduces the legacy driver table | **KEEP** — it is the port |
| `sw_daily_band_spread` | Within-rotation daily spread | computed | **curve** over `sw_mean_band_spread` (130%), +2 | the chain that reproduces the legacy driver table | **KEEP** — it is the port |
| `sw_event_duration` | F10.7 burst duration | 286111 | **number** — a decision; it moves other rows | `prf_events` | **KEEP** — it is the port |
| `sw_f107_cold_long` | Sustained cold F10.7 to design to | computed | **curve** over `sys_mission_requirements_mission_epoch` (38%), +1 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_f107_cold_short` | Single-day cold F10.7 to design to | computed | **curve** over `sys_mission_requirements_mission_epoch` (24%), +1 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_f107_design_long` | Sustained F10.7 to design to | computed | **curve** over `sw_mean_band_spread` (74%), +2 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_f107_design_short` | Single-day F10.7 to design to | computed | **curve** over `sw_mean_band_spread` (83%), +2 | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_forecast_bias` | Issued-outlook F10.7 bias | computed | **curve** over `sw_recurrence_lag` (45%) | `F.bias_fc` | **KEEP** — it is the port |
| `sw_forecast_skill` | Issued-outlook skill against persistence | computed | **curve** over `sw_recurrence_lag` (868%) | `F.skill_clim` | **KEEP** — it is the port |
| `sw_horizon_climatology` | F10.7 error from climatology at a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (1.87%) | `F.horizon_clim` | **KEEP** — it is the port |
| `sw_horizon_persistence` | F10.7 error from persistence at a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (33%) | `F.horizon_persist` | **KEEP** — it is the port |
| `sw_kp_from_ap` | Kp from daily Ap | computed | **curve** over `sys_mission_requirements_mission_duration` (32%) | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_kp_scenarios` | Kp in both slots, for all five scenarios | computed | **number here** — 2 upstream decision(s) refuse at a range end | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_mean_band_spread` | Rotation-forecast residual spread | 13.4544 | **number** — a decision; it moves other rows | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_mean_cycle_level` | Mean-cycle F10.7 at a phase | computed | **number here** — 1 upstream decision(s) refuse at a range end | `prf_cycles` | **KEEP** — it is the port |
| `sw_recurrence_lag` | Rotation recurrence lag | 2.2464e+06 | **number** — a decision; it moves other rows | `C.rot_peak_lag` | **KEEP** — it is the port |
| `sw_recurrence_strength` | Rotation recurrence strength | 0.375988 | **number** — a decision; it moves other rows | `C.rot_peak_r` | **KEEP** — it is the port |
| `sw_regime` | Geomagnetic regime | computed | **number** — all 1 decisions upstream leave it unchanged | the legacy run's own answer, in `parity.csv` | **KEEP** — it is the port |
| `sw_semiannual_amplitude` | Semiannual Ap amplitude | 1.27811 | **number** — a decision; it moves other rows | `Climate tab` | **KEEP** — it is the port |
| `sw_spike_threshold` | F10.7 spike threshold | 1.30487 | **number** — a decision; it moves other rows | `prf_spikes` | **KEEP** — it is the port |
| `sw_storm_return_level` | Ap at a storm return period | computed | **curve** over `sys_mission_requirements_mission_duration` (88%) | `prf_design` | **KEEP** — it is the port |
| `sw_uncertainty_growth` | F10.7 growth at 95% over a lead | computed | **curve** over `sys_mission_requirements_mission_duration` (51%) | `prf_design` | **KEEP** — it is the port |

---

# 2 · Ported, no figure — 0 rows

**And the reason is not an omission.** These reproduce the legacy DRIVER TABLE — five scenarios over f107, f107bar, ap, kp_mean and kp_peak. That was a table in the legacy tool too, not a plot, so there was never a figure here to port. If a picture of the driver set is wanted it has to be drawn new; that is the one real gap in this subsystem's figures.

| row | what it answers | value | number or curve | ported from | remove? |
|---|---|---|---|---|---|

---

# 3 · Added, and a figure cites it — 18 rows

Rows this tree added, which a ported panel then leans on. Removing one leaves its panel arguing about a row that is gone.

| row | what it answers | value | number or curve | figure | remove? |
|---|---|---|---|---|---|
| `l3_solar_ach_01` | Closure — F10.7, sustained | computed | **curve** over `l3_solar_req_01` (246%), +3 | closure | **COSTS A FIGURE** — the closure panel argues with it |
| `l3_solar_ach_02` | Closure — F10.7, single day | computed | **curve** over `l3_solar_req_02` (273%), +3 | closure | **COSTS A FIGURE** — the closure panel argues with it |
| `l3_solar_ach_03` | Closure — Ap, survival | computed | **curve** over `l3_solar_req_03` (3203%), +1 | closure/design | **COSTS A FIGURE** — the closure/design panel argues with it |
| `l3_solar_ach_04` | Closure — Ap, sustained | computed | **curve** over `sw_ap_central_expectation` (376%), +2 | closure | **COSTS A FIGURE** — the closure panel argues with it |
| `l3_solar_ach_05` | Closure — Ap, single day | computed | **curve** over `sw_ap_central_expectation` (331%), +2 | closure | **COSTS A FIGURE** — the closure panel argues with it |
| `l3_solar_req_01` | Required — F10.7, sustained | 260 | **number** — a decision; it moves other rows | closure/design | **KEEP** — `l3_solar_ach_01` read it |
| `l3_solar_req_02` | Required — F10.7, single day | 350 | **number** — a decision; it moves other rows | closure | **KEEP** — `l3_solar_ach_02` read it |
| `l3_solar_req_03` | Required — Ap, survival | 207 | **number** — a decision; it moves other rows | closure/design | **KEEP** — `l3_solar_ach_03` read it |
| `l3_solar_req_04` | Required — Ap, sustained | 48 | **number** — a decision; it moves other rows | closure | **KEEP** — `l3_solar_ach_04` read it |
| `l3_solar_req_05` | Required — Ap, single day | 132 | **number** — a decision; it moves other rows | closure | **KEEP** — `l3_solar_ach_05` read it |
| `sw_design_safe_duration` | How long the design Ap lasts | computed | **curve** over `sw_storm_design_level` (87%) | design | **COSTS A FIGURE** — the design panel argues with it |
| `sw_exceedance_duration` | How long one exceedance lasts | computed | **curve** over `sw_storm_design_level` (11%) | design | **COSTS A FIGURE** — the design panel argues with it |
| `sw_exceedance_phase` | When in the cycle exceedances fall | computed | **curve** over `sw_storm_design_level` (5.14%) | design | **COSTS A FIGURE** — the design panel argues with it |
| `sw_exceedance_rate` | Days a year above the design level | computed | **curve** over `sw_storm_design_level` (1538%) | design | **COSTS A FIGURE** — the design panel argues with it |
| `sw_f107a_ratio` | Daily F10.7 scatter about F10.7A | 0.12221 | **number** — a decision; it moves other rows | climate | **COSTS A FIGURE** — the climate panel argues with it |
| `sw_kp_slot_bias` | Kp slot bias, daily peak | computed | **curve** over `sys_mission_requirements_mission_duration` (8.59%) | climate | **COSTS A FIGURE** — the climate panel argues with it |
| `sw_storm_design_level` | Storm level designed for | 3 | **number** — a decision; it moves other rows | design | **KEEP** — `sw_ap_design` read it |
| `sw_window_peak_level` | F10.7 peak level inside the mission window | computed | **curve** over `sys_mission_requirements_mission_duration` (75%), +1 | design | **COSTS A FIGURE** — the design panel argues with it |

---

# 4 · Added, no figure — 0 rows

Neither in the legacy tool nor cited by a panel. Every free removal is here.

| row | what it answers | value | number or curve | figure | remove? |
|---|---|---|---|---|---|
