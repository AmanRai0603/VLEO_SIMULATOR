# A bundle says what it is, where it came from, and until when it may be used.
#
# Immutable and versioned: a published version is never edited, and a
# correction is a new version. That is what lets a ledger row from eighteen
# months ago be reproduced exactly.
name = "solar-drivers"
version = "2026.09.04"
provenance = "noaa_swpc"
licence_until = "2031-12-31"
stale_after_days = 120
note = """
Monthly means, not daily values. Sufficient for design work, and stated as such
so nobody mistakes it for an operational driver feed: a design sized on a
monthly mean is sized on a month that did not happen. The daily record is a
larger bundle with the same name and a later version, and syncing it changes
nothing in the tool but the numbers.
"""
files = ["drivers.csv"]
# Filled by `xtask bundle publish`; publishing twice from the same input gives
# the same hash, which is what makes the check meaningful.
content_hash = "54784567db11b868"
